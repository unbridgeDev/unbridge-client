export const DOCS_PAGES = [
  {
    "slug": "index",
    "title": "Overview",
    "body": "# Unbridge\n\n**Threshold-signature coordination and aggregation on Solana.** One Solana account signs natively for any chain — no bridge contracts, no wrapped assets, no lock-and-mint. Solana is the control plane: operators bond slashable LST collateral, users post signing intents as instructions, and the program coordinates the ceremony at 400 ms per-slot resolution so multi-round MPC completes in near-real-time.\n\n> Program ID: `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6`\n\n---\n\n## Verify it yourself\n\nUnbridge is not a thought experiment. The core is built, independently verified milestone by milestone, and the reconciled program is deployed and live on Solana **devnet** (devnet only, not mainnet). Every claim below is something you can reproduce:\n\n- **FROST Ed25519 (`engine/kobe`):** `cd engine/kobe && cargo test` runs a 2-of-3 FROST ceremony and verifies the 64-byte aggregate two ways — FROST's own verify and the independent `ed25519-dalek` crate, the exact RFC 8032 primitive a Solana or Cosmos node runs. Wrong message, tampered signature, wrong group key, and sub-threshold quorum all fail. The group secret is never reconstructed.\n- **GG20 ECDSA (`engine/kobe-ecdsa`):** `cd engine/kobe-ecdsa && go test -v -timeout 600s` (~110s; safe-prime DKG is genuinely slow). The secp256k1 signature is checked natively valid on three chains with independent verifiers: Ethereum (`go-ethereum` `Ecrecover` recovers the group address), Bitcoin (BIP-143 sighash, DER + SIGHASH_ALL under decred secp256k1, P2WPKH bech32 against the BIP-173 vector, low-S enforced), and Tron (keccak → 0x41 → base58check against a known vector).\n- **On-chain coordination loop (`engine/coordinator`):** the on-chain SigningRequest drives the off-chain MPC, the real aggregate is posted back via `aggregate_and_emit`, and the result is independently verified end-to-end on a local validator (`engine/coordinator/m7-demo.sh`).\n- **Live program:** verify the deploy yourself with `solana program show 4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6 --url devnet` (deploy tx `3LAt17P8Zmh2EYyphzVF4EHNY1uffkaJp7cp4V2yUpNh3MoV82iP5mEvdg18XMBUnKwANDGANCj43mEVRJUh6ZAQ`).\n\nNothing here is audited for real value and there is no token. The honest scope is on the Security & Trust page.\n\n---\n\n## The Problem Unbridge Solves\n\nCross-chain bridging forces every asset to travel through a lock-and-mint contract. The contract holds custody, creates a synthetic representation, and introduces a new trust assumption at every hop. The canonical failure mode is well-documented: the custodial bridge becomes the most valuable attack target on the destination chain.\n\nUnbridge reverses the model. Instead of moving assets to a chain where a user's key does not exist, Unbridge extends the user's existing key to every chain by producing a valid native signature for that chain's VM — Ed25519 for Aptos/Sui/Solana-style VMs, secp256k1 ECDSA for Ethereum/BTC/Tron. No asset moves. No custody. The destination chain sees a signature that is indistinguishable from one produced by the user's local key.\n\nThe coordination bottleneck in threshold-signature systems has historically been the multi-round MPC ceremony itself: FROST requires two rounds of communication among all signers; GG20 requires three or more. The reason Solana is used as the control plane rather than Ethereum or an L2 is blunt arithmetic: 400 ms slot time collapses a three-round ceremony that would take 45 seconds on a 15-second block chain into under two seconds of on-chain latency.\n\n---\n\n## High-Level Architecture\n\n```mermaid\nsequenceDiagram\n    participant User\n    participant UnbridgeProgram as Unbridge Program (Solana)\n    participant Coordinator as Coordinator (off-chain MPC)\n    participant KobeLib as engine/kobe (FROST) + engine/kobe-ecdsa (GG20)\n    participant DestChain as Destination Chain\n\n    User->>UnbridgeProgram: create_signing_request(scheme, target_vm, message_hash, threshold, validity_slots)\n    Note over UnbridgeProgram: Charges request_fee lamports<br/>Snapshots required_stake_weight<br/>Opens SigningRequest PDA<br/>Emits SigningRequestCreated\n\n    loop Until threshold met or request expires\n        Coordinator->>KobeLib: run FROST/GG20 round on message_hash\n        Coordinator->>UnbridgeProgram: submit_partial_signature\n        Note over UnbridgeProgram: Verifies operator not jailed<br/>PDA uniqueness prevents double-submit<br/>Records a participation receipt:<br/>partials_collected += 1, stake_weight_collected += weight<br/>Emits PartialSignatureSubmitted\n    end\n\n    KobeLib-->>Coordinator: canonical 64-byte group signature\n    Coordinator->>UnbridgeProgram: aggregate_and_emit(aggregate_sig[64])<br/>once partials_collected ≥ threshold AND stake_weight_collected ≥ required_stake_weight\n    Note over UnbridgeProgram: Checks status, expiry, thresholds, non-zero sig<br/>Records aggregate_sig[64], sets status = Aggregated<br/>Emits AggregateSignatureEmitted\n\n    UnbridgeProgram-->>DestChain: Off-chain relayer reads aggregate_sig<br/>broadcasts native tx to destination VM\n```\n\nThe on-chain program is responsible exclusively for: bonded-collateral accounting, threshold enforcement, liveness deadlines, and slashing. The partial-signature step records a participation receipt only — it does not fold or combine share bytes, because summing FROST/GG20 shares is cryptographically meaningless and the SVM has no curve arithmetic for the group-combine. The real group signature is computed off-chain by the coordinator (real FROST round1/round2 with `engine/kobe` for Ed25519, or GG20 with `engine/kobe-ecdsa` for secp256k1) and then posted into `aggregate_and_emit`, which records and emits it after checking the thresholds and that the signature is non-zero.\n\n---\n\n## Core Concepts\n\n### Signing Intent (SigningRequest)\n\nA user posts a 32-byte `message_hash` — the hash of the transaction or payload they want signed on the destination chain — alongside a `scheme`, `target_vm`, `target_chain_id`, and a `validity_slots` window. The program snapshots the current economic-security target at that moment:\n\n```\nrequired_stake_weight = total_bonded * threshold_bps / BPS_DENOMINATOR\n```\n\n`BPS_DENOMINATOR` is fixed at `10_000`. If `threshold_bps = 6_667`, at least two-thirds of total bonded weight must contribute partial signatures before the request can finalize. The snapshot is immutable for the life of the request, insulating it from operators joining or leaving mid-flight.\n\nThe request expires at `created_slot + validity_slots`. The hard ceiling is `MAX_VALIDITY_SLOTS_CEILING = 432_000` slots, which is approximately 48 hours at 400 ms per slot. Requests that are not finalized by `expiry_slot` can be cancelled and their status set to `Expired`.\n\n### Signing Scheme Routing\n\nUnbridge branches on two cryptographic families, set on the `SigningRequest` by the requester at intent creation. The scheme is fixed by the request; a partial carries the request's scheme rather than a caller-supplied one, so there is no untrusted-scheme input path to reject:\n\n| `SignatureScheme` variant | Algorithm | Off-chain signer | Target VM family |\n|---|---|---|---|\n| `FrostEd25519` | FROST Schnorr over Ed25519 | `engine/kobe` (Rust) | `Svm`, Cosmos SDK chains |\n| `Gg20Secp256k1` | GG20 threshold ECDSA over secp256k1 | `engine/kobe-ecdsa` (Go) | `Evm`, `Tron`, `Bitcoin` |\n\nThe `TargetVm` enum values are: `Svm`, `Evm`, `Tron`, `Cosmos`, `Bitcoin`. `target_chain_id` carries the chain-specific identifier (EVM chain id, Cosmos chain index, etc.) and is opaque to the on-chain program — it is passed through to the aggregate output so the relayer knows where to broadcast.\n\n### Operator Bond and Stake Weight\n\nOperators are not distinguished by count alone — they are weighted by economic stake. An operator bonds Token-2022 LST into the protocol-owned `bond_vault`. The program converts this raw LST amount into a SOL-denominated `stake_weight` via a Pyth price feed (`lst_price_feed` on the `Protocol` account, validated against `InvalidOracleAccount` and `StaleOraclePrice`). This weight is what accumulates in `stake_weight_collected` on each signing request.\n\nThe minimum bond to join the signing set is the protocol-configured `min_bond`. Operators below `min_bond` after a slash are automatically jailed (`jailed = true`); they can no longer sign new requests.\n\n### Slashing and Jailing\n\nThe `slash_operator` instruction moves `amount` LST from `bond_vault` to `slash_pool` (a separate protocol-owned Token-2022 account). The `reason: u8` field is an opaque fraud-proof classifier produced by the off-chain signing library — the on-chain program enforces the economic effect without verifying the cryptographic proof itself. After slashing:\n\n- `bonded_amount` is reduced\n- `stake_weight` is recomputed from the residual bond\n- If `bonded_amount < min_bond`, `jailed = true`\n- If the operator was previously active, `total_bonded` and `operator_count` on the `Protocol` account are updated immediately\n\nSlashed funds accumulate in `slash_pool` and are not automatically redistributed; the protocol admin controls their disposition.\n\n### Unbonding Delay\n\nTo prevent an operator from front-running a fraud proof by immediately withdrawing their bond, `begin_unbonding` sets `unbonding_at = current_slot + unbonding_slots` and jails the operator. Only after `clock.slot >= unbonding_at` can `withdraw_bond` execute the Token-2022 transfer back to the operator's account. The `unbonding_slots` parameter is admin-configurable via `update_config`.\n\n---\n\n## Account Architecture\n\nAll accounts derive `InitSpace` from their field set. Rent is computed as `8 (discriminator) + T::INIT_SPACE` bytes.\n\n| Account | PDA Seeds | INIT_SPACE | Total (with disc) | Count |\n|---|---|---|---|---|\n| `Protocol` | `[b\"protocol\"]` | 248 bytes | 256 bytes | 1 (singleton) |\n| `Operator` | `[b\"operator\", protocol, authority]` | 143 bytes | 151 bytes | 1 per operator |\n| `SigningRequest` | `[b\"request\", protocol, request_id_le]` | 224 bytes | 232 bytes | 1 per request |\n| `PartialSignature` | `[b\"partial\", request, operator]` | 146 bytes | 154 bytes | 1 per (request × operator) |\n\nTwo additional Token-2022 accounts are owned by the protocol PDA but are not Anchor accounts with discriminators:\n\n| Account | PDA Seeds | Purpose |\n|---|---|---|\n| `bond_vault` | `[b\"bond_vault\", protocol]` | Holds active operator bonds |\n| `slash_pool` | `[b\"slash_pool\", protocol]` | Accumulates slashed collateral |\n\nThe `PartialSignature` PDA's seeds — `[b\"partial\", request, operator]` — enforce uniqueness at the account layer. An operator that attempts to submit a second partial signature to the same request will trigger an `AccountAlreadyInitialized` error from the Anchor runtime before the instruction body is reached.\n\n---\n\n## Protocol Account Fields\n\nThe singleton `Protocol` account (PDA `[b\"protocol\"]`) holds all global parameters and live accounting:\n\n```rust\npub struct Protocol {\n    pub admin: Pubkey,             // current admin authority\n    pub pending_admin: Pubkey,     // nominee in two-step handover; Pubkey::default() if unset\n    pub bond_mint: Pubkey,         // Token-2022 LST mint accepted as collateral\n    pub bond_vault: Pubkey,        // protocol-owned vault holding active bonds\n    pub slash_pool: Pubkey,        // protocol-owned pool for slashed collateral\n    pub lst_price_feed: Pubkey,    // Pyth price account for LST→SOL valuation\n    pub threshold_bps: u16,        // fraction of total_bonded required per request (bps)\n    pub min_bond: u64,             // minimum LST bond to join the signing set\n    pub unbonding_slots: u64,      // delay between begin_unbonding and withdraw_bond\n    pub request_fee: u64,          // lamports charged per signing request\n    pub max_validity_slots: u64,   // hard ceiling on request expiry (≤ 432_000)\n    pub operator_count: u32,       // active operators (excludes jailed / unbonding)\n    pub total_bonded: u64,         // sum of active operators' stake_weight\n    pub request_nonce: u64,        // monotonic counter seeding request PDAs\n    pub paused: bool,              // emergency pause flag\n    pub bump: u8,\n}\n```\n\n`threshold_bps` must satisfy `1 ≤ threshold_bps ≤ 10_000` at both `initialize` and `update_config` time; values outside this range return `InvalidThreshold`.\n\n---\n\n## Operator Lifecycle\n\n```mermaid\nstateDiagram-v2\n    [*] --> Active: register_operator(group_pubkey, bond_amount ≥ min_bond)\n    Active --> Jailed_Unbonding: begin_unbonding()\n    Active --> Jailed_Slashed: slash_operator() reduces bond < min_bond\n    Jailed_Slashed --> Active: (no automatic path — admin must top up bond externally)\n    Jailed_Unbonding --> Withdrawn: withdraw_bond() after clock.slot ≥ unbonding_at\n    Withdrawn --> [*]: operator account closed; rent returned to authority\n```\n\nAn operator in `Active` state has `jailed = false` and `unbonding_at = 0`. Both `jailed` flags (`AlreadyUnbonding` if `unbonding_at != 0`, `OperatorJailed` if `jailed = true`) block partial-signature submission. `begin_unbonding` is a one-way door per-account: it immediately sets `jailed = true`, decrements `protocol.operator_count`, and subtracts the operator's `stake_weight` from `protocol.total_bonded`.\n\nThe `group_pubkey: [u8; 33]` field on the `Operator` account stores the operator's 33-byte compressed group public key, used by the off-chain `engine/kobe` / `engine/kobe-ecdsa` libraries to verify share provenance. It is set at registration and is immutable.\n\n---\n\n## Signing Request Lifecycle\n\n```mermaid\nstateDiagram-v2\n    [*] --> Pending: create_signing_request()\n    Pending --> Aggregated: aggregate_and_emit() — stake_weight_collected ≥ required_stake_weight\n    Pending --> Expired: clock.slot > expiry_slot\n    Pending --> Cancelled: cancel_request() (requester)\n    Aggregated --> [*]: relayer reads aggregate_sig[64], broadcasts to destination VM\n    Expired --> [*]\n    Cancelled --> [*]\n```\n\nEvery `SigningRequest` stores an `aggregate_sig: [u8; 64]` field, zero until finalization. The chain does not accumulate or fold this signature on-chain. The off-chain coordinator computes the canonical group signature and posts it into `aggregate_and_emit`, which records it into this field and transitions `status` to `Aggregated`. An off-chain relayer observes the `AggregateSignatureEmitted` event and broadcasts the native transaction to the destination chain — the relayer carries no signing key of its own.\n\n`partials_collected: u16` counts the number of distinct operators that have submitted, while `stake_weight_collected: u64` tracks the economic weight. Both are incremented atomically in the same instruction as the `PartialSignature` account is created. The threshold check at finalization requires:\n\n```\nstake_weight_collected ≥ required_stake_weight\n```\n\n`required_stake_weight` is snapshotted at `create_signing_request` time so that an operator exiting or being slashed between intent creation and finalization cannot retroactively invalidate a legitimate collection window.\n\n---\n\n## Protocol Constants\n\n| Constant | Value | Meaning |\n|---|---|---|\n| `BPS_DENOMINATOR` | `10_000` | Divisor for all basis-point calculations |\n| `MAX_VALIDITY_SLOTS_CEILING` | `432_000` | Hard ceiling on `max_validity_slots` (~48 h at 400 ms/slot) |\n\nThe `request_fee` is denominated in **lamports** and transferred via `system_program::transfer` to the protocol PDA at `create_signing_request` time. It is separate from Solana rent, which is charged normally for the `SigningRequest` account itself.\n\n---\n\n## Error Reference Summary\n\nAll errors are defined in `UnbridgeError`. There are exactly 22 variants. The table below lists every variant and the guard that triggers it:\n\n| Error | Trigger |\n|---|---|\n| `ProtocolPaused` | Any state-transition instruction while `protocol.paused = true` |\n| `Unauthorized` | `accept_admin` called by non-`pending_admin` |\n| `InvalidThreshold` | `threshold_bps` outside `[1, 10_000]` |\n| `InsufficientBond` | `bond_amount < min_bond` at registration, or `min_bond = 0` at config |\n| `OperatorJailed` | Partial submission by a jailed or unbonding operator |\n| `AlreadyUnbonding` | `begin_unbonding` when `unbonding_at != 0` |\n| `NotUnbonding` | `withdraw_bond` when `unbonding_at == 0` |\n| `UnbondingNotComplete` | `withdraw_bond` before `clock.slot >= unbonding_at` |\n| `RequestExpired` | Operation on a request past `expiry_slot` |\n| `RequestNotPending` | Partial submission or finalization on a non-`Pending` request |\n| `ThresholdNotMet` | Finalization attempted before threshold is satisfied |\n| `RequestAlreadyFinalized` | Second finalization attempt on `Aggregated` request |\n| `MalformedPartialSignature` | 64-byte share fails basic structure validation |\n| `EmptyMessageHash` | `message_hash` is all zero bytes |\n| `StaleOraclePrice` | Pyth feed price is stale at bond or weight computation |\n| `InvalidOracleAccount` | Oracle account key does not match `protocol.lst_price_feed` |\n| `InvalidVault` | Vault or pool account passed does not match the protocol's stored key |\n| `InvalidValidityWindow` | `validity_slots` outside `[1, max_validity_slots]` or exceeds `432_000` |\n| `NoActiveOperators` | `create_signing_request` when `operator_count == 0` |\n| `SlashAmountExceedsBond` | `amount > operator.bonded_amount` in `slash_operator` |\n| `InvalidAdminTransfer` | `new_admin == Pubkey::default()` in `transfer_admin` |\n| `MathOverflow` | Any checked arithmetic saturates (counters, weight accumulation) |\n\n---\n\n## What Unbridge Does Not Do\n\nBeing precise about scope matters for integration planning:\n\n- **Cryptographic share verification is off-chain.** The on-chain program does not verify FROST commitments or GG20 zero-knowledge proofs. That work is delegated to the `engine/kobe` / `engine/kobe-ecdsa` signing libraries. The program verifies economic accounting and uniqueness; the signing libraries verify cryptographic correctness.\n- **Relaying is off-chain.** After `status = Aggregated`, the `aggregate_sig[64]` stored in the `SigningRequest` account is read by an off-chain relayer that constructs and broadcasts the native transaction to the destination chain. No on-chain CPI to any destination chain is possible; this is an inherent constraint of Solana's execution environment.\n- **No key generation or DKG on-chain.** The distributed key generation ceremony that produces operator shares and the `group_pubkey` is coordinated off-chain. Only the resulting compressed group public key (`[u8; 33]`) is registered on-chain at `register_operator` time.\n- **Slash pool disposition.** Slashed collateral accumulates in `slash_pool` but the program does not implement redistribution logic. Governance over those funds is entirely in the hands of the admin key.\n\n---\n\n## Quick Example: End-to-End Flow\n\nThe following illustrates the minimum happy path for an EVM signing request using the TypeScript SDK (pseudocode — actual SDK types are in the API Reference):\n\n```typescript\n// 1. Derive the singleton protocol PDA\nconst [protocolPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"protocol\")],\n  Unbridge_PROGRAM_ID\n);\n\n// 2. User creates a signing intent for Ethereum mainnet (chain id 1)\n// message_hash is the 32-byte keccak256 of the EVM transaction payload\nconst tx = await program.methods\n  .createSigningRequest(\n    { gg20Secp256k1: {} },        // SignatureScheme::Gg20Secp256k1\n    { evm: {} },                   // TargetVm::Evm\n    new BN(1),                     // target_chain_id = Ethereum mainnet\n    Array.from(messageHash),       // [u8; 32]\n    3,                             // threshold: at least 3 partials required\n    new BN(150)                    // validity_slots: ~60 seconds\n  )\n  .accounts({ requester: wallet.publicKey, protocol: protocolPda, /* ... */ })\n  .rpc();\n\n// 3. Operators observe the event, compute partial shares via engine/kobe-ecdsa,\n//    and each submit a PartialSignature instruction.\n//    The PDA [b\"partial\", request, operator] enforces uniqueness.\n\n// 4. Once threshold is met, anyone can call aggregate_and_emit.\n//    The aggregate_sig[64] field on the SigningRequest account\n//    is written and status transitions to Aggregated.\n\n// 5. The relayer reads aggregate_sig and broadcasts\n//    the signed transaction to Ethereum.\n```\n\n---\n\n## Navigation\n\n| Page | Description |\n|---|---|\n| Architecture | Detailed component diagram: on-chain program, kobe signing libs, relayer |\n| How It Works | Step-by-step instruction execution and state transitions |\n| Security & Trust | Slashing conditions, fraud-proof lifecycle, oracle trust assumptions |\n| Economics | Bond mechanics, fee model, stake-weight computation |\n| Getting Started | Devnet deployment, CLI quickstart |\n| Integration | SDK setup, account derivation, event subscription |\n| API Reference | Full instruction signatures, account schemas, PDA derivation |\n| Error Reference | Every `UnbridgeError` variant with cause and resolution |\n| FAQ | Common integration questions |"
  },
  {
    "slug": "architecture",
    "title": "Architecture",
    "body": "# Architecture\n\n![Unbridge architecture: user posts a signing intent to the Solana control plane, bonded operators threshold-sign, one native signature lands on the destination chain](/architecture.png)\n\n## System Overview\n\nUnbridge is a threshold-signature coordination and aggregation layer deployed as a Solana program. The program's sole responsibility is the *control plane*: accounting for bonded economic security, sequencing signing intents, collecting partial-signature shares, enforcing threshold rules, and publishing finalized aggregate signatures. The cryptographic heavy lifting (DKG, partial-share generation, and the off-chain group-combine) is performed by exactly two off-chain signers — `engine/kobe` (Rust, FROST Ed25519, built on the ZF `frost-ed25519` 3.0 crate) and `engine/kobe-ecdsa` (Go, GG20 threshold ECDSA over secp256k1, built on Binance `tss-lib` v2) — driven by `engine/coordinator`, which uses the on-chain state as a coordination bus. `engine/kobe-ecdsa` also carries the per-chain envelopes (BTC BIP-143/bech32/DER, Tron keccak/base58check) and the networked-operator transport.\n\nSolana's ~400 ms per-slot finality is the architectural premise. A single multi-round MPC protocol that takes 2–4 Solana slots to collect shares (800 ms – 1.6 s) would require minutes on an EVM chain used as the control plane. This slot cadence makes near-real-time threshold signing across all VM families tractable.\n\n**Program ID:** `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6` (live on Solana devnet; verify with `solana program show 4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6 --url devnet`).\n\nThe two off-chain signers are concrete, not placeholders, and each is independently verified: `cd engine/kobe && cargo test` runs a 2-of-3 FROST Ed25519 ceremony and checks the aggregate against both FROST's own verify and the `ed25519-dalek` RFC 8032 primitive; `cd engine/kobe-ecdsa && go test -v -timeout 600s` proves the GG20 secp256k1 signature natively valid on Ethereum (`go-ethereum` `Ecrecover`), Bitcoin (BIP-143), and Tron. The full loop is exercised end-to-end on a local validator by `engine/coordinator/m7-demo.sh`.\n\n---\n\n## Component Architecture\n\n```mermaid\ngraph TB\n    subgraph On-chain [\"On-chain Program (distin)\"]\n        PC[\"Protocol\\n[singleton PDA]\"]\n        BV[\"Bond Vault\\n[Token-2022 TokenAccount]\"]\n        SP[\"Slash Pool\\n[Token-2022 TokenAccount]\"]\n        OP[\"Operator\\n[PDA per authority]\"]\n        SR[\"SigningRequest\\n[PDA per request_id]\"]\n        PS[\"PartialSignature\\n[PDA per request×operator]\"]\n\n        PC --> BV\n        PC --> SP\n        OP --> PC\n        SR --> PC\n        PS --> SR\n        PS --> OP\n    end\n\n    subgraph Actors\n        ADM[\"Admin\"]\n        OPR[\"Operator Node\"]\n        USR[\"User / Requester\"]\n        RLY[\"Relayer\"]\n    end\n\n    subgraph OffChain [\"Off-chain Signers + Coordinator\"]\n        KOBE[\"engine/kobe\\n(FROST Ed25519, Rust)\"]\n        KOBEEC[\"engine/kobe-ecdsa\\n(GG20 secp256k1, Go)\"]\n        COORD[\"engine/coordinator\\n(drives MPC, posts aggregate)\"]\n    end\n\n    subgraph Oracle\n        PYT[\"Pyth LST Price Feed\\nlst_price_feed: Pubkey\"]\n    end\n\n    ADM -->|initialize / update_config / slash_operator| PC\n    USR -->|create_signing_request + lamport fee| SR\n    OPR -->|register_operator → Token-2022 CPI| BV\n    OPR -->|submit_partial_signature| PS\n    KOBE & KOBEEC <-->|FROST / GG20 rounds| COORD\n    COORD -->|aggregate_and_emit(aggregate_sig)| SR\n    RLY -->|read aggregate_sig from SR| SR\n    PYT -->|compute_stake_weight| OP\n```\n\n---\n\n## Account Layout\n\nUnbridge uses six PDA account types. Every account derives `InitSpace`; rent reservation is `8 (discriminator) + T::INIT_SPACE` bytes.\n\n### Protocol — Singleton Config\n\n**Seeds:** `[b\"protocol\"]`\n**Size:** 248 bytes + 8 discriminator = **256 bytes**\n\n| Field | Type | Bytes | Description |\n|---|---|---|---|\n| `admin` | `Pubkey` | 32 | Current admin authority |\n| `pending_admin` | `Pubkey` | 32 | Nominated successor (two-step handover); `Pubkey::default()` until set |\n| `bond_mint` | `Pubkey` | 32 | Token-2022 LST mint accepted as collateral |\n| `bond_vault` | `Pubkey` | 32 | Protocol-owned vault holding active bonds |\n| `slash_pool` | `Pubkey` | 32 | Protocol-owned pool collecting slashed collateral |\n| `lst_price_feed` | `Pubkey` | 32 | Pyth price account for valuing LST in SOL |\n| `threshold_bps` | `u16` | 2 | Fraction of `total_bonded` required to finalize (basis points; range `1..=10_000`) |\n| `min_bond` | `u64` | 8 | Minimum bond an operator must post |\n| `unbonding_slots` | `u64` | 8 | Slots between `begin_unbonding` and `withdraw_bond` |\n| `request_fee` | `u64` | 8 | Lamport fee charged per signing request |\n| `max_validity_slots` | `u64` | 8 | Upper bound on a request's validity window (hard ceiling: `432_000`) |\n| `operator_count` | `u32` | 4 | Number of operators currently in the active signing set |\n| `total_bonded` | `u64` | 8 | Sum of active operators' staked economic weight |\n| `request_nonce` | `u64` | 8 | Monotonic counter seeding request PDAs |\n| `paused` | `bool` | 1 | Emergency pause flag |\n| `bump` | `u8` | 1 | PDA canonical bump |\n\nThe global threshold check at finalization time computes:\n\n```\nrequired_stake_weight = total_bonded × threshold_bps / BPS_DENOMINATOR\n```\n\nwhere `BPS_DENOMINATOR = 10_000`. A `threshold_bps` of `6_667` therefore requires two-thirds of total staked weight.\n\n---\n\n### Operator — Bonded Signing Node\n\n**Seeds:** `[b\"operator\", protocol, authority]`\n**Size:** 143 bytes + 8 discriminator = **151 bytes**\n\n| Field | Type | Bytes | Description |\n|---|---|---|---|\n| `protocol` | `Pubkey` | 32 | Owning protocol PDA |\n| `authority` | `Pubkey` | 32 | Signer for all operator lifecycle actions |\n| `group_pubkey` | `[u8; 33]` | 33 | Compressed group public key / FROST public-share identifier |\n| `bonded_amount` | `u64` | 8 | Raw LST amount held in the vault |\n| `stake_weight` | `u64` | 8 | SOL-denominated economic weight (oracle-derived via Pyth) |\n| `partials_submitted` | `u64` | 8 | Lifetime count of partial signatures submitted |\n| `slash_count` | `u32` | 4 | Number of times this operator has been slashed |\n| `jailed` | `bool` | 1 | If `true`, operator cannot sign new requests |\n| `unbonding_at` | `u64` | 8 | Slot at which unbonding completes; `0` while actively bonded |\n| `joined_slot` | `u64` | 8 | Slot the operator joined |\n| `bump` | `u8` | 1 | PDA canonical bump |\n\nThe `stake_weight` field is the oracle-adjusted economic weight and is what the threshold arithmetic operates on, not the raw `bonded_amount`. After a slash, `stake_weight` is recomputed from `operator.bonded_amount` via `compute_stake_weight`. An operator whose `bonded_amount` drops below `min_bond` is automatically jailed.\n\n---\n\n### SigningRequest — Cross-VM Signing Intent\n\n**Seeds:** `[b\"request\", protocol, request_id_le]`\n**Size:** 224 bytes + 8 discriminator = **232 bytes**\n\nThe `request_id` seed bytes are the little-endian encoding of `protocol.request_nonce` at the moment of creation. This makes every request PDA globally unique and allows O(1) lookup by `(protocol, request_id)`.\n\n| Field | Type | Bytes | Description |\n|---|---|---|---|\n| `protocol` | `Pubkey` | 32 | Owning protocol PDA |\n| `requester` | `Pubkey` | 32 | Account that posted the intent (rent refund destination on close) |\n| `request_id` | `u64` | 8 | Monotonic id — value of `request_nonce` at creation time |\n| `scheme` | `SignatureScheme` | 1 | `FrostEd25519` or `Gg20Secp256k1` |\n| `target_vm` | `TargetVm` | 1 | `Svm` / `Evm` / `Tron` / `Cosmos` / `Bitcoin` |\n| `target_chain_id` | `u64` | 8 | EVM chain id, Cosmos chain index, etc. |\n| `message_hash` | `[u8; 32]` | 32 | 32-byte hash of the message/transaction to sign |\n| `threshold` | `u16` | 2 | Minimum distinct partial signatures required |\n| `partials_collected` | `u16` | 2 | Partial signatures collected so far |\n| `stake_weight_collected` | `u64` | 8 | Staked economic weight accumulated so far |\n| `required_stake_weight` | `u64` | 8 | Economic-security target snapshotted at creation |\n| `created_slot` | `u64` | 8 | Slot the request was created |\n| `expiry_slot` | `u64` | 8 | Slot after which the request can no longer be fulfilled |\n| `status` | `RequestStatus` | 1 | `Pending` / `Aggregated` / `Cancelled` / `Expired` |\n| `aggregate_sig` | `[u8; 64]` | 64 | Zero until finalization; the off-chain coordinator's canonical group signature, recorded by `aggregate_and_emit` |\n| `bump` | `u8` | 1 | PDA canonical bump |\n\n`required_stake_weight` is snapshotted from `total_bonded × threshold_bps / 10_000` at creation time, not at finalization time. This prevents a race where operators unbond between request creation and threshold collection in order to lower the effective bar.\n\n---\n\n### PartialSignature — Operator's Share Contribution\n\n**Seeds:** `[b\"partial\", request, operator]`\n**Size:** 146 bytes + 8 discriminator = **154 bytes**\n\nThe two-pubkey seed `(request, operator)` enforces uniqueness: a single operator cannot submit two shares for the same request. Any second attempt from the same authority would collide on PDA derivation and fail with an Anchor account-already-exists error before any instruction logic executes.\n\n| Field | Type | Bytes | Description |\n|---|---|---|---|\n| `request` | `Pubkey` | 32 | Request this share contributes to |\n| `operator` | `Pubkey` | 32 | Operator that submitted the share |\n| `scheme` | `SignatureScheme` | 1 | Must match the parent request's scheme |\n| `share` | `[u8; 64]` | 64 | 64-byte partial-signature share material |\n| `submitted_slot` | `u64` | 8 | Slot the share was submitted |\n| `stake_weight` | `u64` | 8 | Staked weight credited for this contribution |\n| `bump` | `u8` | 1 | PDA canonical bump |\n\n---\n\n## PDA Derivation Reference\n\n```rust\n// Protocol (singleton)\nlet (protocol_pda, bump) = Pubkey::find_program_address(\n    &[b\"protocol\"],\n    &program_id,\n);\n\n// Bond vault\nlet (bond_vault_pda, _) = Pubkey::find_program_address(\n    &[b\"bond_vault\", protocol_pda.as_ref()],\n    &program_id,\n);\n\n// Slash pool\nlet (slash_pool_pda, _) = Pubkey::find_program_address(\n    &[b\"slash_pool\", protocol_pda.as_ref()],\n    &program_id,\n);\n\n// Operator (one per authority)\nlet (operator_pda, _) = Pubkey::find_program_address(\n    &[b\"operator\", protocol_pda.as_ref(), authority.as_ref()],\n    &program_id,\n);\n\n// Signing request (one per request_id)\nlet request_id_bytes = request_id.to_le_bytes();\nlet (request_pda, _) = Pubkey::find_program_address(\n    &[b\"request\", protocol_pda.as_ref(), &request_id_bytes],\n    &program_id,\n);\n\n// Partial signature (one per request × operator)\nlet (partial_pda, _) = Pubkey::find_program_address(\n    &[b\"partial\", request_pda.as_ref(), operator_pda.as_ref()],\n    &program_id,\n);\n```\n\n---\n\n## Signature Scheme Routing\n\nThe `scheme` field on both `SigningRequest` and `PartialSignature` gates which off-chain library handles share generation and combination.\n\n```mermaid\nflowchart LR\n    TV[TargetVm]\n    TV -->|Svm\\nAptos\\nSui| FROST[\"FrostEd25519\\nengine/kobe\\nSchnorr on Ed25519\"]\n    TV -->|Evm\\nBitcoin| GG20E[\"Gg20Secp256k1\\nengine/kobe-ecdsa\\nECDSA on secp256k1\"]\n    TV -->|Tron| GG20T[\"Gg20Secp256k1\\nengine/kobe-ecdsa\\nECDSA on secp256k1\"]\n    TV -->|Cosmos| FROSTC[\"FrostEd25519\\nengine/kobe\\nSchnorr on Ed25519\"]\n```\n\nThe on-chain program stores `scheme: SignatureScheme` as a single discriminant byte. The scheme is set on the `SigningRequest` at creation; a `PartialSignature` carries the request's scheme rather than a caller-supplied one, so there is no untrusted-scheme path and no scheme-mismatch error to raise.\n\n| `SignatureScheme` variant | Byte | Target VMs | Off-chain signer |\n|---|---|---|---|\n| `FrostEd25519` | `0` | `Svm`, Cosmos SDK chains | `engine/kobe` |\n| `Gg20Secp256k1` | `1` | `Evm`, `Tron`, `Bitcoin` | `engine/kobe-ecdsa` |\n\n`TargetVm::Cosmos` is served by the FROST Ed25519 path (`engine/kobe`). The `target_chain_id` field disambiguates the specific destination at the off-chain layer; the on-chain program records it verbatim without interpreting it.\n\n---\n\n## Full Signing Lifecycle — Sequence Diagram\n\n```mermaid\nsequenceDiagram\n    participant ADM as Admin\n    participant OPR as Operator Node\n    participant USR as User\n    participant PRG as Unbridge Program\n    participant VLT as Bond Vault (Token-2022)\n    participant RLY as Relayer\n\n    ADM->>PRG: initialize(threshold_bps, min_bond, unbonding_slots,\\nrequest_fee, max_validity_slots, lst_price_feed)\n    PRG-->>PRG: create Protocol PDA [b\"protocol\"]\n    PRG-->>VLT: create Bond Vault [b\"bond_vault\", protocol]\n    PRG-->>PRG: create Slash Pool [b\"slash_pool\", protocol]\n\n    OPR->>PRG: register_operator(group_pubkey, bond_amount)\n    PRG->>VLT: transfer_checked(bond_amount) via Token-2022 CPI\n    PRG-->>PRG: compute_stake_weight(lst_price_feed, bond_amount)\n    PRG-->>PRG: create Operator PDA [b\"operator\", protocol, authority]\n    PRG-->>PRG: protocol.total_bonded += stake_weight\n    PRG-->>PRG: emit OperatorRegistered { operator, authority, stake_weight }\n\n    USR->>PRG: create_signing_request(scheme, target_vm, target_chain_id,\\nmessage_hash, threshold, validity_slots)\n    PRG->>PRG: system_program::transfer(request_fee lamports)\n    PRG-->>PRG: snapshot required_stake_weight = total_bonded × threshold_bps / 10_000\n    PRG-->>PRG: create SigningRequest PDA [b\"request\", protocol, request_nonce_le]\n    PRG-->>PRG: request_nonce += 1\n\n    loop Each signing operator\n        OPR->>PRG: submit_partial_signature(share: [u8;64])\n        PRG-->>PRG: verify operator not jailed, request not expired\n        PRG-->>PRG: create PartialSignature PDA\\n[b\"partial\", request, operator]\n        PRG-->>PRG: request.stake_weight_collected += operator.stake_weight\n        PRG-->>PRG: request.partials_collected += 1 (a receipt, not a fold)\n        PRG-->>PRG: operator.partials_submitted += 1\n        PRG-->>PRG: emit PartialSignatureSubmitted\n    end\n\n    Note over OPR,PRG: off-chain coordinator runs real FROST/GG20,\\nproduces the canonical 64-byte group signature\n    OPR->>PRG: aggregate_and_emit(aggregate_sig: [u8;64])\n    PRG-->>PRG: check status==Pending, slot<=expiry,\\npartials>=threshold, weight>=required, sig non-zero\n    PRG-->>PRG: record aggregate_sig, status = Aggregated\n    PRG-->>PRG: emit AggregateSignatureEmitted\n\n    RLY->>PRG: read SigningRequest.aggregate_sig\n    RLY->>DestinationChain: broadcast aggregate signature\n```\n\n---\n\n## Economic Security Data Flow\n\nThe staked weight accounting is the core invariant the program maintains. Three state transitions mutate `total_bonded`:\n\n### Registration\n\n```\nprotocol.total_bonded += compute_stake_weight(lst_price_feed, bond_amount)\nprotocol.operator_count += 1\n```\n\n### Unbonding\n\n```rust\n// begin_unbonding sets:\noperator.unbonding_at = clock.slot + protocol.unbonding_slots;\noperator.jailed = true;\nprotocol.total_bonded -= operator.stake_weight;   // saturating_sub\nprotocol.operator_count -= 1;                     // saturating_sub\n```\n\nAfter `begin_unbonding`, the operator is removed from the active signing set immediately. Its bond remains in the vault and slashable for the full `unbonding_slots` window. Only after `clock.slot >= operator.unbonding_at` can `withdraw_bond` execute the Token-2022 CPI back to the operator's token account.\n\n### Slashing\n\n```rust\n// slash_operator transfers `amount` from bond_vault to slash_pool:\noperator.bonded_amount -= amount;                    // saturating_sub\noperator.stake_weight  = compute_stake_weight(lst_price_feed, operator.bonded_amount);\nif operator.bonded_amount < protocol.min_bond {\n    operator.jailed = true;\n}\n\n// If the operator was active before the slash:\nlet weight_delta = weight_before - new_weight;\nprotocol.total_bonded -= weight_delta;\nif newly_jailed {\n    protocol.total_bonded -= new_weight;\n    protocol.operator_count -= 1;\n}\n```\n\nThe slash path checks whether the operator was active (`unbonding_at == 0 && !jailed`) before the slash to avoid double-subtracting weight for already-inactive operators.\n\n---\n\n## Threshold Enforcement Logic\n\nA request finalization check requires both dimensions to pass simultaneously:\n\n| Dimension | Condition |\n|---|---|\n| Partial count | `partials_collected >= threshold` |\n| Economic weight | `stake_weight_collected >= required_stake_weight` |\n\n`required_stake_weight` is computed at request-creation time:\n\n```\nrequired_stake_weight = total_bonded × threshold_bps / BPS_DENOMINATOR\n```\n\nFor example, with `threshold_bps = 6_667` and `total_bonded = 1_000_000`:\n\n```\nrequired_stake_weight = 1_000_000 × 6_667 / 10_000 = 666_700\n```\n\nThe `create_signing_request` instruction also validates its own `threshold` argument (minimum number of distinct partials):\n\n```rust\nrequire!(\n    threshold >= 1 && (threshold as u32) <= protocol.operator_count,\n    UnbridgeError::InvalidThreshold\n);\n```\n\nThis means a requester cannot set a threshold higher than the current active operator count, preventing permanently unsatisfiable requests.\n\n---\n\n## Validity Window Constraints\n\nThe `validity_slots` parameter in `create_signing_request` is bounded by two guards:\n\n```rust\n// Hard ceiling enforced in initialize / update_config:\npub const MAX_VALIDITY_SLOTS_CEILING: u64 = 432_000; // ~48h at 400ms slots\n\n// Per-request guard:\nrequire!(\n    validity_slots >= 1 && validity_slots <= protocol.max_validity_slots,\n    UnbridgeError::InvalidValidityWindow\n);\n```\n\n`protocol.max_validity_slots` is set by the admin and cannot itself exceed `432_000`. The `expiry_slot` stored on the request is:\n\n```\nexpiry_slot = created_slot + validity_slots\n```\n\nAny partial signature submission after `clock.slot > expiry_slot` returns `UnbridgeError::RequestExpired` (`6008`). The request's `status` field transitions to `Expired` at the finalizer's discretion (off-chain relayer observation).\n\n---\n\n## Admin Governance — Two-Step Handover\n\nAdmin authority follows an explicit two-transaction commit/confirm pattern to prevent one-shot fat-finger transfers:\n\n```mermaid\nsequenceDiagram\n    participant CurrentAdmin\n    participant Program as Unbridge Program\n    participant NewAdmin\n\n    CurrentAdmin->>Program: transfer_admin(new_admin: Pubkey)\n    Note over Program: protocol.pending_admin = new_admin\n    Note over Program: require!(new_admin != Pubkey::default(),\\nInvalidAdminTransfer)\n\n    NewAdmin->>Program: accept_admin()\n    Note over Program: require!(pending_admin == new_admin.key(),\\nUnauthorized)\n    Note over Program: protocol.admin = pending_admin\n    Note over Program: protocol.pending_admin = Pubkey::default()\n```\n\nIf the nominated admin never calls `accept_admin`, `pending_admin` remains set but has no authority until confirmed. The current `admin` retains all privileges and can re-nominate.\n\nEmergency controls (`pause` / `unpause`) are admin-only and gate all user and operator state transitions:\n\n```rust\nrequire!(!protocol.paused, UnbridgeError::ProtocolPaused);\n```\n\nThis guard appears in `register_operator`, `begin_unbonding`, and `create_signing_request`. The slash path does not check `paused`, allowing the admin to slash misbehaving operators even while the protocol is halted.\n\n---\n\n## On-chain vs Off-chain Boundary\n\n| Responsibility | On-chain (Unbridge program) | Off-chain (engine/kobe + engine/kobe-ecdsa) |\n|---|---|---|\n| Bond accounting | ✓ Token-2022 CPI | — |\n| Stake weight oracle | reads Pyth `lst_price_feed` | — |\n| Threshold counting | ✓ `stake_weight_collected` / `partials_collected` | — |\n| Share storage | ✓ `PartialSignature.share [u8; 64]` | — |\n| Share validity (crypto) | — | ✓ FROST/GG20 round verification |\n| Group-combine (FROST aggregate / GG20) | — | ✓ produces the canonical `aggregate_sig` |\n| Equivocation detection | off-chain (operators identify and attest) | slash entrypoint enforces the effect; slash_operator_attested verifies the operator quorum on-chain |\n| Aggregate broadcast | — | ✓ relayer reads `aggregate_sig`, submits to dst chain |\n| DKG / resharing | — | ✓ fully off-chain |\n\nThe program treats `share: [u8; 64]` as an opaque participation receipt — it is recorded but never folded into a running signature, since summing FROST/GG20 share bytes is cryptographically meaningless and the SVM has no curve arithmetic for the group-combine. The canonical group signature is computed off-chain by the coordinator (`engine/kobe` for FROST Ed25519, `engine/kobe-ecdsa` for GG20 secp256k1) and posted into `aggregate_and_emit`. The on-chain layer enforces that each partial came from a bonded, non-jailed operator and that the thresholds and the non-zero-signature check pass, but it does not verify the elliptic-curve mathematics.\n\n---\n\n## On-chain Events\n\nThe program emits five events as Anchor event CPI logs, consumable by indexers: `OperatorRegistered`, `OperatorSlashed`, `SigningRequestCreated`, `PartialSignatureSubmitted`, and `AggregateSignatureEmitted`.\n\n### `OperatorRegistered`\n\nEmitted by `register_operator`:\n\n```rust\nemit!(OperatorRegistered {\n    operator: operator.key(),\n    authority: operator.authority,\n    stake_weight,           // u64 — oracle-adjusted weight at join time\n});\n```\n\n### `OperatorSlashed`\n\nEmitted by `slash_operator`:\n\n```rust\nemit!(OperatorSlashed {\n    operator: operator.key(),\n    amount,   // u64 — LST token units moved to slash_pool\n    reason,   // u8 — opaque reason code (equivocation / invalid-share / liveness)\n});\n```\n\n### `SigningRequestCreated`\n\nEmitted by `create_signing_request` when a new intent is opened.\n\n### `PartialSignatureSubmitted`\n\nEmitted by `submit_partial_signature`:\n\n```rust\nemit!(PartialSignatureSubmitted {\n    request,\n    operator,\n    partials_collected,      // u16 — receipts recorded so far\n    stake_weight_collected,  // u64 — economic weight recorded so far\n});\n```\n\n### `AggregateSignatureEmitted`\n\nEmitted by `aggregate_and_emit` when the off-chain coordinator posts the canonical group signature:\n\n```rust\nemit!(AggregateSignatureEmitted {\n    request,\n    request_id,\n    scheme,\n    target_vm,\n    target_chain_id,\n    message_hash,\n    aggregate_sig,           // [u8; 64] — recorded, not computed on-chain\n});\n```\n\nAn off-chain relayer can subscribe to `AggregateSignatureEmitted` directly rather than polling the `SigningRequest` account.\n\n---\n\n## Edge Cases and Failure Modes\n\n### Slash reduces weight below minimum between request creation and finalization\n\n`required_stake_weight` is snapshotted at creation time. If enough operators are slashed after a request is created but before it is finalized, `stake_weight_collected` may never reach `required_stake_weight` even with all remaining active operators contributing, leaving the request permanently stuck in `Pending` until `expiry_slot`. The request expires and a new one must be submitted with updated parameters.\n\n### Operator attempts double-submit on a request\n\nBecause `PartialSignature` PDA seeds include both `request` and `operator`, a second submission by the same operator for the same request fails at Anchor's account-init stage (`AccountAlreadyInitialized`) before any instruction logic runs. `stake_weight` cannot be double-counted this way.\n\n### Bond falls below `min_bond` mid-unbonding due to slash\n\n`withdraw_bond` only checks `operator.unbonding_at != 0` and `clock.slot >= operator.unbonding_at`. A bond reduced below `min_bond` by a slash while unbonding still completes withdrawal; the operator was already jailed and removed from `operator_count` at slash time. The `slash_operator` instruction guards against over-slashing with:\n\n```rust\nrequire!(\n    amount <= ctx.accounts.operator.bonded_amount,\n    UnbridgeError::SlashAmountExceedsBond\n);\n```\n\n### `total_bonded` underflow\n\nAll decrements to `total_bonded` use `saturating_sub`, not checked arithmetic. This means a sequencing bug that would otherwise underflow to a panic instead floors at zero, which is safe but would corrupt the threshold math. The `operator_count` field is also `saturating_sub`-decremented.\n\n### Oracle staleness\n\n`compute_stake_weight` reads `lst_price_feed` at registration time and at slash time. If the Pyth feed is stale, the instruction returns `UnbridgeError::StaleOraclePrice` (`6014`). If an incorrect oracle account is passed, `UnbridgeError::InvalidOracleAccount` (`6015`) is raised. The stored `stake_weight` on the Operator account is the weight at the time of the last oracle read, not a live value — weight does not auto-update as the LST price changes between calls.\n\n### `request_nonce` monotonicity\n\n`request_nonce` is incremented with `checked_add`, returning `UnbridgeError::MathOverflow` (`6021`) at `u64::MAX`. At theoretical saturation of one request per slot, `u64::MAX` slots at 400 ms/slot is ~2.3 × 10¹¹ years."
  },
  {
    "slug": "how-it-works",
    "title": "How It Works",
    "body": "# How It Works\n\nUnbridge turns Solana into a **threshold-signature control plane**. Every cross-chain signing action moves through four phases: operator bonding, intent posting, partial-signature aggregation, and off-chain relay. Each phase is enforced on-chain by the program deployed at `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6`. This page traces those phases end-to-end with the exact account layouts, instruction signatures, invariants, and failure modes that govern each step.\n\nThis is not a paper design. The program is live on Solana devnet (`solana program show 4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6 --url devnet`), and the full create-request → off-chain MPC → `aggregate_and_emit` → independent-verify loop runs end-to-end on a local validator via `engine/coordinator/m7-demo.sh`.\n\n---\n\n## The Core Insight: Solana as Control Plane\n\nMost cross-chain signature schemes treat their coordination layer as an afterthought -- a side-car service that operators watch out-of-band. Unbridge inverts this: the *coordination layer is the Solana program itself*.\n\nSolana's ~400ms slot time matters here in a concrete way. A multi-round MPC protocol like GG20 requires 3--7 network round trips between signing parties. At 400ms finality, those rounds complete in seconds rather than minutes. Chains with 12-second block times (Ethereum) or 10-minute block times (Bitcoin) cannot serve as the coordination plane for their own outbound signing -- they are too slow to be their own synchronization primitive. Unbridge uses Solana slots as the liveness clock for request deadlines, partial-submission ordering, and unbonding windows.\n\nThe on-chain layer is responsible for:\n\n- **Economic security accounting** -- operator bonds, stake-weight computation via Pyth oracle, slash pool\n- **Threshold enforcement** -- whether collected weight ≥ required weight, whether partial count ≥ threshold\n- **Liveness deadlines** -- slot-based expiry windows, `MAX_VALIDITY_SLOTS_CEILING`\n- **Double-submission prevention** -- PDA uniqueness on `[PARTIAL_SEED, request, operator]`\n- **Slashing execution** -- collateral movement into the slash pool, jailing, weight recalculation\n\nThe on-chain layer is **not** responsible for:\n\n- Cryptographic share verification (delegated to off-chain `engine/kobe` / `engine/kobe-ecdsa` signing libraries)\n- Final group key combination (off-chain, produce the `aggregate_sig` written back on finalization)\n- Broadcasting the aggregate signature to the destination chain (off-chain relayer)\n\n---\n\n## Phase 0 — Protocol Initialization\n\nBefore any operator can bond or user can request a signature, a singleton `Protocol` account must be bootstrapped via `initialize`. This is a one-time permissioned call.\n\n```rust\npub fn initialize(\n    ctx: Context<Initialize>,\n    threshold_bps: u16,       // fraction of total_bonded required; 1–10_000\n    min_bond: u64,            // minimum LST atoms an operator must post\n    unbonding_slots: u64,     // slots an operator waits before withdrawing bond\n    request_fee: u64,         // lamports charged per signing request\n    max_validity_slots: u64,  // upper bound on request expiry; ≤ 432_000\n    lst_price_feed: Pubkey,   // Pyth price account for the bonded LST\n) -> Result<()>\n```\n\nThe `Protocol` account is a PDA derived from `[b\"protocol\"]`. It holds the entire global accounting state in 248 bytes (+8 discriminator):\n\n| Field | Type | Description |\n|---|---|---|\n| `admin` | `Pubkey` | Current admin authority |\n| `pending_admin` | `Pubkey` | Nominated successor (two-step handover); `Pubkey::default()` until set |\n| `bond_mint` | `Pubkey` | Token-2022 LST mint accepted as collateral |\n| `bond_vault` | `Pubkey` | PDA-owned vault holding active bonds |\n| `slash_pool` | `Pubkey` | PDA-owned pool collecting slashed collateral |\n| `lst_price_feed` | `Pubkey` | Pyth price account for LST→SOL valuation |\n| `threshold_bps` | `u16` | bps fraction of `total_bonded` that must sign |\n| `min_bond` | `u64` | Minimum bond in raw LST atoms |\n| `unbonding_slots` | `u64` | Unbonding delay in slots |\n| `request_fee` | `u64` | Lamports per signing request |\n| `max_validity_slots` | `u64` | Hard ceiling on request validity window |\n| `operator_count` | `u32` | Active operators in the signing set |\n| `total_bonded` | `u64` | Sum of active operators' stake weight |\n| `request_nonce` | `u64` | Monotonic counter seeding request PDAs |\n| `paused` | `bool` | Emergency pause flag |\n| `bump` | `u8` | PDA canonical bump |\n\n`initialize` enforces three invariants immediately:\n\n```\nthreshold_bps ∈ [1, 10_000]          → UnbridgeError::InvalidThreshold\nmin_bond > 0                          → UnbridgeError::InsufficientBond\nmax_validity_slots ∈ [1, 432_000]    → UnbridgeError::InvalidValidityWindow\n```\n\nThe constant `MAX_VALIDITY_SLOTS_CEILING = 432_000` corresponds to ~48 hours at 400ms per slot. Stale requests that linger longer than this would corrupt the economic-security snapshot (see Phase 2).\n\n### Admin Two-Step Handover\n\nAdmin is transferred in two steps to prevent accidental key loss:\n\n```rust\n// Step 1 — current admin nominates a successor\npub fn transfer_admin(ctx: Context<AdminConfig>, new_admin: Pubkey) -> Result<()>\n// Writes new_admin into protocol.pending_admin (cannot be Pubkey::default())\n\n// Step 2 — successor claims the role by signing\npub fn accept_admin(ctx: Context<AcceptAdmin>) -> Result<()>\n// Asserts ctx.accounts.new_admin.key() == protocol.pending_admin\n// Promotes pending_admin → admin, clears pending_admin to Pubkey::default()\n```\n\n---\n\n## Phase 1 — Operator Registration & Bonding\n\nAn operator joins the signing set by calling `register_operator`, depositing LST collateral into the protocol-owned vault.\n\n```rust\npub fn register_operator(\n    ctx: Context<RegisterOperator>,\n    group_pubkey: [u8; 33],   // compressed FROST/ECDSA group share identifier\n    bond_amount: u64,         // raw LST atoms; must be ≥ protocol.min_bond\n) -> Result<()>\n```\n\nThe instruction does four things atomically:\n\n1. **Token transfer** -- pulls `bond_amount` from `operator_token_account` into the `bond_vault` via a CPI to the Token-2022 program (`transfer_checked` with the mint's decimal precision).\n2. **Stake-weight computation** -- calls `compute_stake_weight(&lst_price_feed, bond_amount)`, which reads the Pyth oracle to derive a SOL-denominated economic weight from the raw LST amount.\n3. **Operator account creation** -- writes the `Operator` PDA at seeds `[b\"operator\", protocol, authority]`.\n4. **Protocol accounting update** -- increments `protocol.total_bonded` by `stake_weight` and `protocol.operator_count` by 1.\n\nThe `Operator` account (143 bytes + 8 discriminator) tracks:\n\n| Field | Type | Description |\n|---|---|---|\n| `protocol` | `Pubkey` | Owning protocol |\n| `authority` | `Pubkey` | Operator signer key |\n| `group_pubkey` | `[u8; 33]` | Compressed group pub key / FROST share identifier |\n| `bonded_amount` | `u64` | Raw LST atoms held in vault |\n| `stake_weight` | `u64` | SOL-denominated economic weight |\n| `partials_submitted` | `u64` | Lifetime partial signature count |\n| `slash_count` | `u32` | Slash event count |\n| `jailed` | `bool` | Cannot sign new requests when `true` |\n| `unbonding_at` | `u64` | Slot unbonding completes; `0` while active |\n| `joined_slot` | `u64` | Slot of registration |\n| `bump` | `u8` | PDA canonical bump |\n\nOn success the program emits:\n\n```rust\nemit!(OperatorRegistered {\n    operator: operator.key(),\n    authority: operator.authority,\n    stake_weight,\n});\n```\n\n### Threshold Impact\n\nEvery new operator immediately shifts the global `total_bonded`. Because `required_stake_weight` is **snapshotted at request creation time** (not evaluated live), operators that join after a request is posted do not affect that request's threshold. This prevents griefing via operator churn.\n\n---\n\n## Phase 2 — Posting a Signing Intent\n\nA user (or protocol integration) calls `create_signing_request` to publish the intent on-chain.\n\n```rust\npub fn create_signing_request(\n    ctx: Context<CreateSigningRequest>,\n    scheme: SignatureScheme,      // FrostEd25519 | Gg20Secp256k1\n    target_vm: TargetVm,         // Svm | Evm | Tron | Cosmos | Bitcoin\n    target_chain_id: u64,        // EVM chain id, Cosmos chain index, etc.\n    message_hash: [u8; 32],      // 32-byte hash of the tx/message to sign\n    threshold: u16,              // minimum distinct partials required; 1..=operator_count\n    validity_slots: u64,         // request lifetime; 1..=max_validity_slots\n) -> Result<()>\n```\n\n### Scheme × VM Routing\n\nThe `scheme` and `target_vm` fields determine which off-chain signing library handles the cryptographic work:\n\n| `SignatureScheme` | `TargetVm` | Off-chain library | Curve |\n|---|---|---|---|\n| `FrostEd25519` | `Svm` | `engine/kobe` | Ed25519 (Schnorr) |\n| `FrostEd25519` | `Cosmos` | `engine/kobe` | Ed25519 (Schnorr) |\n| `Gg20Secp256k1` | `Evm` | `engine/kobe-ecdsa` | secp256k1 (ECDSA) |\n| `Gg20Secp256k1` | `Tron` | `engine/kobe-ecdsa` | secp256k1 (ECDSA) |\n| `Gg20Secp256k1` | `Bitcoin` | `engine/kobe-ecdsa` | secp256k1 (ECDSA) |\n\nThe on-chain program does not verify the (scheme, target_vm) pairing. The correctness of that choice is the requester's responsibility; a mismatched pair produces an unusable aggregate signature on the destination chain.\n\n### Economic Security Snapshot\n\nThe required stake weight is locked in at creation time:\n\n```rust\nlet required_stake_weight = protocol\n    .total_bonded\n    .checked_mul(protocol.threshold_bps as u64)\n    .ok_or(UnbridgeError::MathOverflow)?\n    / BPS_DENOMINATOR;\n```\n\nSo if `total_bonded = 1_000_000` and `threshold_bps = 6_000`, the request requires `600_000` stake weight worth of partial signatures before it can be finalized. This value is written into `SigningRequest.required_stake_weight` and does not change for the lifetime of the request.\n\n### Request Fee\n\nIf `protocol.request_fee > 0`, the instruction CPIs into the system program to charge the requester:\n\n```rust\nsystem_program::transfer(\n    CpiContext::new(...),\n    protocol.request_fee, // lamports\n)?;\n```\n\nThe fee lands in the protocol PDA account (rent-exemption balance is additive). There is no fee refund on expiry or cancellation.\n\n### Request Account\n\nThe `SigningRequest` PDA is created at seeds `[b\"request\", protocol, request_id.to_le_bytes()]` where `request_id = protocol.request_nonce` (incremented after creation). The account is 224 bytes (+8 discriminator):\n\n| Field | Type | Initial Value |\n|---|---|---|\n| `request_id` | `u64` | `protocol.request_nonce` at creation |\n| `scheme` | `SignatureScheme` | caller-supplied |\n| `target_vm` | `TargetVm` | caller-supplied |\n| `target_chain_id` | `u64` | caller-supplied |\n| `message_hash` | `[u8; 32]` | caller-supplied (must be non-zero) |\n| `threshold` | `u16` | caller-supplied |\n| `partials_collected` | `u16` | `0` |\n| `stake_weight_collected` | `u64` | `0` |\n| `required_stake_weight` | `u64` | snapshotted from `total_bonded * threshold_bps / 10_000` |\n| `created_slot` | `u64` | `Clock::get().slot` |\n| `expiry_slot` | `u64` | `created_slot + validity_slots` |\n| `status` | `RequestStatus` | `Pending` |\n| `aggregate_sig` | `[u8; 64]` | `[0u8; 64]` |\n\n### Pre-flight Checks\n\n`create_signing_request` fails with the following errors before writing any state:\n\n| Check | Error |\n|---|---|\n| `protocol.paused` | `ProtocolPaused` |\n| `protocol.operator_count == 0` | `NoActiveOperators` |\n| `message_hash` is all-zero | `EmptyMessageHash` |\n| `threshold < 1` or `threshold > operator_count` | `InvalidThreshold` |\n| `validity_slots < 1` or `validity_slots > max_validity_slots` | `InvalidValidityWindow` |\n| `total_bonded * threshold_bps` overflows `u64` | `MathOverflow` |\n\n---\n\n## Phase 3 — Partial Signature Submission & Aggregation\n\nAfter a request is posted, eligible operators (not jailed, not unbonding) watch for new `SigningRequest` accounts and submit their partial signatures via `submit_partial`. Each submission creates a `PartialSignature` PDA.\n\n### PartialSignature Account\n\nSeeds: `[b\"partial\", request, operator]`. The PDA uniqueness at these seeds means an operator cannot submit twice for the same request -- the account init will fail on a duplicate attempt.\n\n| Field | Type | Description |\n|---|---|---|\n| `request` | `Pubkey` | Request this share contributes to |\n| `operator` | `Pubkey` | Operator that submitted the share |\n| `scheme` | `SignatureScheme` | Copied from `request.scheme` (set by the request, not the caller) |\n| `share` | `[u8; 64]` | 64-byte partial-signature share material (a receipt) |\n| `submitted_slot` | `u64` | Slot of submission |\n| `stake_weight` | `u64` | Operator's stake weight credited |\n| `bump` | `u8` | PDA canonical bump |\n\nThe on-chain layer credits `stake_weight_collected += operator.stake_weight` and increments `partials_collected`. This is a participation receipt: the program does not fold or combine the `share` bytes — summing FROST/GG20 shares is cryptographically meaningless and the SVM has no curve arithmetic for the group-combine. It emits `PartialSignatureSubmitted { request, operator, partials_collected, stake_weight_collected }`.\n\n### Finalization\n\nOnce both conditions hold:\n\n```\nrequest.stake_weight_collected >= request.required_stake_weight\nAND\nrequest.partials_collected >= request.threshold\n```\n\nthe `aggregate_and_emit(aggregate_sig: [u8; 64])` instruction can be called. The canonical group signature is computed off-chain by the coordinator using real FROST round1/round2 plus `frost::aggregate` (`engine/kobe`, Ed25519) or GG20 (`engine/kobe-ecdsa`, secp256k1), then passed into `aggregate_and_emit`. The program checks `status == Pending`, `slot <= expiry_slot`, both thresholds, and that `aggregate_sig` is non-zero (not all zero bytes), then records it into `request.aggregate_sig`, sets `request.status = RequestStatus::Aggregated`, and emits `AggregateSignatureEmitted`.\n\nA `RequestAlreadyFinalized` error is returned if the request is already `Aggregated`.\n\n---\n\n## Full Sequence Diagram\n\n```mermaid\nsequenceDiagram\n    participant U as User\n    participant S as Solana Program<br/>2KNozrx...\n    participant O1 as Operator A\n    participant O2 as Operator B\n    participant O3 as Operator C\n    participant K as engine/kobe + engine/kobe-ecdsa<br/>(off-chain)\n    participant R as Relayer\n    participant D as Destination Chain\n\n    Note over S: Protocol initialized, operators bonded\n\n    U->>S: create_signing_request(scheme=Gg20Secp256k1,<br/>target_vm=Evm, chain_id=1,<br/>message_hash=[32 bytes], threshold=2,<br/>validity_slots=300)\n    S->>S: Charge request_fee lamports\n    S->>S: Snapshot required_stake_weight =<br/>total_bonded × threshold_bps / 10_000\n    S->>S: Write SigningRequest PDA<br/>seed=[b\"request\", protocol, nonce.to_le_bytes()]\n    S-->>U: Request account created (status=Pending)\n\n    par Operators observe & sign\n        O1->>K: Compute partial share for message_hash\n        K-->>O1: share_bytes [64]\n        O1->>S: submit_partial(request, share_bytes)\n        S->>S: Create PartialSignature PDA<br/>seed=[b\"partial\", request, operator_A]\n        S->>S: stake_weight_collected += operator_A.stake_weight<br/>partials_collected += 1\n    and\n        O2->>K: Compute partial share for message_hash\n        K-->>O2: share_bytes [64]\n        O2->>S: submit_partial(request, share_bytes)\n        S->>S: Create PartialSignature PDA<br/>seed=[b\"partial\", request, operator_B]\n        S->>S: stake_weight_collected += operator_B.stake_weight<br/>partials_collected += 1\n    end\n\n    Note over S: partials_collected >= threshold<br/>stake_weight_collected >= required_stake_weight\n\n    K->>K: Run real FROST/GG20, produce canonical aggregate_sig [64]\n    R->>S: aggregate_and_emit(aggregate_sig)\n    S->>S: Assert status, expiry, thresholds, non-zero sig\n    S->>S: Record aggregate_sig into request<br/>Set status = Aggregated\n    S-->>R: Emit AggregateSignatureEmitted event\n\n    R->>D: Broadcast aggregate_sig to destination chain\n    D->>D: Verify group signature against<br/>on-chain group public key\n```\n\n---\n\n## Phase 4 — Off-chain Relay\n\nThe program does not directly call the destination chain. Instead `aggregate_and_emit` records the finalized `aggregate_sig` field and emits an `AggregateSignatureEmitted` event. An off-chain relayer monitors the program's event stream, reads the `SigningRequest` account at `[b\"request\", protocol, request_id_le]`, and submits the `aggregate_sig` bytes to the appropriate destination:\n\n- **EVM chains:** `eth_sendRawTransaction` with the aggregated secp256k1 ECDSA signature recovered via `engine/kobe-ecdsa`\n- **SVM / Aptos:** transaction with the aggregated Ed25519 Schnorr signature from `engine/kobe`\n- **Tron:** `engine/kobe-ecdsa` produces a Tron-encoded secp256k1 signature\n- **Cosmos:** `engine/kobe` wraps the Ed25519 Schnorr share in a CosmosSDK-compatible signature envelope\n- **Bitcoin:** `engine/kobe-ecdsa` secp256k1 ECDSA output, serialized per the relevant Bitcoin script type\n\nThe relayer is trust-minimized for safety: the canonical aggregate signature is recorded publicly on-chain by `aggregate_and_emit`, so any party watching the program can read it and relay it. The relayer holds no signing key of its own and cannot alter the signature.\n\n---\n\n## Operator Lifecycle & Economic Security\n\n### Unbonding\n\nAn operator initiates exit by calling `begin_unbonding`:\n\n```rust\npub fn begin_unbonding(ctx: Context<OperatorLifecycle>) -> Result<()>\n```\n\nThe instruction:\n\n1. Asserts `operator.unbonding_at == 0` (not already unbonding → `AlreadyUnbonding`)\n2. Sets `operator.unbonding_at = clock.slot + protocol.unbonding_slots`\n3. Sets `operator.jailed = true` -- immediately removes the operator from the active signing set\n4. Decrements `protocol.total_bonded` by `operator.stake_weight`\n5. Decrements `protocol.operator_count` by 1\n\nThe operator cannot sign new requests from this point. Their bond remains in the vault and is slashable until `withdraw_bond` completes.\n\n### Bond Withdrawal\n\nAfter `clock.slot >= operator.unbonding_at`, the operator calls `withdraw_bond`:\n\n```rust\npub fn withdraw_bond(ctx: Context<WithdrawBond>) -> Result<()>\n```\n\nThe program:\n1. Asserts `operator.unbonding_at != 0` → `NotUnbonding`\n2. Asserts `clock.slot >= operator.unbonding_at` → `UnbondingNotComplete`\n3. CPIs into Token-2022 to return `operator.bonded_amount` from the vault to `operator_token_account`, signing with the protocol PDA seeds `[PROTOCOL_SEED, &[protocol.bump]]`\n4. Closes the operator account (rent returned to authority)\n\n### Slashing\n\nThe admin calls `slash_operator` with a `reason: u8` code and `amount: u64` of LST atoms to slash:\n\n```rust\npub fn slash_operator(\n    ctx: Context<SlashOperator>,\n    amount: u64,  // must be ≤ operator.bonded_amount → SlashAmountExceedsBond\n    reason: u8,\n) -> Result<()>\n```\n\nThe flow:\n\n1. Transfers `amount` from `bond_vault` to `slash_pool` via Token-2022 CPI (signed by protocol PDA)\n2. Decrements `operator.bonded_amount -= amount`\n3. Increments `operator.slash_count`\n4. Recomputes `operator.stake_weight` from the residual bond via the oracle\n5. If `operator.bonded_amount < protocol.min_bond` → sets `operator.jailed = true`\n6. If the operator was active before the slash (not already jailed/unbonding), recalculates `protocol.total_bonded` using the weight delta; if the operator is now jailed, also decrements `protocol.operator_count`\n\n```mermaid\nstateDiagram-v2\n    [*] --> Active: register_operator\n    Active --> Unbonding: begin_unbonding\\n(jailed=true, unbonding_at set)\n    Active --> Jailed: slash_operator\\n(bonded < min_bond)\n    Unbonding --> Withdrawn: withdraw_bond\\n(slot >= unbonding_at)\n    Jailed --> Withdrawn: withdraw_bond\\n(after unbonding period if begun)\n    Withdrawn --> [*]\n```\n\n---\n\n## Threshold Math in Detail\n\nThe stake-weight threshold is evaluated in two places with subtly different semantics.\n\n### At Request Creation\n\n```rust\nlet required_stake_weight = protocol\n    .total_bonded\n    .checked_mul(protocol.threshold_bps as u64)\n    .ok_or(UnbridgeError::MathOverflow)?\n    / BPS_DENOMINATOR;           // BPS_DENOMINATOR = 10_000\n```\n\nThis is a snapshot. If `total_bonded` at creation time is `800_000` weight units and `threshold_bps = 6667`, then `required_stake_weight = 533_360`. Operators that join or leave after this snapshot do not affect this value.\n\n### At Finalization\n\nThe program checks both independently:\n\n```\nstake_weight_collected >= required_stake_weight   (economic security)\npartials_collected     >= threshold               (distinct-operator count)\n```\n\nBoth must be satisfied. A single high-weight operator cannot satisfy the count threshold alone; a large number of dust-bond operators cannot satisfy the weight threshold alone.\n\n### Stake Weight Computation\n\n`compute_stake_weight(&lst_price_feed, bond_amount)` reads the Pyth oracle account stored in `protocol.lst_price_feed`. If the price data is stale or the account does not match the configured feed, the call fails with `StaleOraclePrice` or `InvalidOracleAccount` respectively. The weight is denominated in SOL terms, not raw LST atoms, so LST price volatility directly affects operator weight and therefore threshold calculations.\n\n---\n\n## PDA Account Map\n\n```\n[b\"protocol\"]\n    └─ Protocol (singleton, 248+8 bytes)\n          ├─ bond_vault (Token-2022 account, PDA-owned)\n          │    seed: [b\"bond_vault\", protocol]\n          └─ slash_pool (Token-2022 account, PDA-owned)\n               seed: [b\"slash_pool\", protocol]\n\n[b\"operator\", protocol, authority]\n    └─ Operator (per signing-set member, 143+8 bytes)\n\n[b\"request\", protocol, request_id_le_bytes]\n    └─ SigningRequest (per intent, 224+8 bytes)\n          └─ [b\"partial\", request, operator]\n               └─ PartialSignature (per (request, operator) pair, 146+8 bytes)\n```\n\nPDA uniqueness at `[b\"partial\", request, operator]` is the sole on-chain double-submission guard. An operator that attempts to submit a second partial for the same request will hit an `AccountAlreadyInitialized` error from Anchor's `init` constraint, before any program logic runs.\n\n---\n\n## Signature Scheme Branching\n\nThe `SignatureScheme` enum encodes the cryptographic family needed at the destination:\n\n```rust\npub enum SignatureScheme {\n    FrostEd25519,   // FROST Schnorr, Ed25519 curve — SVM / Aptos / Sui\n    Gg20Secp256k1,  // GG20 threshold ECDSA, secp256k1 curve — EVM / BTC / Tron\n}\n```\n\n| Property | `FrostEd25519` | `Gg20Secp256k1` |\n|---|---|---|\n| Curve | Ed25519 | secp256k1 |\n| Protocol | FROST (RFC 9591) | GG20 |\n| Share size (on-chain) | 64 bytes in `share` | 64 bytes in `share` |\n| Aggregate sig | 64-byte Ed25519 Schnorr sig | 64-byte compact ECDSA (r ∥ s) |\n| Off-chain signer | `engine/kobe` (Rust) | `engine/kobe-ecdsa` (Go) |\n| Round complexity | 2-round FROST | 3--7 round GG20 |\n\nThe on-chain program does not branch on `scheme` internally -- the `share` field is 64 bytes regardless. The scheme is recorded so that off-chain components route to the correct signer. The scheme on a `PartialSignature` is copied from the parent request, not supplied by the caller, so there is no untrusted-scheme path to reject and no scheme-mismatch error.\n\n---\n\n## Edge Cases and Failure Modes\n\n### Request Expires Before Threshold\n\nIf `clock.slot > request.expiry_slot` when `aggregate_and_emit` is called, the program returns `RequestExpired`. The request status is written as `Expired`. The requester's fee is not refunded. Partial-signature submitters have already paid their transaction fees.\n\nThe maximum expiry window is enforced at creation by `MAX_VALIDITY_SLOTS_CEILING = 432_000` slots (~48 hours). A request cannot be created with `validity_slots > 432_000`.\n\n### Operator Slashed Mid-Request\n\nIf an operator is slashed between submitting a partial and request finalization, their already-credited `stake_weight` in `PartialSignature.stake_weight` and `request.stake_weight_collected` are **not retroactively adjusted**. The stake weight credited to a partial is snapshotted at submission time. This is intentional: retroactive weight removal would allow griefing attacks where a slash precisely targets operators whose partials are already counted, retroactively invalidating an otherwise-valid request.\n\nThe economic consequence is that the aggregated signature may represent slightly less economic weight than the threshold required at finalization time if the operator was slashed between submission and finalization. The snapshot at creation (`required_stake_weight`) and the snapshot per partial (`PartialSignature.stake_weight`) both use point-in-time oracle reads.\n\n### Operator Jailed Before Threshold\n\nAn operator that is jailed (via `begin_unbonding` or `slash_operator`) can no longer submit new partials. Any partials already submitted and counted are valid. If the count of remaining active operators drops below `threshold`, a pending request becomes permanently unfulfillable and will expire.\n\n### Oracle Staleness\n\n`compute_stake_weight` reads the Pyth account in `protocol.lst_price_feed`. If the price is stale at registration or slash time, the call fails with `StaleOraclePrice`. If the wrong oracle account is passed, it fails with `InvalidOracleAccount`. The oracle account is enforced to match `protocol.lst_price_feed`.\n\n### Protocol Paused\n\nWhile `protocol.paused = true`, the following instructions are blocked and return `ProtocolPaused`:\n\n- `register_operator`\n- `begin_unbonding`\n- `create_signing_request`\n\nAdmin lifecycle instructions (`update_config`, `pause`, `unpause`, `transfer_admin`, `accept_admin`) and `slash_operator` remain callable while paused. This allows the admin to respond to incidents without losing the ability to slash misbehaving operators.\n\n### Arithmetic Overflow\n\nAll arithmetic on `total_bonded`, `operator_count`, and `required_stake_weight` uses Rust `checked_*` arithmetic. Any overflow returns `MathOverflow` and reverts the entire instruction. Underflows on `total_bonded` and `operator_count` during removal use `saturating_sub` rather than `checked_sub` to ensure the protocol never hard-faults on an accounting inconsistency.\n\n---\n\n## Complete Error Reference\n\n| Error | Code | Trigger |\n|---|---|---|\n| `ProtocolPaused` | 6000 | `paused = true` on a blocked instruction |\n| `Unauthorized` | 6001 | `accept_admin` called by non-pending-admin |\n| `InvalidThreshold` | 6002 | `threshold_bps` outside `[1, 10_000]`; request threshold outside `[1, operator_count]` |\n| `InsufficientBond` | 6003 | `bond_amount < min_bond`; `min_bond = 0` on init |\n| `OperatorJailed` | 6004 | Jailed operator attempts to submit a partial |\n| `AlreadyUnbonding` | 6005 | `begin_unbonding` on an already-unbonding operator |\n| `NotUnbonding` | 6006 | `withdraw_bond` before `begin_unbonding` |\n| `UnbondingNotComplete` | 6007 | `withdraw_bond` before `unbonding_at` slot |\n| `RequestExpired` | 6008 | `clock.slot > expiry_slot` on submit or finalize |\n| `RequestNotPending` | 6009 | Submit/finalize on non-`Pending` request |\n| `ThresholdNotMet` | 6010 | Finalize called before weight/count thresholds satisfied |\n| `RequestAlreadyFinalized` | 6011 | Finalize on `Aggregated` request |\n| `MalformedPartialSignature` | 6012 | Share bytes fail format validation |\n| `EmptyMessageHash` | 6013 | All-zero `message_hash` |\n| `StaleOraclePrice` | 6014 | Pyth price data is stale |\n| `InvalidOracleAccount` | 6015 | Oracle account != `protocol.lst_price_feed` |\n| `InvalidVault` | 6016 | Vault or pool account doesn't match protocol config |\n| `InvalidValidityWindow` | 6017 | `validity_slots` outside `[1, 432_000]`; `max_validity_slots` same on init |\n| `NoActiveOperators` | 6018 | `operator_count == 0` on request creation |\n| `SlashAmountExceedsBond` | 6019 | `amount > operator.bonded_amount` |\n| `InvalidAdminTransfer` | 6020 | `new_admin == Pubkey::default()` |\n| `MathOverflow` | 6021 | `checked_*` arithmetic failed |\n\n---\n\n## Working End-to-End Example\n\nThe following TypeScript sketch (using `@coral-xyz/anchor`) shows the complete happy path from operator registration to a finalized EVM signing request.\n\n```typescript\n// Derive PDAs\nconst [protocolPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"protocol\")],\n  Unbridge_PROGRAM_ID\n);\n\nconst [operatorPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"operator\"), protocolPda.toBuffer(), authority.publicKey.toBuffer()],\n  Unbridge_PROGRAM_ID\n);\n\n// --- Operator registration ---\nawait program.methods\n  .registerOperator(\n    Array.from(groupPubkey33Bytes),   // [u8; 33] compressed public key\n    new BN(5_000_000_000)             // 5,000 LST atoms (must be >= min_bond)\n  )\n  .accounts({\n    protocol: protocolPda,\n    operator: operatorPda,\n    authority: authority.publicKey,\n    operatorTokenAccount: operatorAta,\n    bondVault: bondVaultPda,\n    bondMint: bondMintPubkey,\n    lstPriceFeed: pythLstFeedPubkey,\n    tokenProgram: TOKEN_2022_PROGRAM_ID,\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([authority])\n  .rpc();\n\n// --- Post a signing intent for Ethereum mainnet ---\nconst messageHash = Buffer.from(\n  keccak256(ethers.utils.arrayify(rawTxBytes)),\n  \"hex\"\n);                        // exactly 32 bytes, non-zero\n\nconst [requestPda] = PublicKey.findProgramAddressSync(\n  [\n    Buffer.from(\"request\"),\n    protocolPda.toBuffer(),\n    currentNonce.toArrayLike(Buffer, \"le\", 8),\n  ],\n  Unbridge_PROGRAM_ID\n);\n\nawait program.methods\n  .createSigningRequest(\n    { gg20Secp256k1: {} },    // SignatureScheme enum variant\n    { evm: {} },              // TargetVm enum variant\n    new BN(1),                // Ethereum mainnet chain_id = 1\n    Array.from(messageHash),  // [u8; 32]\n    2,                        // threshold: 2 distinct operators\n    new BN(150)               // validity: 150 slots ≈ 60 seconds\n  )\n  .accounts({\n    protocol: protocolPda,\n    request: requestPda,\n    requester: user.publicKey,\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([user])\n  .rpc();\n\n// --- Operator A submits partial signature ---\nconst [partialPdaA] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"partial\"), requestPda.toBuffer(), operatorPdaA.toBuffer()],\n  Unbridge_PROGRAM_ID\n);\n\nawait program.methods\n  .submitPartial(\n    Array.from(shareA64Bytes)    // [u8; 64] partial-signature receipt from the GG20 round\n  )\n  .accounts({\n    protocol: protocolPda,\n    request: requestPda,\n    operator: operatorPdaA,\n    partial: partialPdaA,\n    authority: operatorAuthA.publicKey,\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([operatorAuthA])\n  .rpc();\n\n// --- After the threshold receipts land, the coordinator finalizes ---\n// The canonical group signature is computed OFF-CHAIN by the coordinator\n// running real GG20 (engine/kobe-ecdsa); the on-chain partials are receipts,\n// not share material that can be summed.\nconst aggregateSig = coordinator.canonicalAggregate(requestId); // 64 bytes, non-zero\n\nawait program.methods\n  .aggregateAndEmit(Array.from(aggregateSig))\n  .accounts({\n    protocol: protocolPda,\n    request: requestPda,\n  })\n  .rpc();\n\n// request.status is now Aggregated\n// request.aggregate_sig holds the 64-byte (r ∥ s) ECDSA signature\n// Relayer reads it and calls eth_sendRawTransaction on Ethereum mainnet\n```\n\n---\n\n## Constants Quick Reference\n\n| Constant | Value | Meaning |\n|---|---|---|\n| `BPS_DENOMINATOR` | `10_000` | Denominator for all bps calculations |\n| `MAX_VALIDITY_SLOTS_CEILING` | `432_000` | ~48 hours at 400ms/slot; hard cap on request lifetime |\n| `PROTOCOL_SEED` | `b\"protocol\"` | Singleton protocol PDA seed |\n| `BOND_VAULT_SEED` | `b\"bond_vault\"` | Active bond vault PDA seed |\n| `SLASH_POOL_SEED` | `b\"slash_pool\"` | Slashed collateral pool PDA seed |\n| `OPERATOR_SEED` | `b\"operator\"` | Per-operator PDA seed |\n| `REQUEST_SEED` | `b\"request\"` | Per-signing-request PDA seed |\n| `PARTIAL_SEED` | `b\"partial\"` | Per-(request, operator) partial PDA seed |\n| Program ID | `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6` | Deployed program address |"
  },
  {
    "slug": "security",
    "title": "Security & Trust",
    "body": "# Security & Trust Model\n\nUnbridge is a threshold-signature coordination layer on Solana. Its security properties split cleanly between what the Solana program enforces on-chain and what is delegated to the two off-chain signers: `engine/kobe` (Rust, FROST Ed25519) and `engine/kobe-ecdsa` (Go, GG20 secp256k1). Understanding that boundary is the most important thing a security reviewer or integration builder needs to know.\n\n---\n\n## Trust Hierarchy\n\n```mermaid\ngraph TD\n    A[Admin Key] -->|pause / slash / update_config| P[Protocol PDA]\n    A -->|two-step transfer| NA[Nominee Admin]\n    NA -->|accept_admin| P\n\n    P -->|owns| BV[Bond Vault Token-2022]\n    P -->|owns| SP[Slash Pool Token-2022]\n\n    OP[Operator] -->|bonds LST| BV\n    OP -->|submits partial sig| REQ[SigningRequest PDA]\n\n    USER[User / Requester] -->|create_signing_request| REQ\n    USER -->|pays request_fee lamports| P\n\n    REL[Off-chain Relayer] -->|reads aggregate_sig| REQ\n    REL -->|broadcasts on destination chain| DEST[EVM / SVM / Tron / Cosmos / Bitcoin]\n\n    style A fill:#c0392b,color:#fff\n    style P fill:#2c3e50,color:#fff\n    style REL fill:#7f8c8d,color:#fff\n```\n\n| Principal | Trust Level | What they can do unilaterally |\n|---|---|---|\n| Admin | Privileged | Pause, update_config, slash_operator, nominate successor |\n| Operator | Bonded, economically constrained | Submit partial sigs; begin_unbonding; withdraw bond after window |\n| Requester (User) | Permissionless | Post signing intents; pays lamport fee |\n| Off-chain Relayer | Trusted for liveness, not safety | Reads `aggregate_sig`; broadcasts to destination chain |\n| Pyth Oracle feed | External, staleness-checked | Prices LST bond in SOL terms for stake-weight accounting |\n\n---\n\n## On-Chain Security Guarantees\n\nThe Solana program enforces the following properties unconditionally; no amount of operator collusion or relayer misbehavior can violate them.\n\n### PDA Account Isolation\n\nEvery persistent account is a PDA derived from deterministic seeds. Collisions are computationally infeasible because Anchor rejects any account whose discriminator or seeds do not match.\n\n| Account | Seeds | Uniqueness property |\n|---|---|---|\n| `Protocol` | `[b\"protocol\"]` | Singleton; one config per deployment |\n| Bond vault | `[b\"bond_vault\", protocol]` | Singleton Token-2022 vault |\n| Slash pool | `[b\"slash_pool\", protocol]` | Singleton Token-2022 pool |\n| `Operator` | `[b\"operator\", protocol, authority]` | One operator record per authority key |\n| `SigningRequest` | `[b\"request\", protocol, request_id_le]` | One record per monotonic nonce |\n| `PartialSignature` | `[b\"partial\", request, operator]` | One share per (request, operator) pair |\n\nThe `PartialSignature` seed structure is the mechanism that prevents double-submission. A second call from the same operator for the same request will attempt to `init` an account at the same PDA address and fail with Anchor's `ConstraintSeeds` / \"account already in use\" error before any program logic runs.\n\n### Threshold Enforcement\n\nWhen a signing request is created, the required economic weight is snapshotted from the live state:\n\n```rust\nlet required_stake_weight = protocol\n    .total_bonded\n    .checked_mul(protocol.threshold_bps as u64)\n    .ok_or(UnbridgeError::MathOverflow)?\n    / BPS_DENOMINATOR;\n```\n\n`BPS_DENOMINATOR` is the compile-time constant `10_000`. A threshold of `6_666 bps` therefore requires 66.66% of `total_bonded` at the moment of request creation. This snapshot is stored in `SigningRequest.required_stake_weight` and is immutable for the lifetime of the request. An operator joining or unbonding after request creation does not retroactively change what is needed to finalize that specific request.\n\nThe protocol-level `threshold_bps` is range-checked at every write site:\n\n```rust\nrequire!(\n    threshold_bps as u64 >= 1 && threshold_bps as u64 <= BPS_DENOMINATOR,\n    UnbridgeError::InvalidThreshold\n);\n```\n\nPer-request `threshold` (the minimum distinct partial count) is also range-checked: `threshold >= 1 && threshold <= operator_count`. Both conditions must be satisfied independently; a request can require a head-count threshold and a stake-weight threshold simultaneously.\n\n### Validity Windows\n\nEvery signing request carries an `expiry_slot = created_slot + validity_slots`. The hard ceiling is enforced at the protocol level:\n\n```rust\npub const MAX_VALIDITY_SLOTS_CEILING: u64 = 432_000; // ~48 h at 400 ms slots\n```\n\nThe ceiling is also checked at initialization and on every `update_config` call. A requester cannot post a request that lingers indefinitely; the maximum stale-intent window is 48 hours. After expiry, the request status transitions to `RequestStatus::Expired` and can no longer receive partial signatures.\n\n### Emergency Pause\n\n`pause` / `unpause` are gated on `AdminConfig`, which checks `protocol.admin == ctx.accounts.admin.key()`. When `protocol.paused == true`, every user- and operator-facing instruction (`register_operator`, `create_signing_request`, `begin_unbonding`) returns `UnbridgeError::ProtocolPaused` at the top of its handler before touching any state. Admin-only instructions (`update_config`, `slash_operator`, `unpause`) are not blocked by the pause flag.\n\n### Bonding and Withdrawal Flow\n\nThe bond lifecycle enforces an ordering invariant: an operator cannot withdraw collateral until it has exited the active signing set **and** waited out the unbonding delay:\n\n```mermaid\nsequenceDiagram\n    participant OP as Operator\n    participant PROG as Solana Program\n    participant VAULT as Bond Vault (Token-2022)\n    participant POOL as Slash Pool\n\n    OP->>PROG: register_operator(bond_amount, group_pubkey)\n    PROG->>VAULT: transfer_checked(bond_amount) via CPI\n    Note over PROG: operator.unbonding_at = 0<br/>operator.jailed = false<br/>protocol.total_bonded += stake_weight\n\n    OP->>PROG: begin_unbonding()\n    Note over PROG: operator.unbonding_at = current_slot + unbonding_slots<br/>operator.jailed = true<br/>protocol.total_bonded -= stake_weight<br/>protocol.operator_count -= 1\n\n    Note over OP: Wait until clock.slot >= unbonding_at\n\n    OP->>PROG: withdraw_bond()\n    PROG->>VAULT: transfer_checked(bonded_amount) via PDA signer\n    Note over PROG: operator account closed; rent returned\n\n    alt Admin initiates slash\n        PROG->>VAULT: transfer_checked(slash_amount) via PDA signer\n        VAULT->>POOL: funds move to slash pool\n        Note over PROG: operator.bonded_amount -= amount<br/>operator.stake_weight recomputed<br/>operator.jailed = true if below min_bond\n    end\n```\n\nThe protocol PDA signer seeds used for vault withdrawals and slashes are `&[PROTOCOL_SEED, &[protocol.bump]]`. No instruction other than `withdraw_bond` and `slash_operator` can move tokens out of the vault.\n\n---\n\n## Signing Scheme Security Boundary\n\nThis is the most consequential trust boundary in the protocol. The Solana program is responsible for **accounting and economic incentives**. Cryptographic validity of individual partial signatures is **not verified on-chain**.\n\n| Property | Enforced on-chain? | Mechanism |\n|---|---|---|\n| An operator cannot submit two shares for the same request | Yes | `[PARTIAL_SEED, request, operator]` PDA uniqueness |\n| The `share: [u8; 64]` bytes are cryptographically valid for the scheme | **No** | The share is a participation receipt; round validity is enforced off-chain by `engine/kobe` / `engine/kobe-ecdsa` |\n| The `aggregate_sig: [u8; 64]` is a valid group signature | **No** | Computed off-chain by the coordinator (real FROST/GG20); recorded on-chain by `aggregate_and_emit` |\n| The aggregate is non-zero before it is recorded | Yes | `aggregate_and_emit` rejects an all-zero signature |\n| The operator's `group_pubkey: [u8; 33]` corresponds to a valid key share | **No** | Checked off-chain at DKG / registration time |\n| The partial's scheme comes from a trusted source | n/a | Scheme is copied from the request, not supplied by the caller — there is no untrusted-scheme path, so no scheme-mismatch error exists |\n| Staked-weight threshold is met before finalization | Yes | `stake_weight_collected >= required_stake_weight` check |\n| Partial-count threshold is met before finalization | Yes | `partials_collected >= threshold` check |\n\n### Scheme Dispatch\n\nThe `SignatureScheme` enum drives which off-chain library handles cryptographic work:\n\n```rust\npub enum SignatureScheme {\n    FrostEd25519,    // SVM / Aptos / Sui — FROST Schnorr over Ed25519\n    Gg20Secp256k1,   // EVM / BTC / Tron — GG20 ECDSA over secp256k1\n}\n```\n\n`TargetVm` carries the destination context alongside the scheme:\n\n```rust\npub enum TargetVm { Svm, Evm, Tron, Cosmos, Bitcoin }\n```\n\nThe pairing matters because a mismatch (e.g., `FrostEd25519` targeting `Bitcoin`) would produce an aggregate signature unacceptable on the destination chain. The on-chain program stores both fields in `SigningRequest` but does not enforce scheme-to-VM compatibility; that validation belongs to the off-chain relayer and to protocol-layer tooling that constructs the signing request.\n\n---\n\n## Threat Model\n\n### Threat Matrix\n\n| Threat | Attacker | On-chain mitigation | Residual risk |\n|---|---|---|---|\n| Operator submits garbage share bytes | Malicious operator | None (share not verified on-chain) | Off-chain library rejects invalid share; stake at risk via slash |\n| Operator equivocates (two different shares for same request) | Malicious operator | PDA uniqueness prevents second on-chain submission | Equivocation in off-chain MPC round; slashable via `slash_operator` |\n| Operator unbonds before request finalizes | Rational operator gaming liveness | `required_stake_weight` snapshotted at creation; begin_unbonding removes weight from `total_bonded` immediately | Requests may fail to reach threshold if too many operators unbond concurrently |\n| Admin slashes an innocent operator | Malicious admin | None (admin-gated slash, no on-chain fraud proof) | **Honest weakness** — see below |\n| Requester floods with cheap requests | Griefing user | `request_fee > 0` (set in `initialize`); fee charged via `system_program::transfer` | Fee level is a governance parameter; low fees do not prevent spam |\n| Stale oracle inflates stake weight | Oracle manipulation | `StaleOraclePrice` error on stale feed; `InvalidOracleAccount` error on wrong account | Oracle compromise can over-weight operators |\n| Admin key compromise | External attacker | Two-step transfer (`transfer_admin` → `accept_admin`) only protects succession; does not protect against compromise of the current key | Single key failure; no on-chain multisig in current code |\n| Relayer censors the aggregate signature | Malicious relayer | Aggregate sig is publicly readable on-chain; any party can relay | Liveness risk only; safety unaffected since sig is on-chain |\n| Request lingers past expiry | — | `MAX_VALIDITY_SLOTS_CEILING = 432_000` enforced at creation | Expired requests cannot finalize; no lingering attack |\n| Math overflow in weight accounting | — | All arithmetic uses `checked_add` / `checked_mul`; returns `UnbridgeError::MathOverflow` | Conservative; no silent truncation |\n\n---\n\n## What Is Verified, and What Is Next\n\nThe core is built and independently verified, milestone by milestone, and the program is live on Solana devnet. The proofs are reproducible:\n\n- **FROST Ed25519 (`engine/kobe`):** `cd engine/kobe && cargo test` runs a 2-of-3 ceremony and checks the 64-byte aggregate two ways — FROST's own verify and the independent `ed25519-dalek` crate (the RFC 8032 primitive a Solana/Cosmos node runs). Wrong message, tampered signature, wrong group key, and sub-threshold quorum all fail. The group secret is never reconstructed.\n- **GG20 ECDSA (`engine/kobe-ecdsa`):** `cd engine/kobe-ecdsa && go test -v -timeout 600s` proves the secp256k1 signature natively valid on Ethereum (`go-ethereum` `Ecrecover` recovers the group address), Bitcoin (BIP-143 sighash, DER + SIGHASH_ALL under decred secp256k1, P2WPKH bech32 against the BIP-173 vector, low-S/BIP-62 enforced), and Tron (keccak → 0x41 → base58check against a known vector, (r,s,v) recovers to the group Tron address).\n- **Coordination loop (`engine/coordinator`):** the on-chain request drives the off-chain MPC and the real aggregate is recorded back via `aggregate_and_emit` and independently verified end-to-end on a local validator (`engine/coordinator/m7-demo.sh`).\n- **Networked operators:** three separate OS processes (distinct PIDs, ports, Ed25519 identity keys, share files) run GG20 DKG and a 2-of-3 sign over authenticated TCP; an on-chain request triggers it and the wire signature `ecrecover`-verifies to the group address (`engine/kobe-ecdsa/net`, coordinator net-demo). This proves the GG20/ETH path networked end-to-end.\n- **Live program:** `solana program show 4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6 --url devnet` (deploy tx `3LAt17P8Zmh2EYyphzVF4EHNY1uffkaJp7cp4V2yUpNh3MoV82iP5mEvdg18XMBUnKwANDGANCj43mEVRJUh6ZAQ`). Devnet only, not mainnet.\n\nWhat is **not** done yet, stated plainly:\n\n- **Networked-operator hardening.** Today's network proof is localhost: no TLS, no PKI/CA, a static pinned-key directory, and a fail-stop abort rather than GG20 *identifiable* abort. Shares live in local files, not an HSM.\n- **No audit.** `tss-lib` and `frost-ed25519` are themselves audited, but this integration and the on-chain program are not. Nothing here is audited for real value. There is no audit badge, no partner, and no live token.\n- **FROST networked path.** Only the GG20/ETH path is proven networked end-to-end. The FROST signer follows identical wiring but its `net/` operator is not built yet.\n- **Price feed.** The Pyth LST feed is currently a 1:1 SOL-pegged stub; the real integration point is marked in-source.\n\n---\n\n## Honest Weaknesses\n\nThe following limitations are present in the current on-chain code. They are accurately documented here, not papered over.\n\n### 1. Admin-Gated Slashing (No On-Chain Fraud Proof)\n\nThe `slash_operator` instruction comment is explicit:\n\n> \"In production this entry point is gated by a verified fraud proof (equivocation / invalid-share / liveness fault) produced by the signing libraries; the on-chain effect — moving collateral and jailing — is what is enforced here.\"\n\nIn the current code, `slash_operator` is gated solely on `AdminConfig`, meaning the admin key can slash any operator for any `amount` up to `operator.bonded_amount` and set any `reason: u8` without supplying cryptographic evidence. The on-chain check is:\n\n```rust\nrequire!(\n    amount <= ctx.accounts.operator.bonded_amount,\n    UnbridgeError::SlashAmountExceedsBond\n);\n```\n\nNothing else. Integrators and operators must trust that the admin will only slash on production-grade fraud evidence generated by the off-chain signing libraries. This is a systemic trust assumption that protocol governance must resolve (e.g., by requiring a fraud proof account in the `SlashOperator` context before slashing becomes trustless).\n\n### 2. No On-Chain Cryptographic Share Verification\n\nThe `PartialSignature.share: [u8; 64]` field is stored verbatim. The Solana program does not call any native precompile or custom verifier to check that the bytes constitute a valid FROST partial signature (Ed25519) or a valid GG20 partial ciphertext (secp256k1). A malicious operator can submit 64 bytes of garbage, satisfy the stake-weight threshold in collusion with enough bonded-but-corrupt operators, and cause the off-chain aggregator to fail to produce a valid group signature while having all their shares credited on-chain. They bear slashing risk but only after the fact via admin action.\n\n### 3. Single Admin Key\n\nThe current `Protocol.admin` field is a single `Pubkey`. The two-step handover (`transfer_admin` → `accept_admin`) protects against accidental overwrite but not against compromise of the active key. A compromised admin can:\n- Pause the protocol indefinitely\n- Slash all operators to zero\n- Reduce `threshold_bps` to `1` (one basis point), trivially enabling threshold fulfillment\n- Raise `request_fee` to an arbitrarily high lamport value\n\nThere is no on-chain multisig requirement or timelock on `AdminConfig` instructions in the current code.\n\n### 4. Stake Weight Depends on Oracle Liveness\n\n`compute_stake_weight` reads `lst_price_feed` (a Pyth price account stored as `protocol.lst_price_feed: Pubkey`). If the oracle becomes stale, operators cannot register and the per-operator `stake_weight` cannot be recomputed after a slash. The program returns `UnbridgeError::StaleOraclePrice` and `UnbridgeError::InvalidOracleAccount` as hard errors, so bad oracle data halts registration rather than silently under/over-counting, but Pyth downtime becomes a liveness risk for operator onboarding.\n\n### 5. `total_bonded` Accounting Asymmetry on Slash\n\nIn `slash_operator`, the weight delta is only subtracted from `protocol.total_bonded` if the operator was active (`was_active = unbonding_at == 0 && !jailed`). If the operator was already jailed (e.g., had begun unbonding) and is then slashed, `total_bonded` is not further decremented. This is correct because `begin_unbonding` already removed the weight. The code relies on `was_active` being computed before the operator's `jailed` field is mutated:\n\n```rust\nlet was_active = ctx.accounts.operator.unbonding_at == 0 && !ctx.accounts.operator.jailed;\n// ...\nlet operator = &mut ctx.accounts.operator;\n// mutations happen here\nif was_active { /* adjust protocol.total_bonded */ }\n```\n\nIf the pre-mutation snapshot is wrong (e.g., because another instruction in the same transaction mutated the operator), the accounting could drift. In practice Anchor's single-account-per-type constraint prevents same-transaction double-write, but composability with future instructions warrants review.\n\n---\n\n## Admin Key Risk: Mitigations Available\n\nThe current code provides a clean two-step admin transfer path. The intended upgrade path to reduce admin key risk (not yet in the on-chain code, noted here for completeness) would be to make `protocol.admin` a program-derived multisig or a governance program address. Until that upgrade, operators and users bear admin key risk.\n\nThe two-step handover as implemented:\n\n```rust\n// Step 1 — nominate\npub fn transfer_admin(ctx: Context<AdminConfig>, new_admin: Pubkey) -> Result<()> {\n    require_keys_neq!(new_admin, Pubkey::default(), UnbridgeError::InvalidAdminTransfer);\n    ctx.accounts.protocol.pending_admin = new_admin;\n    Ok(())\n}\n\n// Step 2 — accept (called by the nominee, not the current admin)\npub fn accept_admin(ctx: Context<AcceptAdmin>) -> Result<()> {\n    let protocol = &mut ctx.accounts.protocol;\n    require_keys_eq!(\n        protocol.pending_admin,\n        ctx.accounts.new_admin.key(),\n        UnbridgeError::Unauthorized\n    );\n    protocol.admin = protocol.pending_admin;\n    protocol.pending_admin = Pubkey::default();\n    Ok(())\n}\n```\n\nThis prevents the current admin from unilaterally redirecting control to an arbitrary address without the nominee's cooperation. After `accept_admin`, `pending_admin` is zeroed to `Pubkey::default()`, preventing replay.\n\n---\n\n## Oracle Risk\n\nThe LST price feed is stored at `protocol.lst_price_feed: Pubkey`, set once at `initialize` and not changeable by `update_config` (the field is absent from `update_config`'s parameter list). This means oracle migration requires an admin upgrade path that is not currently implemented.\n\nThe `InvalidOracleAccount` error fires if the passed oracle account does not match `protocol.lst_price_feed`:\n\n```rust\n// UnbridgeError::InvalidOracleAccount\n#[msg(\"Oracle account does not match the configured price feed\")]\nInvalidOracleAccount,\n```\n\nThe `StaleOraclePrice` error fires if the feed's confidence interval or publish time falls outside the program's staleness threshold. The exact staleness window is enforced inside `compute_stake_weight`; the specific confidence interval or age check is not shown in the provided excerpt but the error path is defined.\n\n---\n\n## Edge Cases and Failure Modes\n\n### Threshold Impossible After Operator Churn\n\nIf enough operators call `begin_unbonding` between when a request is created and when it needs to be finalized, `required_stake_weight` (snapshotted at creation) may exceed the weight still held by operators who remain active. The request will expire with `RequestStatus::Expired`. This is a liveness failure, not a safety failure. The requester loses the `request_fee` and must repost.\n\n### Jailed Operator After Slash Falls Below `min_bond`\n\n```rust\nif operator.bonded_amount < min_bond {\n    operator.jailed = true;\n}\n```\n\nAn operator slashed below `min_bond` is automatically jailed even if they had not initiated unbonding. Their weight is removed from `total_bonded` and `operator_count` decremented, but their `bonded_amount` is not zero. They can still call `begin_unbonding` to start the withdrawal timer and eventually recover the residual bond. The residual bond remains in the vault and is slashable until `withdraw_bond` is called.\n\n### Zero-Weight Operator Registration Race\n\n`register_operator` checks `bond_amount >= protocol.min_bond` but computes `stake_weight` from the oracle at the time of the call. If the oracle price is very low (extreme LST depeg) at registration time, `stake_weight` may be non-zero but very small. The operator joins the set, contributes negligible weight to threshold calculations, and poses minimal economic security. There is no minimum `stake_weight` floor check separate from the `min_bond` LST amount check.\n\n### `total_bonded` Saturation Behavior\n\nSubtractions from `protocol.total_bonded` use `saturating_sub` rather than `checked_sub`:\n\n```rust\nprotocol.total_bonded = protocol.total_bonded.saturating_sub(removed_weight);\nprotocol.operator_count = protocol.operator_count.saturating_sub(1);\n```\n\nThis means a double-removal bug (if one were introduced in a future instruction) would silently clamp to zero rather than error. The use of saturation is a defensive choice against underflow panics; reviewers should audit any future instruction that removes weight to ensure it does not double-count.\n\n### Request Fee Bypass\n\nThe lamport fee transfer uses `system_program::transfer`, which requires the requester to be a signer. There is no way to bypass the fee for a `create_signing_request` call. However, `request_fee = 0` is a valid configuration (set at `initialize` or via `update_config`). If the fee is zero, the fee transfer CPI is skipped entirely:\n\n```rust\nif protocol.request_fee > 0 {\n    system_program::transfer(/* ... */, protocol.request_fee)?;\n}\n```\n\nAt zero fee, request spam costs only the Solana transaction fee plus the rent for the `SigningRequest` account. Protocol governance should set a non-zero `request_fee` to prevent griefing.\n\n---\n\n## Security-Relevant Error Reference\n\n| Error | Code mnemonic | Condition that triggers it |\n|---|---|---|\n| `ProtocolPaused` | 6000 | Admin called `pause`; user/operator action blocked |\n| `Unauthorized` | 6001 | Caller is not `protocol.admin` or `protocol.pending_admin` |\n| `InvalidThreshold` | 6002 | `threshold_bps` outside `[1, 10_000]` or per-request threshold outside `[1, operator_count]` |\n| `InsufficientBond` | 6003 | `bond_amount < protocol.min_bond` at registration |\n| `OperatorJailed` | 6004 | Jailed or unbonding operator attempts to sign |\n| `AlreadyUnbonding` | 6005 | `begin_unbonding` called when `unbonding_at != 0` |\n| `NotUnbonding` | 6006 | `withdraw_bond` called before `begin_unbonding` |\n| `UnbondingNotComplete` | 6007 | `withdraw_bond` called before `clock.slot >= unbonding_at` |\n| `RequestExpired` | 6008 | `submit_partial_signature` or finalize called after `expiry_slot` |\n| `RequestNotPending` | 6009 | Action on a non-Pending request |\n| `ThresholdNotMet` | 6010 | Finalize attempted before weight + count thresholds satisfied |\n| `RequestAlreadyFinalized` | 6011 | Duplicate finalization attempt |\n| `MalformedPartialSignature` | 6012 | Share bytes fail basic format check (length, non-zero) |\n| `EmptyMessageHash` | 6013 | `message_hash` is all zeros |\n| `StaleOraclePrice` | 6014 | Oracle publish time is too old |\n| `InvalidOracleAccount` | 6015 | Passed oracle account != `protocol.lst_price_feed` |\n| `InvalidVault` | 6016 | Vault or pool account does not match protocol config |\n| `InvalidValidityWindow` | 6017 | `validity_slots` outside `[1, MAX_VALIDITY_SLOTS_CEILING]` |\n| `NoActiveOperators` | 6018 | `protocol.operator_count == 0` at request creation |\n| `SlashAmountExceedsBond` | 6019 | Slash `amount > operator.bonded_amount` |\n| `InvalidAdminTransfer` | 6020 | `new_admin == Pubkey::default()` in `transfer_admin` |\n| `MathOverflow` | 6021 | Checked arithmetic (add/mul) overflowed |"
  },
  {
    "slug": "economics",
    "title": "Economics",
    "body": "# Economics\n\nUnbridge's economic security model is built on three interlocking mechanisms: bonded LST collateral that converts to stake-weighted signing power, a lamport fee charged per signing request that flows directly to the protocol PDA, and a slashing system that moves misbehaving operators' bonds into an isolated slash pool. Everything is enforced on-chain by the program at `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6`.\n\n---\n\n## Core Parameters\n\nThe singleton `Protocol` account (PDA seed `[b\"protocol\"]`) stores every live economic parameter. They are set at `initialize` and can be tuned by the admin at any time via `update_config`.\n\n| Field | Type | Role |\n|---|---|---|\n| `threshold_bps` | `u16` | Fraction of `total_bonded` required to finalize a request, expressed in basis points (1 = 0.01 %, 10 000 = 100 %) |\n| `min_bond` | `u64` | Minimum raw LST units an operator must deposit to enter the signing set |\n| `unbonding_slots` | `u64` | Slots between `begin_unbonding` and `withdraw_bond`; the operator's weight leaves `total_bonded` immediately but the collateral remains slashable until withdrawal |\n| `request_fee` | `u64` | Lamports charged to the requester per `create_signing_request` |\n| `max_validity_slots` | `u64` | Per-request deadline ceiling; hard upper bound is the protocol constant `MAX_VALIDITY_SLOTS_CEILING = 432_000` (~48 h at 400 ms/slot) |\n| `lst_price_feed` | `Pubkey` | Pyth price account used by `compute_stake_weight` to denominate bonds in SOL-equivalent weight |\n| `total_bonded` | `u64` | Running sum of all active operators' `stake_weight`; the denominator in threshold arithmetic |\n| `operator_count` | `u32` | Count of operators currently in the active signing set (jailed / unbonding operators are excluded) |\n| `request_nonce` | `u64` | Monotonic counter that seeds every `SigningRequest` PDA |\n\nHard-coded module-level constants in `lib.rs`:\n\n```rust\npub const BPS_DENOMINATOR: u64 = 10_000;\npub const MAX_VALIDITY_SLOTS_CEILING: u64 = 432_000; // ~48 h\n```\n\n`BPS_DENOMINATOR` is the only divisor used in threshold arithmetic — there is no separate percentage helper.\n\n---\n\n## Operator Bonding\n\n### Bond deposit\n\nAn operator enters the signing set by calling `register_operator`, providing:\n\n```rust\npub fn register_operator(\n    ctx: Context<RegisterOperator>,\n    group_pubkey: [u8; 33],  // compressed FROST/ECDSA group pubkey share\n    bond_amount: u64,         // raw LST token units\n) -> Result<()>\n```\n\nThe program immediately performs a Token-2022 `transfer_checked` CPI, moving `bond_amount` from the operator's token account into the protocol-owned vault:\n\n```rust\ntransfer_checked(\n    CpiContext::new(\n        ctx.accounts.token_program.to_account_info(),\n        TransferChecked {\n            from: ctx.accounts.operator_token_account.to_account_info(),\n            mint: ctx.accounts.bond_mint.to_account_info(),\n            to:   ctx.accounts.bond_vault.to_account_info(),\n            authority: ctx.accounts.authority.to_account_info(),\n        },\n    ),\n    bond_amount,\n    ctx.accounts.bond_mint.decimals,\n)?;\n```\n\nThe vault PDA (`[b\"bond_vault\", protocol]`) is the sole Token-2022 token account owned by the protocol PDA. No third party can withdraw from it; only the program's own signer seeds can authorize outbound transfers.\n\n### Stake weight and the oracle\n\nRaw LST units are not used directly in threshold math. The program calls `compute_stake_weight(&ctx.accounts.lst_price_feed, bond_amount)` to produce a SOL-denominated `stake_weight: u64` stored on the `Operator` account. The feed is the Pyth account whose key is stored in `protocol.lst_price_feed`. If the oracle is stale the function returns `UnbridgeError::StaleOraclePrice` and registration fails atomically.\n\nThe mapping is 1:1 under the current oracle policy: `stake_weight` is directly proportional to the SOL value of the LST at the moment of registration (or slashing recompute). The `Operator` account stores both raw and weighted figures:\n\n```\nOperator {\n    bonded_amount:  u64,   // raw LST units in the vault\n    stake_weight:   u64,   // SOL-equivalent weight used in threshold math\n    ...\n}\n```\n\n### Global accounting invariant\n\nAfter every registration the program updates protocol-level counters with checked arithmetic to prevent silent overflow:\n\n```rust\nprotocol.total_bonded = protocol.total_bonded\n    .checked_add(stake_weight)\n    .ok_or(UnbridgeError::MathOverflow)?;\nprotocol.operator_count = protocol.operator_count\n    .checked_add(1)\n    .ok_or(UnbridgeError::MathOverflow)?;\n```\n\n### Minimum bond enforcement\n\n```rust\nrequire!(bond_amount >= protocol.min_bond, UnbridgeError::InsufficientBond);\n```\n\nThis check runs before the Token-2022 CPI. If `min_bond` is later raised by `update_config`, existing operators who are already bonded are grandfathered in; they only encounter the new floor if they are slashed below the new threshold (which triggers an automatic jail, see [Slashing](#slashing)).\n\n---\n\n## Threshold Arithmetic\n\n### Required stake weight per request\n\nWhen a user calls `create_signing_request` the program snapshots the security target into the new `SigningRequest` account:\n\n```rust\nlet required_stake_weight = protocol.total_bonded\n    .checked_mul(protocol.threshold_bps as u64)\n    .ok_or(UnbridgeError::MathOverflow)?\n    / BPS_DENOMINATOR;\n```\n\nThis value is immutable for the lifetime of the request. It does not change if operators join, leave, or are slashed after the request is created. A request created when `total_bonded = 1_000_000` and `threshold_bps = 6_666` requires exactly `666_600` stake weight to finalize, regardless of what happens to the operator set afterwards.\n\n### Finalization condition\n\nA request moves to `RequestStatus::Aggregated` when the `aggregate_and_emit(aggregate_sig)` instruction verifies that both of the following hold:\n\n1. `stake_weight_collected >= required_stake_weight`\n2. `partials_collected >= threshold` (the per-request minimum-operator-count floor set by the requester at creation)\n\nIt also requires that the request is still `Pending`, that `slot <= expiry_slot`, and that the supplied `aggregate_sig` is non-zero. The canonical group signature itself is computed off-chain by the coordinator; this instruction records it, it does not derive it. Neither threshold alone is sufficient. The `ThresholdNotMet` error is returned if either is unsatisfied.\n\n### Request-level threshold parameter\n\n```rust\npub fn create_signing_request(\n    ...\n    threshold: u16,        // minimum distinct partial sigs required\n    validity_slots: u64,   // slots until expiry\n) -> Result<()>\n```\n\nThe requester's `threshold` argument is bounded:\n\n```rust\nrequire!(\n    threshold >= 1 && (threshold as u32) <= protocol.operator_count,\n    UnbridgeError::InvalidThreshold\n);\n```\n\nThis means a requester cannot demand more signers than currently exist in the active set, and cannot set it to zero.\n\n---\n\n## Request Fee Model\n\nEvery `create_signing_request` call transfers `protocol.request_fee` lamports from the requester to the protocol PDA via a System Program CPI:\n\n```rust\nif protocol.request_fee > 0 {\n    system_program::transfer(\n        CpiContext::new(\n            ctx.accounts.system_program.to_account_info(),\n            SystemTransfer {\n                from: ctx.accounts.requester.to_account_info(),\n                to:   ctx.accounts.protocol.to_account_info(),\n            },\n        ),\n        protocol.request_fee,\n    )?;\n}\n```\n\nThe fee is denominated in lamports, not LST units. It accumulates in the protocol PDA's native SOL balance. There is no per-request escrow account; the fee is non-refundable once the instruction succeeds, regardless of whether the request is ultimately fulfilled, expired, or cancelled.\n\nFee distribution to operators (e.g. pro-rata by partials submitted) is not implemented in the current on-chain program. The `Operator.partials_submitted` counter tracks lifetime contribution and is the natural basis for an off-chain or future on-chain distribution, but no disbursement logic exists on-chain today.\n\n---\n\n## Unbonding\n\nUnbonding is a two-step process designed to keep bonds slashable through the exit period.\n\n### Step 1: begin_unbonding\n\n```rust\npub fn begin_unbonding(ctx: Context<OperatorLifecycle>) -> Result<()>\n```\n\nEffects:\n- Sets `operator.unbonding_at = clock.slot + protocol.unbonding_slots`\n- Sets `operator.jailed = true` (the operator cannot accept new requests)\n- Subtracts `operator.stake_weight` from `protocol.total_bonded`\n- Decrements `protocol.operator_count`\n\nThe operator is removed from the active signing set immediately. Future signing requests will not count this operator toward `required_stake_weight`. However the raw `bonded_amount` remains in the vault — the program can still slash it.\n\nGuard:\n\n```rust\nrequire!(operator.unbonding_at == 0, UnbridgeError::AlreadyUnbonding);\n```\n\nCalling `begin_unbonding` a second time returns `AlreadyUnbonding`.\n\n### Step 2: withdraw_bond\n\n```rust\npub fn withdraw_bond(ctx: Context<WithdrawBond>) -> Result<()>\n```\n\nGuards:\n\n```rust\nrequire!(operator.unbonding_at != 0, UnbridgeError::NotUnbonding);\nrequire!(clock.slot >= operator.unbonding_at, UnbridgeError::UnbondingNotComplete);\n```\n\nOn success the program transfers `operator.bonded_amount` from the vault back to the operator's token account using the protocol PDA as the signer:\n\n```rust\nlet signer_seeds: &[&[&[u8]]] = &[&[PROTOCOL_SEED, &[ctx.accounts.protocol.bump]]];\ntransfer_checked(\n    CpiContext::new_with_signer(..., signer_seeds),\n    amount,\n    ctx.accounts.bond_mint.decimals,\n)?;\n```\n\nThe `Operator` account is closed and rent is returned to the authority.\n\n### Timeline\n\n```\nslot N              slot N + unbonding_slots\n  |                          |\n  begin_unbonding         withdraw_bond (earliest)\n  weight removed          bond unlocked\n  jailed = true\n  still slashable ──────────────────────────────>\n```\n\nDuring the entire `unbonding_slots` window, `slash_operator` can still reduce `bonded_amount` to zero and permanently jail the operator even though they have already left the active set.\n\n---\n\n## Slashing\n\n### Mechanics\n\n```rust\npub fn slash_operator(\n    ctx: Context<SlashOperator>,\n    amount: u64,\n    reason: u8,\n) -> Result<()>\n```\n\nThe `reason` byte is an opaque tag — the current program stores it in the emitted `OperatorSlashed` event for off-chain indexing. It is not validated or range-checked on-chain.\n\nThe program enforces a ceiling:\n\n```rust\nrequire!(\n    amount <= ctx.accounts.operator.bonded_amount,\n    UnbridgeError::SlashAmountExceedsBond\n);\n```\n\nSlashed collateral moves from the vault into the slash pool (PDA seed `[b\"slash_pool\", protocol]`):\n\n```rust\ntransfer_checked(\n    CpiContext::new_with_signer(..., signer_seeds),  // protocol PDA signs\n    amount,\n    ctx.accounts.bond_mint.decimals,\n)?;\n```\n\n### Post-slash accounting\n\nAfter the transfer the program recomputes the operator's stake weight from the residual bond:\n\n```rust\noperator.bonded_amount = operator.bonded_amount.saturating_sub(amount);\noperator.slash_count   = operator.slash_count.checked_add(1)?;\nlet new_weight = compute_stake_weight(&ctx.accounts.lst_price_feed, operator.bonded_amount)?;\noperator.stake_weight  = new_weight;\nif operator.bonded_amount < min_bond {\n    operator.jailed = true;\n}\n```\n\nIf the operator was active (not already jailed or unbonding) at the time of the slash, the protocol-wide `total_bonded` is decremented by the weight delta and, if the operator is now jailed, by the remaining new weight as well:\n\n```rust\nif was_active {\n    let weight_delta = operator_weight_before.saturating_sub(new_weight);\n    protocol.total_bonded = protocol.total_bonded.saturating_sub(weight_delta);\n    if operator.jailed {\n        protocol.total_bonded = protocol.total_bonded.saturating_sub(new_weight);\n        protocol.operator_count = protocol.operator_count.saturating_sub(1);\n    }\n}\n```\n\n`saturating_sub` is used throughout the weight-removal path to avoid underflow panics in edge cases where state could be inconsistent.\n\n### Slash pool\n\nThe slash pool is a separate Token-2022 account (PDA seed `[b\"slash_pool\", protocol]`) that accumulates all slashed collateral. There is no on-chain disbursement instruction in the current program. Redistribution — e.g., to compensate affected users or burn the LST — is outside the on-chain scope and would require a new governance instruction.\n\n---\n\n## Fund Flow Diagram\n\n```mermaid\nsequenceDiagram\n    participant Operator as Operator\n    participant Program as Unbridge Program\n    participant Vault as bond_vault PDA\n    participant Pool as slash_pool PDA\n    participant Protocol as protocol PDA\n    participant Requester as Requester\n    participant Coordinator as Coordinator (off-chain MPC)\n\n    Note over Operator,Vault: register_operator\n    Operator->>Vault: transfer_checked(bond_amount, Token-2022)\n    Program->>Program: compute_stake_weight(lst_price_feed, bond_amount)\n    Program->>Protocol: total_bonded += stake_weight\n\n    Note over Requester,Protocol: create_signing_request\n    Requester->>Protocol: system_program::transfer(request_fee lamports)\n    Program->>Program: snapshot required_stake_weight = total_bonded * threshold_bps / 10_000\n\n    Note over Operator,Program: submit_partial_signature records a participation receipt\n    Operator->>Program: submit partial sig receipt [b\"partial\", request, operator]\n    Program->>Program: stake_weight_collected += operator.stake_weight; partials_collected += 1\n    Program->>Program: (off-chain coordinator computes the canonical aggregate)\n    Coordinator->>Program: aggregate_and_emit(aggregate_sig) once thresholds met\n\n    Note over Operator,Vault: begin_unbonding → withdraw_bond\n    Program->>Protocol: total_bonded -= stake_weight (immediately)\n    Operator->>Program: withdraw_bond after unbonding_slots\n    Vault->>Operator: transfer_checked(bonded_amount, Token-2022)\n\n    Note over Program,Pool: slash_operator\n    Program->>Vault: transfer_checked(slash_amount) out of vault\n    Vault->>Pool: slash_amount lands in slash_pool\n    Program->>Protocol: total_bonded -= weight_delta (if was active)\n```\n\n---\n\n## Signing Request Lifecycle and Validity Window\n\n```rust\nrequire!(\n    validity_slots >= 1 && validity_slots <= protocol.max_validity_slots,\n    UnbridgeError::InvalidValidityWindow\n);\n```\n\nThe `max_validity_slots` field is itself bounded by `MAX_VALIDITY_SLOTS_CEILING = 432_000`. At 400 ms per slot this is exactly 48 hours. The program rejects any attempt to configure a tighter ceiling of zero or a looser one above 432,000 with `InvalidValidityWindow`.\n\nOnce `clock.slot > expiry_slot` the request transitions to `RequestStatus::Expired`. Any instruction that touches an expired request (e.g., a late partial submission) will return `RequestExpired`. The request fee is not refunded.\n\n| Condition | Error |\n|---|---|\n| `validity_slots == 0` | `InvalidValidityWindow` |\n| `validity_slots > max_validity_slots` | `InvalidValidityWindow` |\n| `max_validity_slots > 432_000` | `InvalidValidityWindow` |\n| Partial submitted after `expiry_slot` | `RequestExpired` |\n| Status not `Pending` at aggregation | `RequestNotPending` |\n| Request already `Aggregated` | `RequestAlreadyFinalized` |\n\n---\n\n## Stake Weight and Threshold: Worked Example\n\nAssume the protocol is initialized with:\n\n- `threshold_bps = 6_667` (~66.67 %)\n- `min_bond = 1_000_000` (1 000 000 raw LST units)\n- `unbonding_slots = 216_000` (~24 h)\n- `request_fee = 5_000_000` lamports (0.005 SOL)\n- `max_validity_slots = 432_000` (~48 h)\n\nThree operators bond:\n\n| Operator | `bonded_amount` | `stake_weight` (oracle) |\n|---|---|---|\n| A | 5 000 000 | 4 800 000 |\n| B | 3 000 000 | 2 900 000 |\n| C | 2 000 000 | 1 950 000 |\n\nAfter all three register:\n\n```\ntotal_bonded    = 4_800_000 + 2_900_000 + 1_950_000 = 9_650_000\noperator_count  = 3\n```\n\nA requester calls `create_signing_request` with `threshold = 2` (at least 2 distinct operators):\n\n```\nrequired_stake_weight = 9_650_000 * 6_667 / 10_000 = 6_433_555\n```\n\nOperators A and B submit partial signatures. Their combined weight is `4_800_000 + 2_900_000 = 7_700_000`. Both conditions are met:\n\n```\n7_700_000 >= 6_433_555   ✓  (stake weight)\n2         >= 2           ✓  (partial count)\n```\n\nThe request finalizes. If only operator B had submitted (`2_900_000 < 6_433_555`), the stake threshold would be unmet even though the partial count is 1 (also below threshold = 2).\n\n---\n\n## Edge Cases and Failure Modes\n\n### Operator slashed below min_bond mid-request\n\nIf operator A is slashed mid-flight on a pending request, `required_stake_weight` is **not** retroactively updated — it was snapshotted at creation. The weight that A would contribute to partial collection (`operator.stake_weight`) is, however, recomputed after the slash. If A's residual weight after slashing still satisfies the threshold alongside other operators, the request can finalize normally. If not, it expires at `expiry_slot`.\n\n### total_bonded drops to zero after creation\n\nIf every operator unbonds after a request is created, `required_stake_weight` is already snapshotted. The request will simply expire, since no operator is left to contribute partials. Subsequent `create_signing_request` calls gate on `protocol.operator_count > 0`:\n\n```rust\nrequire!(protocol.operator_count > 0, UnbridgeError::NoActiveOperators);\n```\n\nBut this guard protects new requests only; existing pending requests are not cancelled.\n\n### Double unbonding\n\n```rust\nrequire!(operator.unbonding_at == 0, UnbridgeError::AlreadyUnbonding);\n```\n\nAn operator who has already called `begin_unbonding` cannot call it again. This prevents `total_bonded` from being double-decremented.\n\n### Slash during unbonding\n\n`was_active` is computed before the slash transfer as:\n\n```rust\nlet was_active = operator.unbonding_at == 0 && !operator.jailed;\n```\n\nAn unbonding operator has `unbonding_at != 0`, so `was_active = false`. The weight delta path that adjusts `total_bonded` is skipped — the weight was already removed when `begin_unbonding` ran. Only the `bonded_amount` in the vault is reduced, and if it falls below `min_bond` the operator is jailed (it already is in practice, since `begin_unbonding` sets `jailed = true`).\n\n### Oracle staleness at registration\n\n`compute_stake_weight` reads the Pyth feed. If the feed is stale the function returns `StaleOraclePrice` and the `register_operator` transaction fails atomically, leaving the bond in the operator's token account. No partial state is committed.\n\n### Math overflow guards\n\nAll additive paths on `total_bonded`, `operator_count`, `stake_weight_collected`, and `request_nonce` use `checked_add` / `checked_mul` and map overflow to `UnbridgeError::MathOverflow`. Subtractive paths on `total_bonded` use `saturating_sub` to avoid panics during cleanup (e.g., if weight accounting drifts due to oracle price changes between registration and slash).\n\n---\n\n## Account Space and Rent\n\nAccount sizes are derived from `InitSpace` (field-level derivation) plus the 8-byte Anchor discriminator. Economics-relevant accounts:\n\n| Account | PDA Seeds | INIT_SPACE | Total rent-exempt bytes |\n|---|---|---|---|\n| `Protocol` | `[b\"protocol\"]` | 248 | 256 |\n| `Operator` | `[b\"operator\", protocol, authority]` | 143 | 151 |\n| `SigningRequest` | `[b\"request\", protocol, request_id_le]` | 224 | 232 |\n| `PartialSignature` | `[b\"partial\", request, operator]` | 146 | 154 |\n\n`SigningRequest` rent is paid by the requester at account creation. `PartialSignature` rent is paid by the submitting operator. On close (finalization or expiry), rent is returned to the `requester` (for `SigningRequest`) and to the submitting operator's authority (for `PartialSignature`). Rent economics are independent of the LST bond system; they are standard Solana lamport mechanics."
  },
  {
    "slug": "getting-started",
    "title": "Getting Started",
    "body": "# Getting Started with Unbridge\n\nUnbridge is a threshold-signature coordination and aggregation layer deployed on Solana. This guide walks you from a bare machine to a running devnet deployment with a live signing request — covering every prerequisite, build flag, PDA derivation, and on-chain call with the exact parameters the program enforces.\n\nThe reconciled program is already live on Solana devnet (devnet only, not mainnet). You can confirm the deploy before building anything: `solana program show 4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6 --url devnet`. The off-chain signers are verifiable too — `cd engine/kobe && cargo test` (2-of-3 FROST Ed25519, checked against `ed25519-dalek`) and `cd engine/kobe-ecdsa && go test -v -timeout 600s` (GG20 secp256k1, checked native-valid on Ethereum, Bitcoin, and Tron) — and the full on-chain loop runs end-to-end on a local validator via `engine/coordinator/m7-demo.sh`.\n\n## Prerequisites\n\n### Toolchain Versions\n\n| Tool | Required version | Notes |\n|---|---|---|\n| Rust (stable) | 1.75+ | `rustup update stable` |\n| Solana CLI | 1.18.26 | Must match `solana-program =1.18.26` in Cargo |\n| Anchor CLI | 0.30.1 | Program was written against Anchor 0.30.1 |\n| Node.js | 20 LTS | Client SDK scripts |\n| Yarn / pnpm | any | Package manager for the TS client |\n\nSolana CLI and Anchor version must be pinned exactly — the on-chain program depends on `solana-program =1.18.26` and `anchor-lang` / `anchor-spl` 0.30.1. Mismatched toolchains produce incompatible discriminators.\n\n```bash\n# Install Solana CLI 1.18.26\nsh -c \"$(curl -sSfL https://release.solana.com/v1.18.26/install)\"\n\n# Install Anchor CLI 0.30.1 via avm\ncargo install --git https://github.com/coral-xyz/anchor avm --locked --force\navm install 0.30.1\navm use 0.30.1\n\n# Verify\nanchor --version   # anchor-cli 0.30.1\nsolana --version   # solana-cli 1.18.26\n```\n\n### Accounts You Need Before Starting\n\n- **Admin keypair** — will be set as `protocol.admin` at `initialize`. Keep this offline in production.\n- **Operator keypair** — per-operator authority signer for `register_operator`, `begin_unbonding`, `withdraw_bond`, and `submit_partial_signature`.\n- **Token-2022 LST mint** — the collateral mint. Must be a Token-2022 mint (not legacy Token); `register_operator` uses `transfer_checked` via `TokenInterface`.\n- **Pyth price feed account** — passed as `lst_price_feed` on `initialize`. Used inside `compute_stake_weight` to value bonded LST in SOL terms.\n\n## Workspace Layout\n\n```\ndistin/\n├── Anchor.toml\n├── Cargo.toml          # workspace root\n├── programs/\n│   └── distin/\n│       ├── Cargo.toml\n│       └── src/\n│           ├── lib.rs\n│           ├── state.rs\n│           └── errors.rs\n└── tests/\n    └── distin.ts\n```\n\nThe program is declared at:\n\n```\ndeclare_id!(\"4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6\");\n```\n\nThis is the canonical program ID. Every PDA derivation must use this pubkey as the program ID.\n\n## Build\n\n```bash\nanchor build\n```\n\nThe build produces `target/deploy/distin.so` and `target/idl/distin.json`. The IDL is what the TypeScript client uses for account deserialization and instruction encoding.\n\nIf the build errors on the Token-2022 CPI path, confirm `anchor-spl` has the `token-2022` feature enabled in `programs/distin/Cargo.toml`:\n\n```toml\nanchor-spl = { version = \"0.30.1\", features = [\"token-2022\"] }\n```\n\n## Deploy to Devnet\n\n```bash\n# Point CLI at devnet\nsolana config set --url https://api.devnet.solana.com\n\n# Airdrop SOL to your deploy authority\nsolana airdrop 4\n\n# Deploy\nanchor deploy --provider.cluster devnet\n```\n\nThis sets the program at `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6`. If you are deploying a fork under a different ID, update `declare_id!` and rebuild.\n\n## PDA Reference\n\nEvery on-chain account is a PDA. The seeds are defined in `state.rs` and must be reproduced exactly in client code.\n\n| Account | Seeds | Notes |\n|---|---|---|\n| `Protocol` (singleton) | `[b\"protocol\"]` | One per deployment |\n| `bond_vault` (Token-2022) | `[b\"bond_vault\", protocol]` | Holds active operator bonds |\n| `slash_pool` (Token-2022) | `[b\"slash_pool\", protocol]` | Receives slashed collateral |\n| `Operator` | `[b\"operator\", protocol, authority]` | One per operator keypair |\n| `SigningRequest` | `[b\"request\", protocol, request_id_le]` | `request_id` is little-endian `u64` bytes |\n| `PartialSignature` | `[b\"partial\", request, operator]` | Uniqueness prevents double-submit |\n\n```typescript\n\nconst PROGRAM_ID = new PublicKey(\"4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6\");\n\n// Protocol PDA\nconst [protocolPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"protocol\")],\n  PROGRAM_ID\n);\n\n// Bond vault PDA\nconst [bondVaultPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"bond_vault\"), protocolPda.toBuffer()],\n  PROGRAM_ID\n);\n\n// Slash pool PDA\nconst [slashPoolPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"slash_pool\"), protocolPda.toBuffer()],\n  PROGRAM_ID\n);\n\n// Operator PDA (per authority)\nconst [operatorPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"operator\"), protocolPda.toBuffer(), authorityPubkey.toBuffer()],\n  PROGRAM_ID\n);\n\n// Signing request PDA (request_id as u64 LE bytes)\nconst requestId = new BN(42);\nconst [requestPda] = PublicKey.findProgramAddressSync(\n  [\n    Buffer.from(\"request\"),\n    protocolPda.toBuffer(),\n    requestId.toArrayLike(Buffer, \"le\", 8),\n  ],\n  PROGRAM_ID\n);\n\n// Partial signature PDA\nconst [partialPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"partial\"), requestPda.toBuffer(), operatorPda.toBuffer()],\n  PROGRAM_ID\n);\n```\n\n## Step 1: Initialize the Protocol\n\n`initialize` is called once by the admin. It creates the `Protocol` singleton, the `bond_vault`, and the `slash_pool` as Token-2022 accounts owned by the protocol PDA.\n\n### Parameter Constraints (enforced on-chain)\n\n| Parameter | Type | On-chain constraint |\n|---|---|---|\n| `threshold_bps` | `u16` | `1 ≤ threshold_bps ≤ 10_000` (`BPS_DENOMINATOR`) |\n| `min_bond` | `u64` | `> 0` |\n| `unbonding_slots` | `u64` | No upper bound; choose carefully (see below) |\n| `request_fee` | `u64` | Lamports; `0` is valid (no fee) |\n| `max_validity_slots` | `u64` | `1 ≤ max_validity_slots ≤ 432_000` (`MAX_VALIDITY_SLOTS_CEILING`) |\n| `lst_price_feed` | `Pubkey` | Must match the Pyth account provided in the same tx |\n\n`MAX_VALIDITY_SLOTS_CEILING = 432_000` slots corresponds to approximately 48 hours at Solana's 400 ms per-slot target.\n\n```typescript\nawait program.methods\n  .initialize(\n    6_700,                       // threshold_bps: 67% of total bonded weight\n    1_000_000_000n,              // min_bond: 1 LST (assuming 9 decimals)\n    201_600n,                    // unbonding_slots: ~1 week (604_800s / 3)\n    5_000_000n,                  // request_fee: 0.005 SOL in lamports\n    14_400n,                     // max_validity_slots: ~96 min window cap\n    PYTH_LST_PRICE_FEED_PUBKEY\n  )\n  .accounts({\n    admin: adminKeypair.publicKey,\n    protocol: protocolPda,\n    bondMint: lstMintPubkey,\n    bondVault: bondVaultPda,\n    slashPool: slashPoolPda,\n    tokenProgram: TOKEN_2022_PROGRAM_ID,\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([adminKeypair])\n  .rpc();\n```\n\nAfter initialization the `Protocol` account is 256 bytes on-chain (248 bytes INIT_SPACE + 8-byte Anchor discriminator). The key fields you will read back frequently:\n\n```rust\npub struct Protocol {\n    pub admin: Pubkey,             // Current admin\n    pub pending_admin: Pubkey,     // Two-step handover target\n    pub bond_mint: Pubkey,\n    pub bond_vault: Pubkey,\n    pub slash_pool: Pubkey,\n    pub lst_price_feed: Pubkey,\n    pub threshold_bps: u16,        // e.g. 6_700 = 67%\n    pub min_bond: u64,\n    pub unbonding_slots: u64,\n    pub request_fee: u64,\n    pub max_validity_slots: u64,\n    pub operator_count: u32,       // Live active operator count\n    pub total_bonded: u64,         // Sum of active stake_weight\n    pub request_nonce: u64,        // Monotonic; seeds next request PDA\n    pub paused: bool,\n    pub bump: u8,\n}\n```\n\n## Step 2: Register an Operator\n\nOperators are the economic security layer. Each operator:\n\n1. Holds a Token-2022 LST token account funded with at least `protocol.min_bond` tokens.\n2. Has a 33-byte compressed group public key — the FROST public-share identifier for Ed25519 requests, or the GG20 participant key for secp256k1 requests.\n3. Calls `register_operator`, which pulls the bond into the `bond_vault` via `transfer_checked` and records `stake_weight` using the oracle-priced bond value.\n\n```typescript\n// group_pubkey is a 33-byte compressed point (FROST or GG20 share identifier)\nconst groupPubkey = Buffer.alloc(33);\n// ... populate from your engine/kobe / engine/kobe-ecdsa key generation\n\nawait program.methods\n  .registerOperator(\n    Array.from(groupPubkey),     // [u8; 33]\n    new BN(1_000_000_000)        // bond_amount in LST base units\n  )\n  .accounts({\n    authority: operatorKeypair.publicKey,\n    protocol: protocolPda,\n    operator: operatorPda,\n    operatorTokenAccount: operatorLstAta,\n    bondVault: bondVaultPda,\n    bondMint: lstMintPubkey,\n    lstPriceFeed: PYTH_LST_PRICE_FEED_PUBKEY,\n    tokenProgram: TOKEN_2022_PROGRAM_ID,\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([operatorKeypair])\n  .rpc();\n```\n\nOn success the `OperatorRegistered` event is emitted:\n\n```rust\nemit!(OperatorRegistered {\n    operator: operator.key(),\n    authority: operator.authority,\n    stake_weight,\n});\n```\n\nThe `Operator` account is 151 bytes on-chain (143 bytes INIT_SPACE + 8-byte discriminator):\n\n```rust\npub struct Operator {\n    pub protocol: Pubkey,\n    pub authority: Pubkey,\n    pub group_pubkey: [u8; 33],    // Compressed group / share pubkey\n    pub bonded_amount: u64,        // Raw LST units in vault\n    pub stake_weight: u64,         // Oracle-valued economic weight\n    pub partials_submitted: u64,   // Lifetime partial count\n    pub slash_count: u32,\n    pub jailed: bool,              // true = cannot sign new requests\n    pub unbonding_at: u64,         // 0 while actively bonded\n    pub joined_slot: u64,\n    pub bump: u8,\n}\n```\n\n### Operator Lifecycle\n\n```mermaid\nstateDiagram-v2\n    [*] --> Active: register_operator\\n(bond_amount ≥ min_bond)\n    Active --> Unbonding: begin_unbonding()\\njailed=true, unbonding_at=slot+unbonding_slots\n    Unbonding --> Withdrawn: withdraw_bond()\\n(clock.slot ≥ unbonding_at)\n    Withdrawn --> [*]: operator account closed\n    Active --> Jailed: slash_operator()\\nbonded_amount < min_bond\n    Jailed --> Unbonding: begin_unbonding()\n```\n\n**Unbonding detail:** `begin_unbonding` sets `operator.unbonding_at = clock.slot + protocol.unbonding_slots` and immediately sets `jailed = true`. The operator's `stake_weight` is subtracted from `protocol.total_bonded` and `operator_count` is decremented at this point — the operator is no longer part of the active signing set even while bonds are still locked. `withdraw_bond` will only execute when `clock.slot >= operator.unbonding_at`.\n\n## Step 3: Create a Signing Request\n\nA user posts a cross-VM signing intent by calling `create_signing_request`. The program:\n\n1. Verifies the protocol is not paused and at least one active operator exists.\n2. Charges `protocol.request_fee` lamports to the protocol account via `system_program::transfer`.\n3. Snapshots `required_stake_weight = protocol.total_bonded * threshold_bps / 10_000` at creation time.\n4. Opens a `SigningRequest` PDA seeded with `[b\"request\", protocol, request_nonce_le]`.\n\n### Parameter Constraints\n\n| Parameter | Type | Constraint |\n|---|---|---|\n| `scheme` | `SignatureScheme` | `FrostEd25519` or `Gg20Secp256k1` |\n| `target_vm` | `TargetVm` | `Svm`, `Evm`, `Tron`, `Cosmos`, or `Bitcoin` |\n| `target_chain_id` | `u64` | Any; semantics are VM-family specific |\n| `message_hash` | `[u8; 32]` | Must have at least one non-zero byte |\n| `threshold` | `u16` | `1 ≤ threshold ≤ protocol.operator_count` |\n| `validity_slots` | `u64` | `1 ≤ validity_slots ≤ protocol.max_validity_slots` |\n\n```typescript\n// Sign an EVM transaction hash for Ethereum mainnet (chain id 1)\nconst messageHash = Buffer.from(\n  \"a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2\",\n  \"hex\"\n);\n\nawait program.methods\n  .createSigningRequest(\n    { gg20Secp256k1: {} },   // scheme: GG20 for EVM\n    { evm: {} },             // target_vm\n    new BN(1),               // target_chain_id: Ethereum mainnet\n    Array.from(messageHash), // message_hash [u8; 32]\n    3,                       // threshold: require 3 partial sigs minimum\n    3_600n                   // validity_slots: ~24 min window\n  )\n  .accounts({\n    requester: userKeypair.publicKey,\n    protocol: protocolPda,\n    request: requestPda,             // derived with current request_nonce\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([userKeypair])\n  .rpc();\n```\n\nThe `SigningRequest` account is 232 bytes on-chain (224 bytes INIT_SPACE + 8-byte discriminator):\n\n```rust\npub struct SigningRequest {\n    pub protocol: Pubkey,\n    pub requester: Pubkey,\n    pub request_id: u64,\n    pub scheme: SignatureScheme,\n    pub target_vm: TargetVm,\n    pub target_chain_id: u64,\n    pub message_hash: [u8; 32],\n    pub threshold: u16,\n    pub partials_collected: u16,\n    pub stake_weight_collected: u64,\n    pub required_stake_weight: u64,  // snapshotted at creation\n    pub created_slot: u64,\n    pub expiry_slot: u64,            // created_slot + validity_slots\n    pub status: RequestStatus,       // Pending | Aggregated | Cancelled | Expired\n    pub aggregate_sig: [u8; 64],     // published on finalization\n    pub bump: u8,\n}\n```\n\n### Scheme–VM Mapping\n\nThe program enforces scheme branching per VM family. Use the following as your guide when constructing requests:\n\n| `TargetVm` | Correct `SignatureScheme` | Off-chain signing library |\n|---|---|---|\n| `Svm` | `FrostEd25519` | `engine/kobe` |\n| `Evm` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n| `Tron` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n| `Cosmos` | `FrostEd25519` | `engine/kobe` |\n| `Bitcoin` | `Gg20Secp256k1` | (BTC secp256k1 path) |\n\nA `PartialSignature` inherits its `scheme` from the parent `SigningRequest` — the caller does not supply it — so there is no scheme-mismatch path to guard against. Choosing the wrong scheme for a VM is a request-construction error, not a partial-submission error.\n\n## Step 4: Operators Submit Partial Signatures\n\nAfter a `SigningRequest` is created and while `clock.slot < request.expiry_slot`, each active operator calls `submit_partial_signature` (instruction present in the program; full body truncated in the published source excerpt). The `PartialSignature` PDA at seeds `[b\"partial\", request, operator]` enforces uniqueness — a second call from the same operator to the same request is rejected because the PDA already exists.\n\nEach `PartialSignature` account is 154 bytes on-chain (146 bytes INIT_SPACE + 8-byte discriminator):\n\n```rust\npub struct PartialSignature {\n    pub request: Pubkey,\n    pub operator: Pubkey,\n    pub scheme: SignatureScheme,\n    pub share: [u8; 64],         // partial-sig share material\n    pub submitted_slot: u64,\n    pub stake_weight: u64,       // operator weight at submission time\n    pub bump: u8,\n}\n```\n\nEach submission is a participation receipt: the program credits `stake_weight_collected` and increments `partials_collected` on the parent `SigningRequest`, but it does not fold the share bytes (summing FROST/GG20 shares is meaningless, and the SVM has no curve arithmetic for the group-combine). Once both the minimum `threshold` partial count and the `required_stake_weight` are met, the off-chain coordinator computes the canonical group signature and posts it via `aggregate_and_emit(aggregate_sig)`, which records it into `request.aggregate_sig` and transitions `status` to `Aggregated`.\n\n## Full Flow Sequence\n\n```mermaid\nsequenceDiagram\n    participant Admin\n    participant Operator\n    participant User\n    participant Unbridge as Unbridge Program\n    participant Relayer\n\n    Admin->>Unbridge: initialize(threshold_bps=6700, min_bond, ...)\\nCreates Protocol PDA + bond_vault + slash_pool\n\n    Operator->>Unbridge: register_operator(group_pubkey, bond_amount)\\nbond_amount transferred to bond_vault\\nOperator PDA created, stake_weight recorded\n\n    User->>Unbridge: create_signing_request(scheme=Gg20Secp256k1, target_vm=Evm,\\ntarget_chain_id=1, message_hash, threshold=3, validity_slots=3600)\\nRequest fee charged; SigningRequest PDA opened\\nrequired_stake_weight snapshotted\n\n    loop Each operator (until threshold met)\n        Operator->>Unbridge: submit_partial_signature(share[64])\\nPartialSignature PDA created (receipt)\\nstake_weight_collected += operator.stake_weight\n    end\n\n    Note over Unbridge: off-chain coordinator runs real FROST/GG20,\\nproduces canonical aggregate_sig\n    Operator->>Unbridge: aggregate_and_emit(aggregate_sig)\\nthresholds + non-zero sig checked\\naggregate_sig recorded, status = Aggregated\n\n    Relayer->>Unbridge: Read request.aggregate_sig (64 bytes)\n    Relayer->>Relayer: broadcast to EVM / destination chain\n```\n\n## Reading Back Accounts\n\n```typescript\n// Fetch and decode Protocol account\nconst protocol = await program.account.protocol.fetch(protocolPda);\nconsole.log(\"threshold_bps:\", protocol.thresholdBps);       // u16\nconsole.log(\"total_bonded:\", protocol.totalBonded.toString()); // u64 as BN\nconsole.log(\"operator_count:\", protocol.operatorCount);      // u32\nconsole.log(\"request_nonce:\", protocol.requestNonce.toString());\nconsole.log(\"paused:\", protocol.paused);\n\n// Fetch Operator\nconst operator = await program.account.operator.fetch(operatorPda);\nconsole.log(\"stake_weight:\", operator.stakeWeight.toString());\nconsole.log(\"jailed:\", operator.jailed);\nconsole.log(\"unbonding_at:\", operator.unbondingAt.toString()); // 0 = active\n\n// Fetch SigningRequest\nconst req = await program.account.signingRequest.fetch(requestPda);\nconsole.log(\"status:\", req.status);         // { pending: {} } | { aggregated: {} } etc.\nconsole.log(\"partials_collected:\", req.partialsCollected);\nconsole.log(\"aggregate_sig:\", Buffer.from(req.aggregateSig).toString(\"hex\"));\n```\n\n## Admin Operations\n\n### Emergency Pause\n\n```typescript\nawait program.methods\n  .pause()\n  .accounts({ admin: adminKeypair.publicKey, protocol: protocolPda })\n  .signers([adminKeypair])\n  .rpc();\n```\n\nWhen `protocol.paused = true`, the instructions `register_operator`, `begin_unbonding`, and `create_signing_request` all fail with `UnbridgeError::ProtocolPaused`. Admin actions (`update_config`, `transfer_admin`, `slash_operator`, `unpause`) are unaffected.\n\n### Two-Step Admin Handover\n\n```typescript\n// Step 1: current admin nominates\nawait program.methods\n  .transferAdmin(newAdminPubkey)\n  .accounts({ admin: adminKeypair.publicKey, protocol: protocolPda })\n  .signers([adminKeypair])\n  .rpc();\n// protocol.pending_admin is now set\n\n// Step 2: nominee accepts\nawait program.methods\n  .acceptAdmin()\n  .accounts({ newAdmin: newAdminKeypair.publicKey, protocol: protocolPda })\n  .signers([newAdminKeypair])\n  .rpc();\n// protocol.admin = newAdminKeypair.publicKey\n// protocol.pending_admin = Pubkey::default()\n```\n\nPassing `Pubkey::default()` as `new_admin` to `transfer_admin` is rejected with `UnbridgeError::InvalidAdminTransfer`. Calling `accept_admin` before `transfer_admin` is set, or with the wrong keypair, produces `UnbridgeError::Unauthorized`.\n\n### Update Config\n\nAll parameters are optional; pass `null` to leave a field unchanged:\n\n```typescript\nawait program.methods\n  .updateConfig(\n    null,         // threshold_bps: no change\n    null,         // min_bond: no change\n    null,         // unbonding_slots: no change\n    10_000_000n,  // request_fee: raise to 0.01 SOL\n    null          // max_validity_slots: no change\n  )\n  .accounts({ admin: adminKeypair.publicKey, protocol: protocolPda })\n  .signers([adminKeypair])\n  .rpc();\n```\n\n## Error Reference\n\nAll errors are from `UnbridgeError`. The discriminator offsets follow Anchor's standard 6000-base encoding.\n\n| Error name | Trigger condition |\n|---|---|\n| `ProtocolPaused` | Any user/operator action while `protocol.paused = true` |\n| `Unauthorized` | `accept_admin` called by non-nominee; or admin instruction from non-admin |\n| `InvalidThreshold` | `threshold_bps < 1` or `> 10_000` on init/update; or request `threshold < 1` or `> operator_count` |\n| `InsufficientBond` | `bond_amount < protocol.min_bond`; or `min_bond == 0` on config |\n| `OperatorJailed` | Operator attempts to submit a partial while `jailed = true` |\n| `AlreadyUnbonding` | `begin_unbonding` called when `operator.unbonding_at != 0` |\n| `NotUnbonding` | `withdraw_bond` called when `operator.unbonding_at == 0` |\n| `UnbondingNotComplete` | `withdraw_bond` called before `clock.slot >= operator.unbonding_at` |\n| `RequestExpired` | `submit_partial` called after `request.expiry_slot` |\n| `RequestNotPending` | Submission to a request not in `RequestStatus::Pending` |\n| `ThresholdNotMet` | Finalization attempted before `required_stake_weight` is met |\n| `RequestAlreadyFinalized` | Finalization attempted on an `Aggregated` / `Cancelled` request |\n| `MalformedPartialSignature` | Share bytes are structurally invalid (checked by off-chain library CPI or program-side validation) |\n| `EmptyMessageHash` | `message_hash` is all zero bytes |\n| `StaleOraclePrice` | Pyth price account has a stale timestamp when `compute_stake_weight` is called |\n| `InvalidOracleAccount` | Oracle account key does not match `protocol.lst_price_feed` |\n| `InvalidVault` | Provided vault or pool account is not the protocol-owned PDA |\n| `InvalidValidityWindow` | `validity_slots < 1` or `> MAX_VALIDITY_SLOTS_CEILING (432_000)` |\n| `NoActiveOperators` | `create_signing_request` called when `protocol.operator_count == 0` |\n| `SlashAmountExceedsBond` | `slash_operator` called with `amount > operator.bonded_amount` |\n| `InvalidAdminTransfer` | `transfer_admin` called with `Pubkey::default()` as the target |\n| `MathOverflow` | Checked arithmetic overflow in any accumulator |\n\n## Edge Cases and Failure Modes\n\n### Request Expiry Race\n\nA request's `expiry_slot` is `created_slot + validity_slots`. If the final partial signature transaction lands one slot after `expiry_slot`, the program rejects it with `RequestExpired`. In practice, set `validity_slots` conservatively — even with Solana's 400 ms target slot time, congestion can delay landing. The hard ceiling is `432_000` slots (~48 h), defined as `MAX_VALIDITY_SLOTS_CEILING`.\n\n### Slashing Below `min_bond`\n\nWhen `slash_operator` reduces `operator.bonded_amount` below `protocol.min_bond`, the program automatically sets `operator.jailed = true` and subtracts the operator's remaining weight from `protocol.total_bonded`, decrementing `operator_count`. This means a slash can silently reduce the active signing set and raise the effective difficulty of future requests (since `required_stake_weight` is snapshotted per-request, already-open requests are unaffected, but new requests will see a smaller `total_bonded`).\n\n### `required_stake_weight` Snapshot Behavior\n\nThe program snapshots `required_stake_weight = total_bonded * threshold_bps / BPS_DENOMINATOR` at `create_signing_request` time. Operators who join or are slashed after the snapshot do not change the target for in-flight requests. This means:\n\n- A request created when `total_bonded` was high cannot be cheaply satisfied by slashing the operator set down after the fact.\n- A request created when `total_bonded` was low requires fewer absolute weight units to finalize even if the set grows later.\n\n### Double-Submit Prevention\n\n`PartialSignature` PDAs are derived from `[b\"partial\", request, operator]`. Anchor's `init` constraint on the PDA creation means a second `submit_partial_signature` call from the same operator on the same request will fail at account creation time — the account already exists. There is no explicit duplicate-check error; the runtime returns a native account-already-in-use error.\n\n### Oracle Staleness\n\n`compute_stake_weight` reads the Pyth price account passed as `lst_price_feed`. If the oracle price is stale (beyond the freshness window), the call reverts with `UnbridgeError::StaleOraclePrice`. This blocks both `register_operator` and `slash_operator` when the oracle feed is down. Monitor the Pyth feed health before triggering operator lifecycle operations during network stress.\n\n### Pausing During Active Requests\n\n`pause` does not cancel in-flight `SigningRequest` accounts. Operators can still call `submit_partial_signature` on existing pending requests while the protocol is paused — only new requests and new operator registrations are blocked. Unpause with `unpause` to restore full operation."
  },
  {
    "slug": "integration",
    "title": "Integration",
    "body": "# Integration\n\nUnbridge's on-chain program (`4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6`) acts as a coordination control plane: it holds economic state, enforces liveness deadlines, and publishes aggregate signatures that an off-chain relayer carries to the destination chain. This page covers every integration touchpoint — PDA derivation, account schemas, instruction parameters, TypeScript examples, and failure modes — drawn directly from `programs/distin/src/lib.rs` and `state.rs`. The program is live on devnet; confirm it with `solana program show 4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6 --url devnet` before you wire up a client.\n\n---\n\n## Prerequisites\n\n| Dependency | Version |\n|---|---|\n| `@coral-xyz/anchor` | `^0.30.x` |\n| `@solana/web3.js` | `^1.95.x` |\n| `@solana/spl-token` | `^0.4.x` (Token-2022 support) |\n| Solana CLI | `>=1.18.26` |\n| Anchor CLI | `0.30.1` |\n\nThe program uses Token-2022 (`anchor_spl::token_interface`) for all LST bond transfers. Make sure you pass the Token-2022 program ID (`TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb`) wherever `token_program` is expected, not the legacy SPL token address.\n\n---\n\n## Program ID & Global Constants\n\n```typescript\n\nconst Unbridge_PROGRAM_ID = new PublicKey(\n  \"4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6\"\n);\n\n// Basis-point denominator for staked-weight threshold checks.\nconst BPS_DENOMINATOR = 10_000n;\n\n// Hard ceiling on how far into the future a request's expiry can be set.\n// Corresponds to ~48 hours at 400 ms per slot.\nconst MAX_VALIDITY_SLOTS_CEILING = 432_000n;\n```\n\n`BPS_DENOMINATOR` and `MAX_VALIDITY_SLOTS_CEILING` are protocol-level compile-time constants in `lib.rs`; they cannot be overridden by `update_config`. The practical implication: any `validity_slots` you pass to `create_signing_request` must satisfy `1 ≤ validity_slots ≤ min(protocol.max_validity_slots, 432_000)`.\n\n---\n\n## PDA Derivation Reference\n\nAll accounts in Unbridge are program-derived. The seed strings are byte-literals defined in `state.rs`; match them exactly (no null terminators, no length prefix).\n\n| Account | Seeds | Notes |\n|---|---|---|\n| `Protocol` | `[b\"protocol\"]` | Singleton — one per deployment |\n| `BondVault` | `[b\"bond_vault\", protocol_pubkey]` | Token-2022 account owned by Protocol PDA |\n| `SlashPool` | `[b\"slash_pool\", protocol_pubkey]` | Token-2022 account owned by Protocol PDA |\n| `Operator` | `[b\"operator\", protocol_pubkey, authority_pubkey]` | One per operator authority |\n| `SigningRequest` | `[b\"request\", protocol_pubkey, request_id_le8]` | `request_id` is u64, little-endian 8 bytes |\n| `PartialSignature` | `[b\"partial\", request_pubkey, operator_pubkey]` | Uniqueness prevents double-submit per (request, operator) pair |\n\n```typescript\n\nfunction deriveProtocol(): [PublicKey, number] {\n  return PublicKey.findProgramAddressSync(\n    [Buffer.from(\"protocol\")],\n    Unbridge_PROGRAM_ID\n  );\n}\n\nfunction deriveBondVault(protocolPda: PublicKey): [PublicKey, number] {\n  return PublicKey.findProgramAddressSync(\n    [Buffer.from(\"bond_vault\"), protocolPda.toBuffer()],\n    Unbridge_PROGRAM_ID\n  );\n}\n\nfunction deriveSlashPool(protocolPda: PublicKey): [PublicKey, number] {\n  return PublicKey.findProgramAddressSync(\n    [Buffer.from(\"slash_pool\"), protocolPda.toBuffer()],\n    Unbridge_PROGRAM_ID\n  );\n}\n\nfunction deriveOperator(\n  protocolPda: PublicKey,\n  authority: PublicKey\n): [PublicKey, number] {\n  return PublicKey.findProgramAddressSync(\n    [Buffer.from(\"operator\"), protocolPda.toBuffer(), authority.toBuffer()],\n    Unbridge_PROGRAM_ID\n  );\n}\n\nfunction deriveSigningRequest(\n  protocolPda: PublicKey,\n  requestId: bigint\n): [PublicKey, number] {\n  const idBuf = Buffer.alloc(8);\n  idBuf.writeBigUInt64LE(requestId);\n  return PublicKey.findProgramAddressSync(\n    [Buffer.from(\"request\"), protocolPda.toBuffer(), idBuf],\n    Unbridge_PROGRAM_ID\n  );\n}\n\nfunction derivePartialSignature(\n  requestPda: PublicKey,\n  operatorPda: PublicKey\n): [PublicKey, number] {\n  return PublicKey.findProgramAddressSync(\n    [Buffer.from(\"partial\"), requestPda.toBuffer(), operatorPda.toBuffer()],\n    Unbridge_PROGRAM_ID\n  );\n}\n```\n\n> **`request_id` endianness.** The PDA seed uses the raw little-endian bytes of the u64 `request_nonce` snapshotted at request creation time. When you fetch `protocol.request_nonce` to pre-compute a request PDA before sending the transaction, use `writeBigUInt64LE`, not `writeBigUInt64BE`.\n\n---\n\n## Account Schema Reference\n\n### `Protocol` — 256 bytes (8 disc + 248 data)\n\n```\nadmin             Pubkey   [32]   current admin authority\npending_admin     Pubkey   [32]   nominated successor (Pubkey::default if unset)\nbond_mint         Pubkey   [32]   Token-2022 LST mint accepted as collateral\nbond_vault        Pubkey   [32]   protocol-owned vault holding active bonds\nslash_pool        Pubkey   [32]   protocol-owned pool collecting slashed collateral\nlst_price_feed    Pubkey   [32]   Pyth price account valuing the LST in SOL terms\nthreshold_bps     u16      [2]    fraction of total_bonded required to finalize (bps)\nmin_bond          u64      [8]    minimum bond an operator must post\nunbonding_slots   u64      [8]    delay between begin_unbonding and withdraw_bond\nrequest_fee       u64      [8]    lamports charged per signing request\nmax_validity_slots u64     [8]    ceiling on validity_slots per request\noperator_count    u32      [4]    active operators in the signing set\ntotal_bonded      u64      [8]    sum of active operators' staked economic weight\nrequest_nonce     u64      [8]    monotonic counter; next request PDA seed\npaused            bool     [1]    emergency brake\nbump              u8       [1]    PDA bump\n```\n\n### `Operator` — 151 bytes (8 disc + 143 data)\n\n```\nprotocol          Pubkey   [32]   owning protocol\nauthority         Pubkey   [32]   signer for submissions and lifecycle actions\ngroup_pubkey      [u8;33]  [33]   compressed group pubkey / FROST share identifier\nbonded_amount     u64      [8]    raw LST amount held in the vault\nstake_weight      u64      [8]    SOL-denominated economic weight from oracle\npartials_submitted u64     [8]    lifetime partial signatures submitted\nslash_count       u32      [4]    number of slashing events\njailed            bool     [1]    cannot sign new requests when true\nunbonding_at      u64      [8]    slot unbonding completes; 0 while active\njoined_slot       u64      [8]    slot the operator joined\nbump              u8       [1]    PDA bump\n```\n\nAn operator is \"active\" when `jailed == false && unbonding_at == 0`. Only active operators contribute to `protocol.total_bonded` and `protocol.operator_count`.\n\n### `SigningRequest` — 232 bytes (8 disc + 224 data)\n\n```\nprotocol              Pubkey        [32]\nrequester             Pubkey        [32]   rent refund destination on close\nrequest_id            u64           [8]    monotonic id in PDA seed\nscheme                SignatureScheme [1]  FrostEd25519 | Gg20Secp256k1\ntarget_vm             TargetVm      [1]    Svm | Evm | Tron | Cosmos | Bitcoin\ntarget_chain_id       u64           [8]    EVM chainId, Cosmos index, etc.\nmessage_hash          [u8;32]       [32]   hash of the message to sign\nthreshold             u16           [2]    minimum distinct partial sigs required\npartials_collected    u16           [2]    partial sigs collected so far\nstake_weight_collected u64          [8]    economic weight collected so far\nrequired_stake_weight  u64          [8]    bps snapshot of total_bonded at creation\ncreated_slot          u64           [8]\nexpiry_slot           u64           [8]    after this slot no new partials are accepted\nstatus                RequestStatus [1]    Pending | Aggregated | Cancelled | Expired\naggregate_sig         [u8;64]       [64]   published on finalization\nbump                  u8            [1]\n```\n\n### `PartialSignature` — 154 bytes (8 disc + 146 data)\n\n```\nrequest           Pubkey         [32]\noperator          Pubkey         [32]\nscheme            SignatureScheme [1]   must match the parent request scheme\nshare             [u8;64]        [64]   partial-signature material\nsubmitted_slot    u64            [8]\nstake_weight      u64            [8]    credited weight from this contribution\nbump              u8             [1]\n```\n\n---\n\n## Signing Scheme & Target VM Selection\n\nThe program branches on two orthogonal axes: the cryptographic scheme and the destination VM family.\n\n| `TargetVm` | `SignatureScheme` | Off-chain library | Curve |\n|---|---|---|---|\n| `Svm` | `FrostEd25519` | `engine/kobe` | Ed25519 |\n| `Evm` | `Gg20Secp256k1` | `engine/kobe-ecdsa` | secp256k1 |\n| `Tron` | `Gg20Secp256k1` | `engine/kobe-ecdsa` | secp256k1 |\n| `Cosmos` | `FrostEd25519` | `engine/kobe` | Ed25519 |\n| `Bitcoin` | `Gg20Secp256k1` | `engine/kobe-ecdsa` (BTC variant) | secp256k1 |\n\nThe `scheme` field on a `PartialSignature` is copied from the parent `SigningRequest`, not supplied by the submitter, so there is no scheme-mismatch path and no corresponding error. Selecting the wrong scheme for a given VM is a request-construction error — it is fixed at `create_signing_request` time, and neither the program nor the relayer will catch it afterward.\n\n```typescript\nenum SignatureScheme {\n  FrostEd25519 = 0,\n  Gg20Secp256k1 = 1,\n}\n\nenum TargetVm {\n  Svm = 0,\n  Evm = 1,\n  Tron = 2,\n  Cosmos = 3,\n  Bitcoin = 4,\n}\n\nfunction inferScheme(vm: TargetVm): SignatureScheme {\n  switch (vm) {\n    case TargetVm.Svm:\n    case TargetVm.Cosmos:\n      return SignatureScheme.FrostEd25519;\n    case TargetVm.Evm:\n    case TargetVm.Tron:\n    case TargetVm.Bitcoin:\n      return SignatureScheme.Gg20Secp256k1;\n  }\n}\n```\n\n---\n\n## End-to-End Flow\n\n```mermaid\nsequenceDiagram\n    participant U as User / Requester\n    participant S as Solana Program\n    participant O1 as Operator 1\n    participant O2 as Operator 2\n    participant O3 as Operator 3\n    participant R as Off-chain Relayer\n    participant D as Destination Chain\n\n    U->>S: create_signing_request(scheme, target_vm, chain_id, msg_hash, threshold, validity_slots)\n    Note over S: Charges request_fee lamports.<br/>Snapshots required_stake_weight.<br/>status = Pending\n\n    par Operators watch for Pending requests\n        O1->>S: submit_partial_signature(share[64])\n        O2->>S: submit_partial_signature(share[64])\n        O3->>S: submit_partial_signature(share[64])\n    end\n\n    Note over S: After each submit (a receipt):<br/>stake_weight_collected += op.stake_weight<br/>partials_collected += 1<br/>emit PartialSignatureSubmitted\n\n    Note over O1,S: off-chain coordinator runs real FROST/GG20<br/>and produces the canonical aggregate_sig\n    O1->>S: aggregate_and_emit(aggregate_sig)<br/>when stake_weight_collected ≥ required_stake_weight<br/>AND partials_collected ≥ threshold AND sig non-zero<br/>status = Aggregated, emit AggregateSignatureEmitted\n\n    R->>S: poll / subscribe to AggregateSignatureEmitted\n    R->>D: broadcast tx with aggregate_sig on destination chain\n```\n\nThe Solana program is responsible for the **coordination layer only**: fee collection, economic-weight accounting, partial-signature receipts, and threshold enforcement. The actual cryptographic group-combine (`engine/kobe` for FROST, `engine/kobe-ecdsa` for GG20) is done by the off-chain coordinator, which posts the canonical signature into `aggregate_and_emit`; the relayer only broadcasts.\n\n---\n\n## TypeScript Integration\n\n### Project Setup\n\n```bash\nanchor build\nanchor idl parse --file programs/distin/src/lib.rs -o target/idl/distin.json\n```\n\n```typescript\nimport {\n  Connection,\n  Keypair,\n  PublicKey,\n  SystemProgram,\n  LAMPORTS_PER_SOL,\n} from \"@solana/web3.js\";\nimport {\n  TOKEN_2022_PROGRAM_ID,\n  getAssociatedTokenAddressSync,\n} from \"@solana/spl-token\";\n\nconst connection = new Connection(\"https://api.devnet.solana.com\", \"confirmed\");\nconst wallet = /* your Anchor wallet */;\nconst provider = new AnchorProvider(connection, wallet, {\n  commitment: \"confirmed\",\n});\nconst program = new Program(idl as any, provider);\n\nconst Unbridge_PROGRAM_ID = new PublicKey(\n  \"4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6\"\n);\nconst TOKEN_2022 = TOKEN_2022_PROGRAM_ID;\n```\n\n### Reading Protocol State\n\n```typescript\nasync function fetchProtocol() {\n  const [protocolPda] = deriveProtocol();\n  const protocol = await program.account.protocol.fetch(protocolPda);\n\n  console.log({\n    admin: protocol.admin.toBase58(),\n    thresholdBps: protocol.thresholdBps,          // e.g. 6667 for ~2/3\n    minBond: protocol.minBond.toString(),\n    unbondingSlots: protocol.unbondingSlots.toString(),\n    requestFee: protocol.requestFee.toString(),    // lamports\n    maxValiditySlots: protocol.maxValiditySlots.toString(),\n    operatorCount: protocol.operatorCount,\n    totalBonded: protocol.totalBonded.toString(),\n    requestNonce: protocol.requestNonce.toString(),\n    paused: protocol.paused,\n  });\n\n  return protocol;\n}\n```\n\n### Creating a Signing Request (User Path)\n\n```typescript\nasync function createSigningRequest({\n  targetVm,\n  targetChainId,\n  messageHash,   // Uint8Array of exactly 32 bytes\n  threshold,     // number of distinct partials required; must be ≤ operator_count\n  validitySlots, // 1 – max_validity_slots (≤ 432_000)\n}: {\n  targetVm: TargetVm;\n  targetChainId: bigint;\n  messageHash: Uint8Array;\n  threshold: number;\n  validitySlots: bigint;\n}) {\n  const [protocolPda] = deriveProtocol();\n  const protocol = await program.account.protocol.fetch(protocolPda);\n\n  // The request PDA seeds use the CURRENT nonce before the instruction increments it.\n  const requestId = BigInt(protocol.requestNonce.toString());\n  const [requestPda] = deriveSigningRequest(protocolPda, requestId);\n\n  const scheme = inferScheme(targetVm);\n\n  const tx = await program.methods\n    .createSigningRequest(\n      { [SignatureScheme[scheme]]: {} },        // Anchor enum variant\n      { [TargetVm[targetVm]]: {} },\n      new BN(targetChainId.toString()),\n      Array.from(messageHash),                  // [u8; 32] as number[]\n      threshold,\n      new BN(validitySlots.toString())\n    )\n    .accounts({\n      protocol: protocolPda,\n      request: requestPda,\n      requester: wallet.publicKey,\n      systemProgram: SystemProgram.programId,\n    })\n    .rpc();\n\n  console.log(\"SigningRequest created:\", requestPda.toBase58(), \"tx:\", tx);\n  return { requestPda, requestId };\n}\n```\n\n**Fee note.** `protocol.request_fee` lamports are transferred from `requester` to the `protocol` PDA via `system_program::transfer` inside the instruction. Ensure the requester account holds `request_fee + transaction_fee` lamports before calling. If `request_fee == 0` the transfer CPI is skipped entirely.\n\n**`message_hash` constraint.** The program rejects an all-zero `[u8; 32]` with `EmptyMessageHash`. Always pass the actual hash of your cross-chain payload.\n\n### Registering as a Signing Operator\n\n```typescript\nasync function registerOperator({\n  bondAmount,    // u64 in LST base units; must be >= protocol.min_bond\n  groupPubkey,   // Uint8Array of exactly 33 bytes (compressed pubkey / FROST share id)\n}: {\n  bondAmount: bigint;\n  groupPubkey: Uint8Array;\n}) {\n  const authority = wallet.publicKey;\n  const [protocolPda] = deriveProtocol();\n  const protocol = await program.account.protocol.fetch(protocolPda);\n\n  const [operatorPda] = deriveOperator(protocolPda, authority);\n  const [bondVaultPda] = deriveBondVault(protocolPda);\n\n  // Operator must hold LST in a Token-2022 ATA.\n  const operatorTokenAccount = getAssociatedTokenAddressSync(\n    protocol.bondMint,\n    authority,\n    false,\n    TOKEN_2022\n  );\n\n  const tx = await program.methods\n    .registerOperator(\n      Array.from(groupPubkey),   // [u8; 33] as number[]\n      new BN(bondAmount.toString())\n    )\n    .accounts({\n      protocol: protocolPda,\n      operator: operatorPda,\n      authority,\n      operatorTokenAccount,\n      bondVault: bondVaultPda,\n      bondMint: protocol.bondMint,\n      lstPriceFeed: protocol.lstPriceFeed,\n      tokenProgram: TOKEN_2022,\n    })\n    .rpc();\n\n  console.log(\"Operator registered:\", operatorPda.toBase58(), \"tx:\", tx);\n  return operatorPda;\n}\n```\n\n**`group_pubkey` format.** This is a 33-byte compressed public key / FROST public-share identifier that the `engine/kobe` / `engine/kobe-ecdsa` libraries use to verify partial-signature shares. Pass the correct compressed point for your signing key material; the program stores it as opaque bytes and does not validate the curve point on-chain.\n\n**`stake_weight` derivation.** The program calls `compute_stake_weight(&lst_price_feed, bond_amount)` using the Pyth price account set at `initialize`. The resulting `stake_weight` is stored in the `Operator` account and is what gets accumulated in `required_stake_weight` checks. If the oracle price is stale the instruction fails with `StaleOraclePrice`.\n\n### Operator Lifecycle — Unbonding and Withdrawal\n\n```typescript\nasync function beginUnbonding() {\n  const [protocolPda] = deriveProtocol();\n  const [operatorPda] = deriveOperator(protocolPda, wallet.publicKey);\n\n  const tx = await program.methods\n    .beginUnbonding()\n    .accounts({\n      protocol: protocolPda,\n      operator: operatorPda,\n      authority: wallet.publicKey,\n    })\n    .rpc();\n\n  const operator = await program.account.operator.fetch(operatorPda);\n  console.log(\n    \"Unbonding until slot:\",\n    operator.unbondingAt.toString(),\n    \"tx:\", tx\n  );\n}\n\nasync function withdrawBond() {\n  const [protocolPda] = deriveProtocol();\n  const protocol = await program.account.protocol.fetch(protocolPda);\n  const [operatorPda] = deriveOperator(protocolPda, wallet.publicKey);\n  const [bondVaultPda] = deriveBondVault(protocolPda);\n\n  const operator = await program.account.operator.fetch(operatorPda);\n  const current = await connection.getSlot();\n\n  if (current < operator.unbondingAt.toNumber()) {\n    const remaining = operator.unbondingAt.toNumber() - current;\n    throw new Error(`Unbonding incomplete. ~${remaining * 0.4}s remaining.`);\n  }\n\n  const operatorTokenAccount = getAssociatedTokenAddressSync(\n    protocol.bondMint,\n    wallet.publicKey,\n    false,\n    TOKEN_2022\n  );\n\n  const tx = await program.methods\n    .withdrawBond()\n    .accounts({\n      protocol: protocolPda,\n      operator: operatorPda,\n      authority: wallet.publicKey,\n      operatorTokenAccount,\n      bondVault: bondVaultPda,\n      bondMint: protocol.bondMint,\n      tokenProgram: TOKEN_2022,\n    })\n    .rpc();\n\n  console.log(\"Bond withdrawn, tx:\", tx);\n}\n```\n\nThe program uses `signer_seeds` derived from `[PROTOCOL_SEED, &[protocol.bump]]` when transferring from the vault back to the operator — the Protocol PDA is the vault authority, not the admin.\n\n### Watching for Aggregation (Off-chain Relayer)\n\n```typescript\nasync function watchRequest(requestPda: PublicKey): Promise<Uint8Array> {\n  return new Promise((resolve, reject) => {\n    const sub = connection.onAccountChange(\n      requestPda,\n      async (accountInfo) => {\n        const request = program.coder.accounts.decode(\n          \"signingRequest\",\n          accountInfo.data\n        );\n\n        if (request.status.aggregated !== undefined) {\n          connection.removeAccountChangeListener(sub);\n          // aggregate_sig is a [u8; 64] stored in the account.\n          resolve(new Uint8Array(request.aggregateSig));\n        }\n\n        if (\n          request.status.expired !== undefined ||\n          request.status.cancelled !== undefined\n        ) {\n          connection.removeAccountChangeListener(sub);\n          reject(new Error(`Request ended with status: ${JSON.stringify(request.status)}`));\n        }\n      },\n      \"confirmed\"\n    );\n  });\n}\n```\n\nOnce `aggregate_sig` is published (status transitions to `Aggregated`), it is already the final chain-native group signature — the off-chain coordinator computed it before calling `aggregate_and_emit`. Your relayer reads the 64 bytes, assembles the destination transaction (using the per-chain envelope in `engine/kobe-ecdsa` for secp256k1 chains), and broadcasts it.\n\n---\n\n## Instruction Reference\n\n### `initialize`\n\nBootstrap the protocol singleton. Called once by the deployer.\n\n```rust\npub fn initialize(\n    ctx: Context<Initialize>,\n    threshold_bps: u16,       // 1–10_000\n    min_bond: u64,            // > 0\n    unbonding_slots: u64,\n    request_fee: u64,         // lamports; 0 = free\n    max_validity_slots: u64,  // 1–432_000\n    lst_price_feed: Pubkey,   // Pyth price account\n) -> Result<()>\n```\n\n### `update_config`\n\nAdmin-only. All parameters are `Option<T>` — omit to leave unchanged.\n\n```rust\npub fn update_config(\n    ctx: Context<AdminConfig>,\n    threshold_bps: Option<u16>,\n    min_bond: Option<u64>,\n    unbonding_slots: Option<u64>,\n    request_fee: Option<u64>,\n    max_validity_slots: Option<u64>,\n) -> Result<()>\n```\n\n### `transfer_admin` / `accept_admin`\n\nTwo-step admin handover. `transfer_admin` sets `protocol.pending_admin`; `accept_admin` must be called by the nominee (enforced via `require_keys_eq!(protocol.pending_admin, ctx.accounts.new_admin.key())`). Passing `Pubkey::default()` to `transfer_admin` fails with `InvalidAdminTransfer`.\n\n### `pause` / `unpause`\n\nEmergency brake. When `protocol.paused == true`, `register_operator`, `begin_unbonding`, and `create_signing_request` all fail with `ProtocolPaused`. Admin-only.\n\n### `register_operator`\n\n```rust\npub fn register_operator(\n    ctx: Context<RegisterOperator>,\n    group_pubkey: [u8; 33],\n    bond_amount: u64,         // must be >= protocol.min_bond\n) -> Result<()>\n```\n\nEmits `OperatorRegistered { operator, authority, stake_weight }`.\n\n### `begin_unbonding`\n\nMarks the operator `jailed = true`, sets `unbonding_at = current_slot + unbonding_slots`, and decrements `protocol.total_bonded` and `protocol.operator_count`. After this call the operator is excluded from new request coverage.\n\n### `withdraw_bond`\n\nFails with `UnbondingNotComplete` if `Clock::get().slot < operator.unbonding_at`. Transfers `operator.bonded_amount` LST back to the operator's Token-2022 ATA and closes the operator account (returning rent).\n\n### `slash_operator`\n\nAdmin-only. Moves `amount` LST from the bond vault to the slash pool. Post-slash, if `operator.bonded_amount < protocol.min_bond` the operator is automatically jailed. Emits `OperatorSlashed { operator, amount, reason }`. `reason` is an opaque `u8` whose semantics are application-defined (equivocation, invalid share, liveness fault, etc.).\n\n### `create_signing_request`\n\n```rust\npub fn create_signing_request(\n    ctx: Context<CreateSigningRequest>,\n    scheme: SignatureScheme,\n    target_vm: TargetVm,\n    target_chain_id: u64,\n    message_hash: [u8; 32],   // non-zero\n    threshold: u16,           // 1 <= threshold <= operator_count\n    validity_slots: u64,      // 1 <= validity_slots <= max_validity_slots\n) -> Result<()>\n```\n\nInternally snapshots:\n```\nrequired_stake_weight = total_bonded * threshold_bps / BPS_DENOMINATOR\nexpiry_slot           = current_slot + validity_slots\nrequest_id            = protocol.request_nonce  (then nonce++)\n```\n\n---\n\n## Threshold Math\n\nThe program uses two independent threshold conditions that both must be satisfied before a request can be finalized:\n\n1. **Count threshold** (`partials_collected >= request.threshold`): minimum number of distinct operator submissions.\n2. **Weight threshold** (`stake_weight_collected >= required_stake_weight`): economic security expressed in stake weight units.\n\n`required_stake_weight` is computed at creation time as a snapshot of `total_bonded * threshold_bps / BPS_DENOMINATOR`. If operators are slashed or begin unbonding between request creation and finalization, the absolute weight target remains fixed — it does not recompute. This means:\n\n- A request created when 10 operators hold 1000 weight each, at `threshold_bps = 6667`, sets `required_stake_weight = 6667`. If 4 operators are later slashed to dust, the remaining operators may still accumulate 6667 weight and finalize legitimately.\n- Conversely, a spike in `total_bonded` after creation does not tighten the bar for an existing request.\n\n```\nrequired_stake_weight = (total_bonded × threshold_bps) / 10_000\n\nExample: total_bonded = 15_000, threshold_bps = 6_667\n         required     = (15_000 × 6_667) / 10_000 = 10_000 (two-thirds)\n```\n\n---\n\n## Error Reference\n\nEvery error is declared in `programs/distin/src/errors.rs` with a 6-digit Anchor error code (base offset `6000`).\n\n| Error name | When it fires |\n|---|---|\n| `ProtocolPaused` | `register_operator`, `begin_unbonding`, or `create_signing_request` called while `paused == true` |\n| `Unauthorized` | `accept_admin` called by a non-nominee, or protected instruction called by non-admin |\n| `InvalidThreshold` | `threshold_bps` outside `[1, 10_000]`; or `threshold` param to `create_signing_request` is 0 or exceeds `operator_count` |\n| `InsufficientBond` | `bond_amount < protocol.min_bond`, or `min_bond` updated to 0 |\n| `OperatorJailed` | Operator with `jailed == true` attempts to submit a partial signature |\n| `AlreadyUnbonding` | `begin_unbonding` called when `operator.unbonding_at != 0` |\n| `NotUnbonding` | `withdraw_bond` called when `operator.unbonding_at == 0` |\n| `UnbondingNotComplete` | `Clock::slot < operator.unbonding_at` at `withdraw_bond` time |\n| `RequestExpired` | Partial submitted after `request.expiry_slot` |\n| `RequestNotPending` | Partial submitted to a request with status != `Pending` |\n| `ThresholdNotMet` | Finalization attempted before both weight and count thresholds are met |\n| `RequestAlreadyFinalized` | Finalization attempted on an `Aggregated` request |\n| `MalformedPartialSignature` | Share bytes fail basic sanity check |\n| `EmptyMessageHash` | All 32 bytes of `message_hash` are zero |\n| `StaleOraclePrice` | Pyth price account is past its max confidence window |\n| `InvalidOracleAccount` | `lst_price_feed` in the account does not match `protocol.lst_price_feed` |\n| `InvalidVault` | Bond vault or slash pool account key does not match the derived PDA |\n| `InvalidValidityWindow` | `validity_slots` is 0 or > `MAX_VALIDITY_SLOTS_CEILING` (432_000) |\n| `NoActiveOperators` | `create_signing_request` with `protocol.operator_count == 0` |\n| `SlashAmountExceedsBond` | `amount > operator.bonded_amount` in `slash_operator` |\n| `InvalidAdminTransfer` | `transfer_admin(Pubkey::default())` |\n| `MathOverflow` | Checked arithmetic overflow in weight or nonce accounting |\n\n### Handling Errors in TypeScript\n\n```typescript\n\ntry {\n  await createSigningRequest({ ... });\n} catch (err) {\n  if (err instanceof AnchorError) {\n    switch (err.error.errorCode.code) {\n      case \"ProtocolPaused\":\n        console.error(\"Protocol is paused. Contact admin.\");\n        break;\n      case \"NoActiveOperators\":\n        console.error(\"No operators are currently bonded.\");\n        break;\n      case \"InvalidValidityWindow\":\n        console.error(\n          `validity_slots must be 1–${MAX_VALIDITY_SLOTS_CEILING}.`\n        );\n        break;\n      case \"EmptyMessageHash\":\n        console.error(\"message_hash must be non-zero.\");\n        break;\n      default:\n        console.error(\"Unbridge error:\", err.error.errorCode.code, err.error.errorMessage);\n    }\n  }\n}\n```\n\n---\n\n## Edge Cases and Common Pitfalls\n\n### Slot expiry races\n\nEvery `SigningRequest` has a hard `expiry_slot = created_slot + validity_slots`. Solana's cluster advances roughly one slot every 400 ms, but slot rate fluctuates under load. Operator relayers should:\n\n1. Subscribe to the request account immediately after `create_signing_request` confirms.\n2. Monitor `Clock::slot` against `request.expiry_slot`; do not attempt `submit_partial` if the current slot is at or past expiry.\n3. Factor in ~2–4 slots of confirmation latency when setting `validity_slots`. A minimum of 150 slots (~60s) is a reasonable floor for networks of fewer than 20 operators; larger networks converge faster.\n\n### Pre-computing request PDAs\n\nThe `request_id` used in the PDA seed equals `protocol.request_nonce` at the moment `create_signing_request` executes. If two requests land in the same block, the nonce is incremented atomically per instruction. Read `request_nonce` immediately before constructing the transaction; do not cache it across multiple requests without re-fetching.\n\n### Double-submit prevention\n\n`PartialSignature` PDAs are seeded `[b\"partial\", request, operator]`. Because PDA derivation is deterministic, a second `submit_partial_signature` from the same operator for the same request will attempt to `init` an account at an address that already exists and fail with an Anchor account-already-initialized error, not a custom Unbridge error. This is by design — there is no explicit \"already submitted\" error.\n\n### Operator jailing on slash\n\n`slash_operator` can reduce `bonded_amount` below `min_bond`, which triggers automatic jailing (`operator.jailed = true`). A jailed operator's `stake_weight` is still subtracted from `protocol.total_bonded` inside the same instruction. If this causes `total_bonded` to drop so low that no future set of partial submissions can reach `required_stake_weight` for an in-flight request, that request will expire — there is no on-chain mechanism to re-open it. Monitor `total_bonded` relative to open request thresholds when running the admin slash path.\n\n### `begin_unbonding` is irreversible\n\nOnce `begin_unbonding` is called, `operator.unbonding_at` is set and `operator.jailed = true`. There is no `cancel_unbonding` instruction in the current program. The operator must wait out the full `unbonding_slots` delay and call `withdraw_bond`, then re-register with a new `register_operator` call if they wish to rejoin the set.\n\n### Oracle staleness\n\n`register_operator` and `slash_operator` both call `compute_stake_weight` against the Pyth price feed. If the feed is stale at the time of the call the transaction fails with `StaleOraclePrice`. This is a liveness concern for operators registering during periods of low oracle activity. Retry after the feed refreshes — typically within one Pythnet heartbeat interval (~400 ms).\n\n### Token-2022 transfer hook compatibility\n\nThe program uses `transfer_checked` from `anchor_spl::token_interface`, which is compatible with Token-2022 transfer hooks. However, if the LST mint has a transfer hook enabled, the hook's extra accounts must be appended to the instruction's remaining accounts. Anchor does not do this automatically; you must resolve and append them using the `@solana/spl-token` `resolveExtraAccountMetas` helper before sending.\n\n### `paused` flag and in-flight requests\n\nThe `paused` flag blocks `create_signing_request` and `register_operator` but does not prevent operators from submitting partials to requests that were created before the pause. In-flight requests continue accumulating partial signatures during a pause; the pause only stops new requests from entering the pipeline.\n\n---\n\n## Monitoring Events\n\nThe program emits five named events via `emit!()`: `OperatorRegistered`, `OperatorSlashed`, `SigningRequestCreated`, `PartialSignatureSubmitted`, and `AggregateSignatureEmitted`. You can drive a relayer entirely off the event stream — `AggregateSignatureEmitted` carries the finalized signature, so polling the account is optional.\n\n```typescript\n// OperatorRegistered\nprogram.addEventListener(\"OperatorRegistered\", (event) => {\n  console.log({\n    operator: event.operator.toBase58(),\n    authority: event.authority.toBase58(),\n    stakeWeight: event.stakeWeight.toString(),\n  });\n});\n\n// OperatorSlashed\nprogram.addEventListener(\"OperatorSlashed\", (event) => {\n  console.log({\n    operator: event.operator.toBase58(),\n    amount: event.amount.toString(),\n    reason: event.reason,  // u8, application-defined\n  });\n});\n\n// PartialSignatureSubmitted — one per recorded receipt\nprogram.addEventListener(\"PartialSignatureSubmitted\", (event) => {\n  console.log({\n    request: event.request.toBase58(),\n    operator: event.operator.toBase58(),\n    partialsCollected: event.partialsCollected,\n    stakeWeightCollected: event.stakeWeightCollected.toString(),\n  });\n});\n\n// AggregateSignatureEmitted — the finalized group signature\nprogram.addEventListener(\"AggregateSignatureEmitted\", (event) => {\n  console.log({\n    request: event.request.toBase58(),\n    requestId: event.requestId.toString(),\n    aggregateSig: Buffer.from(event.aggregateSig).toString(\"hex\"),\n  });\n});\n```\n\n`SigningRequestCreated` fires on `create_signing_request`. Subscribing to these events is the recommended path; account-change subscriptions on the `SigningRequest` PDA remain available as an alternative."
  },
  {
    "slug": "api-reference",
    "title": "API Reference",
    "body": "# Unbridge API Reference\n\n## Program Overview\n\n| Field | Value |\n|---|---|\n| Program ID | `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6` |\n| Framework | Anchor 0.30.1 |\n| Token standard | Token-2022 |\n| Solana program crate | `solana-program = 1.18.26` |\n\nThe Unbridge program is the on-chain control plane for a threshold-signature coordination network. Its responsibilities are strictly accounting, economic security, threshold enforcement, liveness deadlines, and slashing. Cryptographic share verification and the group-combine are delegated to two off-chain signers — `engine/kobe` (Rust, FROST Ed25519) and `engine/kobe-ecdsa` (Go, GG20 secp256k1) — driven by `engine/coordinator`. The program does not compute the aggregate; it records the canonical 64-byte signature the coordinator posts via `aggregate_and_emit` once the thresholds are met and the signature is non-zero.\n\n---\n\n## Global Constants\n\n```rust\npub const BPS_DENOMINATOR: u64 = 10_000;\npub const MAX_VALIDITY_SLOTS_CEILING: u64 = 432_000; // ~48 h at 400 ms/slot\n```\n\n`BPS_DENOMINATOR` is the denominator for every basis-point fraction in the protocol. `threshold_bps` of `10_000` means 100% of total staked weight is required; `6_667` means ⅔.\n\n`MAX_VALIDITY_SLOTS_CEILING` is the absolute upper bound on how long a signing request may remain open before expiry. At Solana's 400 ms per-slot target, 432,000 slots equals 48 hours. Per-request `validity_slots` must satisfy `1 ≤ validity_slots ≤ 432_000`.\n\n---\n\n## PDA Derivation Reference\n\nAll accounts are program-derived. The canonical seeds are constants defined in `state.rs`.\n\n| Account | Seed formula | Derivation sketch |\n|---|---|---|\n| `Protocol` | `[b\"protocol\"]` | singleton; one per deployment |\n| `bond_vault` | `[b\"bond_vault\", protocol_pubkey]` | Token-2022 account owned by protocol PDA |\n| `slash_pool` | `[b\"slash_pool\", protocol_pubkey]` | Token-2022 account owned by protocol PDA |\n| `Operator` | `[b\"operator\", protocol_pubkey, authority_pubkey]` | one per operator authority |\n| `SigningRequest` | `[b\"request\", protocol_pubkey, request_id_le]` | `request_id` is the u64 nonce serialized little-endian |\n| `PartialSignature` | `[b\"partial\", request_pubkey, operator_pubkey]` | uniqueness prevents double-submit per operator per request |\n\n```rust\n// Rust derivation examples\nlet (protocol_pda, bump) = Pubkey::find_program_address(\n    &[b\"protocol\"],\n    &program_id,\n);\n\nlet (operator_pda, bump) = Pubkey::find_program_address(\n    &[b\"operator\", &protocol_pda.to_bytes(), &authority.to_bytes()],\n    &program_id,\n);\n\nlet (request_pda, bump) = Pubkey::find_program_address(\n    &[b\"request\", &protocol_pda.to_bytes(), &request_id.to_le_bytes()],\n    &program_id,\n);\n\nlet (partial_pda, bump) = Pubkey::find_program_address(\n    &[b\"partial\", &request_pda.to_bytes(), &operator_pda.to_bytes()],\n    &program_id,\n);\n```\n\n```typescript\n// TypeScript / @coral-xyz/anchor\n\nconst [protocolPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"protocol\")],\n  Unbridge_PROGRAM_ID\n);\n\nconst [operatorPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"operator\"), protocolPda.toBuffer(), authority.toBuffer()],\n  Unbridge_PROGRAM_ID\n);\n\nconst requestIdBuf = Buffer.alloc(8);\nrequestIdBuf.writeBigUInt64LE(BigInt(requestId));\nconst [requestPda] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"request\"), protocolPda.toBuffer(), requestIdBuf],\n  Unbridge_PROGRAM_ID\n);\n```\n\n---\n\n## Type Definitions\n\n### `SignatureScheme`\n\nBranched per destination VM family. Must match the target chain's native signature format.\n\n| Variant | Value | Used for |\n|---|---|---|\n| `FrostEd25519` | `0` | SVM, Aptos, Sui — FROST Schnorr over Ed25519 |\n| `Gg20Secp256k1` | `1` | EVM, BTC, Tron — GG20 threshold ECDSA over secp256k1 |\n\nThe scheme is set on the `SigningRequest` at `create_signing_request`. A `PartialSignature` carries that scheme by copy rather than accepting a caller-supplied one, so there is no scheme-mismatch check on submission (no untrusted-scheme input path).\n\n### `TargetVm`\n\nIdentifies the destination chain family. Used by off-chain relayers to select the broadcast adapter.\n\n| Variant | Value | Destination |\n|---|---|---|\n| `Svm` | `0` | Solana / SVM-compatible |\n| `Evm` | `1` | EVM-compatible (Ethereum, Arbitrum, Base, …) |\n| `Tron` | `2` | Tron (secp256k1, same curve as EVM) |\n| `Cosmos` | `3` | Cosmos SDK chains |\n| `Bitcoin` | `4` | Bitcoin (secp256k1) |\n\n### `RequestStatus`\n\nLifecycle state stored in `SigningRequest.status`.\n\n| Variant | Value | Meaning |\n|---|---|---|\n| `Pending` | `0` | Waiting for partial signatures |\n| `Aggregated` | `1` | Threshold met; `aggregate_sig` is populated |\n| `Cancelled` | `2` | Requester cancelled before expiry |\n| `Expired` | `3` | Passed `expiry_slot` without reaching threshold |\n\n---\n\n## State Accounts\n\n### `Protocol`\n\nSingleton protocol configuration and global accounting.\n\nSeeds: `[b\"protocol\"]`\nSpace: `8 (discriminator) + 248 = 256 bytes`\n\n| Field | Type | Description |\n|---|---|---|\n| `admin` | `Pubkey` | Current admin authority |\n| `pending_admin` | `Pubkey` | Nominated successor; `Pubkey::default()` until set |\n| `bond_mint` | `Pubkey` | Token-2022 LST mint accepted as bonded collateral |\n| `bond_vault` | `Pubkey` | Protocol-owned vault holding active bonds |\n| `slash_pool` | `Pubkey` | Protocol-owned pool collecting slashed collateral |\n| `lst_price_feed` | `Pubkey` | Pyth price account for valuing the LST bond in SOL terms |\n| `threshold_bps` | `u16` | Fraction of total staked weight required to finalize (in bps, denominator `10_000`) |\n| `min_bond` | `u64` | Minimum bond an operator must post to join the signing set (in LST raw units) |\n| `unbonding_slots` | `u64` | Slots between `begin_unbonding` and `withdraw_bond` |\n| `request_fee` | `u64` | Lamports charged per `create_signing_request` (transferred to protocol PDA) |\n| `max_validity_slots` | `u64` | Upper bound on a request's validity window; capped at `432_000` |\n| `operator_count` | `u32` | Number of operators currently in the active signing set |\n| `total_bonded` | `u64` | Sum of active operators' staked economic weight |\n| `request_nonce` | `u64` | Monotonic counter seeding `SigningRequest` PDAs |\n| `paused` | `bool` | Emergency pause flag; blocks all user/operator state transitions |\n| `bump` | `u8` | PDA canonical bump |\n\n### `Operator`\n\nA bonded signing operator record.\n\nSeeds: `[b\"operator\", protocol_pubkey, authority_pubkey]`\nSpace: `8 + 143 = 151 bytes`\n\n| Field | Type | Description |\n|---|---|---|\n| `protocol` | `Pubkey` | Owning protocol PDA |\n| `authority` | `Pubkey` | Operator authority (required signer for submissions and lifecycle actions) |\n| `group_pubkey` | `[u8; 33]` | Compressed group public key / FROST public-share identifier (33 bytes = compressed secp/ed point) |\n| `bonded_amount` | `u64` | Raw LST amount currently held in the bond vault |\n| `stake_weight` | `u64` | SOL-denominated economic weight derived from `bonded_amount` via the Pyth oracle |\n| `partials_submitted` | `u64` | Lifetime count of partial signatures submitted |\n| `slash_count` | `u32` | Number of times this operator has been slashed |\n| `jailed` | `bool` | `true` when unbonding or when bond falls below `min_bond`; operator cannot sign while jailed |\n| `unbonding_at` | `u64` | Slot at which unbonding completes; `0` while actively bonded |\n| `joined_slot` | `u64` | Slot the operator joined the signing set |\n| `bump` | `u8` | PDA canonical bump |\n\n### `SigningRequest`\n\nA user's cross-VM signing intent and its aggregation progress.\n\nSeeds: `[b\"request\", protocol_pubkey, request_id.to_le_bytes()]`\nSpace: `8 + 224 = 232 bytes`\n\n| Field | Type | Description |\n|---|---|---|\n| `protocol` | `Pubkey` | Owning protocol PDA |\n| `requester` | `Pubkey` | Account that posted the intent; receives rent refund on close |\n| `request_id` | `u64` | Monotonic id (taken from `protocol.request_nonce` at creation) |\n| `scheme` | `SignatureScheme` | Required signature scheme for the destination VM |\n| `target_vm` | `TargetVm` | Destination VM family |\n| `target_chain_id` | `u64` | Destination chain identifier (EVM chain id, Cosmos chain index, etc.) |\n| `message_hash` | `[u8; 32]` | 32-byte hash of the transaction or message to be signed off-chain |\n| `threshold` | `u16` | Minimum number of distinct partial signatures required |\n| `partials_collected` | `u16` | Partial signatures collected so far |\n| `stake_weight_collected` | `u64` | Staked economic weight accumulated so far |\n| `required_stake_weight` | `u64` | Economic-security target snapshotted at creation (`total_bonded × threshold_bps / 10_000`) |\n| `created_slot` | `u64` | Slot the request was created |\n| `expiry_slot` | `u64` | Slot after which the request can no longer be fulfilled |\n| `status` | `RequestStatus` | Current lifecycle state |\n| `aggregate_sig` | `[u8; 64]` | Running aggregate signature accumulator; populated on finalization |\n| `bump` | `u8` | PDA canonical bump |\n\n### `PartialSignature`\n\nA single operator's partial-signature contribution to a request. The PDA uniqueness constraint (`[b\"partial\", request, operator]`) enforces that each operator may submit exactly one share per request; a second attempt fails at account initialization with an `AccountAlreadyInitialized` error at the runtime level.\n\nSeeds: `[b\"partial\", request_pubkey, operator_pubkey]`\nSpace: `8 + 146 = 154 bytes`\n\n| Field | Type | Description |\n|---|---|---|\n| `request` | `Pubkey` | Request this share contributes to |\n| `operator` | `Pubkey` | Operator PDA that submitted the share |\n| `scheme` | `SignatureScheme` | Scheme of the share (must match the request scheme) |\n| `share` | `[u8; 64]` | 64-byte partial-signature share material |\n| `submitted_slot` | `u64` | Slot the share was submitted |\n| `stake_weight` | `u64` | Staked weight credited for this contribution |\n| `bump` | `u8` | PDA canonical bump |\n\n---\n\n## Instructions\n\n```mermaid\nsequenceDiagram\n    participant Admin\n    participant Operator\n    participant User\n    participant Program as Unbridge Program\n    participant Relayer\n\n    Admin->>Program: initialize(threshold_bps, min_bond, …)\n    Operator->>Program: register_operator(group_pubkey, bond_amount)\n    User->>Program: create_signing_request(scheme, target_vm, message_hash, …)\n    loop Until threshold met\n        Operator->>Program: submit_partial_signature(share)  // receipt\n    end\n    Note over Operator,Program: off-chain coordinator runs FROST/GG20\n    Operator->>Program: aggregate_and_emit(aggregate_sig)\n    Program-->>Relayer: AggregateSignatureEmitted\n    Relayer->>DestChain: broadcast aggregate signature\n```\n\n---\n\n### `initialize`\n\nBootstrap the protocol singleton. Creates the `Protocol` config account, the bonded-collateral vault, and the slash pool (both Token-2022 accounts owned by the protocol PDA). Can only be called once per deployment.\n\n```rust\npub fn initialize(\n    ctx: Context<Initialize>,\n    threshold_bps: u16,\n    min_bond: u64,\n    unbonding_slots: u64,\n    request_fee: u64,\n    max_validity_slots: u64,\n    lst_price_feed: Pubkey,\n) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `admin` | `Signer` | Becomes the initial admin authority |\n| `protocol` | `init`, PDA `[b\"protocol\"]`, `mut` | Singleton config account |\n| `bond_mint` | Token-2022 `Mint` | LST mint accepted as collateral |\n| `bond_vault` | `init`, PDA `[b\"bond_vault\", protocol]`, `mut` | Vault receiving operator bonds |\n| `slash_pool` | `init`, PDA `[b\"slash_pool\", protocol]`, `mut` | Pool receiving slashed collateral |\n| `token_program` | `TokenInterface` | Token-2022 program |\n| `system_program` | `Program<System>` | For account initialization |\n\n**Parameters**\n\n| Parameter | Type | Constraints | Description |\n|---|---|---|---|\n| `threshold_bps` | `u16` | `1 ≤ threshold_bps ≤ 10_000` | Fraction of total staked weight required to finalize a request |\n| `min_bond` | `u64` | `> 0` | Minimum LST bond to join the signing set |\n| `unbonding_slots` | `u64` | `> 0` (not validated on-chain, operator-set governance) | Slots between begin_unbonding and withdraw_bond |\n| `request_fee` | `u64` | any | Lamports per signing request; `0` to disable |\n| `max_validity_slots` | `u64` | `1 ≤ max_validity_slots ≤ 432_000` | Maximum allowed validity window for requests |\n| `lst_price_feed` | `Pubkey` | — | Pyth price account for LST → SOL valuation |\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `InvalidThreshold` | `threshold_bps < 1` or `threshold_bps > 10_000` |\n| `InsufficientBond` | `min_bond == 0` |\n| `InvalidValidityWindow` | `max_validity_slots < 1` or `max_validity_slots > 432_000` |\n\n---\n\n### `update_config`\n\nAdmin: tune live economic-security and liveness parameters. All fields are `Option`; only provided `Some` values are applied.\n\n```rust\npub fn update_config(\n    ctx: Context<AdminConfig>,\n    threshold_bps: Option<u16>,\n    min_bond: Option<u64>,\n    unbonding_slots: Option<u64>,\n    request_fee: Option<u64>,\n    max_validity_slots: Option<u64>,\n) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `admin` | `Signer`, must equal `protocol.admin` | Current admin |\n| `protocol` | PDA `[b\"protocol\"]`, `mut` | Config account to update |\n\n**Parameters**\n\nAll parameters mirror `initialize` semantics. Any field set to `None` is left unchanged.\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `Unauthorized` | Signer does not match `protocol.admin` |\n| `InvalidThreshold` | Provided `threshold_bps` out of `[1, 10_000]` |\n| `InsufficientBond` | Provided `min_bond` is `0` |\n| `InvalidValidityWindow` | Provided `max_validity_slots` out of `[1, 432_000]` |\n\n---\n\n### `transfer_admin`\n\nStep 1 of the two-step admin handover. Nominates a successor by writing `new_admin` into `Protocol.pending_admin`. The current admin retains control until `accept_admin` is called.\n\n```rust\npub fn transfer_admin(\n    ctx: Context<AdminConfig>,\n    new_admin: Pubkey,\n) -> Result<()>\n```\n\n**Accounts**\n\nSame as `update_config` (`AdminConfig` context).\n\n**Parameters**\n\n| Parameter | Type | Constraints | Description |\n|---|---|---|---|\n| `new_admin` | `Pubkey` | must not be `Pubkey::default()` | Nominated successor admin |\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `Unauthorized` | Signer does not match `protocol.admin` |\n| `InvalidAdminTransfer` | `new_admin == Pubkey::default()` |\n\n---\n\n### `accept_admin`\n\nStep 2 of the two-step admin handover. The nominated account (`pending_admin`) signs this transaction to accept the role. On success, `protocol.admin` is set to the nominee and `protocol.pending_admin` is reset to `Pubkey::default()`.\n\n```rust\npub fn accept_admin(ctx: Context<AcceptAdmin>) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `new_admin` | `Signer` | Must match `protocol.pending_admin` |\n| `protocol` | PDA `[b\"protocol\"]`, `mut` | Config account |\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `Unauthorized` | Signer does not match `protocol.pending_admin` |\n\n---\n\n### `pause`\n\nEmergency brake: sets `protocol.paused = true`. All user and operator state transitions (`register_operator`, `begin_unbonding`, `create_signing_request`, and partial submission) will immediately fail with `ProtocolPaused` until `unpause` is called. Admin-only management instructions (`update_config`, `transfer_admin`, `accept_admin`, `slash_operator`) remain callable while paused.\n\n```rust\npub fn pause(ctx: Context<AdminConfig>) -> Result<()>\n```\n\n**Accounts**: Same as `update_config`.\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `Unauthorized` | Signer does not match `protocol.admin` |\n\n---\n\n### `unpause`\n\nResumes normal operation by setting `protocol.paused = false`.\n\n```rust\npub fn unpause(ctx: Context<AdminConfig>) -> Result<()>\n```\n\n**Accounts**: Same as `update_config`.\n\n**Errors thrown**: Same as `pause`.\n\n---\n\n### `register_operator`\n\nJoin the signing set by bonding LST collateral. Transfers `bond_amount` of Token-2022 LST from the operator's token account into the protocol-owned `bond_vault`. The oracle-derived `stake_weight` is added to `protocol.total_bonded` immediately.\n\n```rust\npub fn register_operator(\n    ctx: Context<RegisterOperator>,\n    group_pubkey: [u8; 33],\n    bond_amount: u64,\n) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `authority` | `Signer` | Operator wallet; becomes `operator.authority` |\n| `protocol` | PDA `[b\"protocol\"]`, `mut` | Updated: `operator_count += 1`, `total_bonded += stake_weight` |\n| `operator` | `init`, PDA `[b\"operator\", protocol, authority]` | Created operator record |\n| `operator_token_account` | Token-2022, `mut` | Source of the LST bond |\n| `bond_vault` | PDA `[b\"bond_vault\", protocol]`, `mut` | Destination |\n| `bond_mint` | Token-2022 `Mint` | Required by `transfer_checked` |\n| `lst_price_feed` | Pyth price account | Must match `protocol.lst_price_feed` |\n| `token_program` | `TokenInterface` | Token-2022 |\n| `system_program` | `Program<System>` | For `operator` account initialization |\n\n**Parameters**\n\n| Parameter | Type | Constraints | Description |\n|---|---|---|---|\n| `group_pubkey` | `[u8; 33]` | non-zero | Compressed 33-byte group/FROST public key. Used by off-chain signing library to route share material |\n| `bond_amount` | `u64` | `≥ protocol.min_bond` | Raw LST units to bond |\n\n**Behavior details**\n\n- The bond is transferred via `transfer_checked` (Token-2022 CPI), which enforces `bond_mint.decimals` — no manual decimal math.\n- `stake_weight` is computed by `compute_stake_weight(&lst_price_feed, bond_amount)`. The exact oracle math is implemented off-chain; the on-chain effect is a proportional `u64` weight assigned to this operator.\n- `operator.unbonding_at` is initialized to `0`, indicating active bonded status.\n- Emits `OperatorRegistered { operator, authority, stake_weight }`.\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `ProtocolPaused` | `protocol.paused == true` |\n| `InsufficientBond` | `bond_amount < protocol.min_bond` |\n| `InvalidOracleAccount` | `lst_price_feed` key does not match `protocol.lst_price_feed` |\n| `StaleOraclePrice` | Oracle price data has exceeded freshness threshold |\n| `MathOverflow` | `total_bonded` or `operator_count` would overflow |\n\n**Example**\n\n```typescript\nconst bondAmount = new BN(1_000_000_000); // 1 LST (assuming 9 decimals)\nconst groupPubkey = Buffer.from(operatorCompressedPubkey); // 33 bytes\n\nawait program.methods\n  .registerOperator([...groupPubkey], bondAmount)\n  .accounts({\n    authority: wallet.publicKey,\n    protocol: protocolPda,\n    operator: operatorPda,\n    operatorTokenAccount: operatorAta,\n    bondVault: bondVaultPda,\n    bondMint: lstMint,\n    lstPriceFeed: pythFeedAccount,\n    tokenProgram: TOKEN_2022_PROGRAM_ID,\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([wallet])\n  .rpc();\n```\n\n---\n\n### `begin_unbonding`\n\nOperator: start the unbonding timer and exit the active signing set. The operator is immediately `jailed = true` and its `stake_weight` is subtracted from `protocol.total_bonded`. The operator can no longer be assigned to new requests, but its bond remains in the vault (still slashable) until `unbonding_at` is reached.\n\n```rust\npub fn begin_unbonding(ctx: Context<OperatorLifecycle>) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `authority` | `Signer` | Must match `operator.authority` |\n| `protocol` | PDA `[b\"protocol\"]`, `mut` | `total_bonded -= operator.stake_weight`; `operator_count -= 1` |\n| `operator` | PDA `[b\"operator\", protocol, authority]`, `mut` | Sets `unbonding_at`, `jailed = true` |\n\n**Behavior details**\n\n`operator.unbonding_at` is set to `current_slot + protocol.unbonding_slots`. While `unbonding_at != 0` and the current slot is before `unbonding_at`, the bond is still slashable — economic security is preserved during the exit window.\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `ProtocolPaused` | `protocol.paused == true` |\n| `AlreadyUnbonding` | `operator.unbonding_at != 0` |\n| `MathOverflow` | `current_slot + unbonding_slots` overflows `u64` |\n\n---\n\n### `withdraw_bond`\n\nOperator: reclaim the bond after the unbonding window has elapsed. Transfers `operator.bonded_amount` from the vault back to the operator's token account and closes the operator account (returning rent to the authority).\n\n```rust\npub fn withdraw_bond(ctx: Context<WithdrawBond>) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `authority` | `Signer` | Must match `operator.authority`; receives rent |\n| `protocol` | PDA `[b\"protocol\"]` | Signing authority for vault CPI |\n| `operator` | PDA `[b\"operator\", protocol, authority]`, `mut`, `close = authority` | Closed on success |\n| `operator_token_account` | Token-2022, `mut` | Receives the returned bond |\n| `bond_vault` | PDA `[b\"bond_vault\", protocol]`, `mut` | Source |\n| `bond_mint` | Token-2022 `Mint` | Required by `transfer_checked` |\n| `token_program` | `TokenInterface` | Token-2022 |\n\nThe vault transfer is signed by the protocol PDA using `[b\"protocol\", bump]` as signer seeds.\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `NotUnbonding` | `operator.unbonding_at == 0` (never called `begin_unbonding`) |\n| `UnbondingNotComplete` | `current_slot < operator.unbonding_at` |\n\n---\n\n### `slash_operator`\n\nAdmin: slash a misbehaving operator by moving `amount` LST from the bond vault to the slash pool. If the residual bond falls below `protocol.min_bond`, the operator is jailed. If the operator was active (not already unbonding/jailed) before the slash, `protocol.total_bonded` is reduced by the weight delta.\n\n```rust\npub fn slash_operator(\n    ctx: Context<SlashOperator>,\n    amount: u64,\n    reason: u8,\n) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `admin` | `Signer`, must equal `protocol.admin` | Slashing authority |\n| `protocol` | PDA `[b\"protocol\"]`, `mut` | Updated if operator was active |\n| `operator` | PDA of target operator, `mut` | Bond reduced; may be jailed |\n| `bond_vault` | PDA `[b\"bond_vault\", protocol]`, `mut` | Source |\n| `slash_pool` | PDA `[b\"slash_pool\", protocol]`, `mut` | Destination |\n| `bond_mint` | Token-2022 `Mint` | Required by `transfer_checked` |\n| `token_program` | `TokenInterface` | Token-2022 |\n\n**Parameters**\n\n| Parameter | Type | Description |\n|---|---|---|\n| `amount` | `u64` | LST amount to slash; must not exceed `operator.bonded_amount` |\n| `reason` | `u8` | Fraud-proof category code (equivocation = 0, invalid-share = 1, liveness = 2; encoding is off-chain convention, not validated on-chain) |\n\n**Behavior details**\n\nThe on-chain weight accounting after a slash:\n\n```\nnew_weight = compute_stake_weight(oracle, operator.bonded_amount - amount)\nweight_delta = old_weight - new_weight\n\nif operator was active:\n    protocol.total_bonded -= weight_delta\n    if operator.bonded_amount - amount < min_bond:\n        protocol.total_bonded -= new_weight   // also remove remaining weight\n        protocol.operator_count -= 1\n        operator.jailed = true\n```\n\nEmits `OperatorSlashed { operator, amount, reason }`.\n\nIn production, `slash_operator` is gated by a fraud-proof verified off-chain by the signing library before the admin submits this transaction. The on-chain code enforces collateral movement and jailing; fraud-proof logic itself is not implemented on-chain.\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `Unauthorized` | Signer does not match `protocol.admin` |\n| `SlashAmountExceedsBond` | `amount > operator.bonded_amount` |\n| `MathOverflow` | `slash_count` increment overflows |\n\n---\n\n### `create_signing_request`\n\nUser: post a cross-VM signing intent for the operator set to fulfill. Charges `protocol.request_fee` lamports via `system_program::transfer` and allocates a `SigningRequest` account. Snapshots `required_stake_weight = total_bonded × threshold_bps / 10_000` at creation time so threshold evaluation is deterministic against the operator set at the moment of posting.\n\n```rust\npub fn create_signing_request(\n    ctx: Context<CreateSigningRequest>,\n    scheme: SignatureScheme,\n    target_vm: TargetVm,\n    target_chain_id: u64,\n    message_hash: [u8; 32],\n    threshold: u16,\n    validity_slots: u64,\n) -> Result<()>\n```\n\n**Accounts**\n\n| Account | Constraint | Description |\n|---|---|---|\n| `requester` | `Signer` | Pays request fee and request account rent |\n| `protocol` | PDA `[b\"protocol\"]`, `mut` | Receives fee; `request_nonce` incremented |\n| `signing_request` | `init`, PDA `[b\"request\", protocol, request_nonce.to_le_bytes()]` | Created signing intent |\n| `system_program` | `Program<System>` | For fee transfer and account init |\n\n**Parameters**\n\n| Parameter | Type | Constraints | Description |\n|---|---|---|---|\n| `scheme` | `SignatureScheme` | valid variant | Cryptographic scheme for the target VM |\n| `target_vm` | `TargetVm` | valid variant | Destination VM family |\n| `target_chain_id` | `u64` | any | Chain identifier on the destination network |\n| `message_hash` | `[u8; 32]` | at least one non-zero byte | 32-byte hash of the off-chain payload |\n| `threshold` | `u16` | `1 ≤ threshold ≤ protocol.operator_count` | Minimum partial signatures to collect |\n| `validity_slots` | `u64` | `1 ≤ validity_slots ≤ protocol.max_validity_slots` | Request lifetime in slots |\n\n**Errors thrown**\n\n| Error | Condition |\n|---|---|\n| `ProtocolPaused` | `protocol.paused == true` |\n| `NoActiveOperators` | `protocol.operator_count == 0` |\n| `EmptyMessageHash` | All 32 bytes of `message_hash` are `0x00` |\n| `InvalidThreshold` | `threshold < 1` or `threshold > protocol.operator_count` |\n| `InvalidValidityWindow` | `validity_slots < 1` or `validity_slots > protocol.max_validity_slots` |\n| `MathOverflow` | `total_bonded × threshold_bps` overflows before division |\n\n**Example**\n\n```typescript\n\n// Encode the EVM transaction you want signed\nconst txPayload = encodeFunctionData({ abi, functionName: \"transfer\", args: [...] });\nconst messageHash = Array.from(keccak256(toBytes(txPayload)));\n\nawait program.methods\n  .createSigningRequest(\n    { gg20Secp256k1: {} },          // SignatureScheme variant\n    { evm: {} },                     // TargetVm variant\n    new BN(8453),                    // Base chain id\n    messageHash,                     // [u8; 32]\n    5,                               // threshold: at least 5 partials\n    new BN(750),                     // validity_slots: ~5 min\n  )\n  .accounts({\n    requester: wallet.publicKey,\n    protocol: protocolPda,\n    signingRequest: requestPda,      // derived from protocol + nonce\n    systemProgram: SystemProgram.programId,\n  })\n  .signers([wallet])\n  .rpc();\n```\n\n**Scheme × VM matrix**\n\n| `target_vm` | Required `scheme` | Off-chain signer |\n|---|---|---|\n| `Svm` | `FrostEd25519` | `engine/kobe` |\n| `Evm` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n| `Tron` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n| `Cosmos` | `FrostEd25519` | `engine/kobe` |\n| `Bitcoin` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n\nThe program does not validate the scheme-vm pairing itself; the requester selects it at `create_signing_request`, and the off-chain coordinator runs the matching signer. A wrong pairing produces a group signature the destination chain will reject — it is not caught on-chain.\n\n---\n\n## Partial Submission and Finalization\n\nThe `submit_partial_signature` and `aggregate_and_emit` instructions complete the request lifecycle. The on-chain flow is:\n\n```mermaid\nstateDiagram-v2\n    [*] --> Pending : create_signing_request\n    Pending --> Pending : submit_partial_signature\\n(partials_collected < threshold)\n    Pending --> Aggregated : aggregate_and_emit\\n(stake_weight_collected ≥ required_stake_weight\\nAND partials_collected ≥ threshold)\n    Pending --> Cancelled : cancel_request (requester)\n    Pending --> Expired : expire_request\\n(current_slot > expiry_slot)\n    Aggregated --> [*]\n    Cancelled --> [*]\n    Expired --> [*]\n```\n\nEach `submit_partial_signature` call:\n1. Initializes a `PartialSignature` PDA at `[b\"partial\", request, operator]` — the PDA uniqueness prevents double-submit without any explicit duplicate check.\n2. Copies `scheme` from the parent `SigningRequest` onto the receipt (the caller does not supply a scheme, so there is no scheme-mismatch path).\n3. Validates the operator is not jailed/unbonding (throws `OperatorJailed`).\n4. Validates the request is `Pending` and has not passed `expiry_slot` (throws `RequestNotPending`, `RequestExpired`).\n5. Records the receipt: credits `stake_weight_collected`, increments `partials_collected`, and emits `PartialSignatureSubmitted`. It does not fold the share bytes.\n\n`aggregate_and_emit(aggregate_sig)` records the canonical 64-byte group signature into `request.aggregate_sig`, sets `status = Aggregated`, and emits `AggregateSignatureEmitted`. The aggregate is computed off-chain by the coordinator using real FROST (`engine/kobe`) or GG20 (`engine/kobe-ecdsa`) and passed as an instruction argument; the program records it after checking the thresholds and a non-zero-signature guard, without performing any elliptic-curve verification on-chain (verification is done by the destination chain upon broadcast).\n\n---\n\n## Events\n\nFive events are emitted via Anchor's `emit!` macro and appear in the transaction log: `OperatorRegistered`, `OperatorSlashed`, `SigningRequestCreated`, `PartialSignatureSubmitted`, and `AggregateSignatureEmitted`. Off-chain indexers and relayers subscribe to these for the full request lifecycle.\n\n### `OperatorRegistered`\n\n```rust\npub struct OperatorRegistered {\n    pub operator: Pubkey,   // OperatorPDA\n    pub authority: Pubkey,  // operator.authority\n    pub stake_weight: u64,  // oracle-derived weight at join time\n}\n```\n\nEmitted by: `register_operator`\n\n### `OperatorSlashed`\n\n```rust\npub struct OperatorSlashed {\n    pub operator: Pubkey,  // OperatorPDA\n    pub amount: u64,       // LST amount moved to slash pool\n    pub reason: u8,        // fraud-proof category code\n}\n```\n\nEmitted by: `slash_operator`\n\n### `SigningRequestCreated`\n\nEmitted by: `create_signing_request`.\n\n### `PartialSignatureSubmitted`\n\n```rust\npub struct PartialSignatureSubmitted {\n    pub request: Pubkey,\n    pub operator: Pubkey,\n    pub partials_collected: u16,\n    pub stake_weight_collected: u64,\n}\n```\n\nEmitted by: `submit_partial_signature` (one per recorded receipt).\n\n### `AggregateSignatureEmitted`\n\n```rust\npub struct AggregateSignatureEmitted {\n    pub request: Pubkey,\n    pub request_id: u64,\n    pub scheme: SignatureScheme,\n    pub target_vm: TargetVm,\n    pub target_chain_id: u64,\n    pub message_hash: [u8; 32],\n    pub aggregate_sig: [u8; 64],\n}\n```\n\nEmitted by: `aggregate_and_emit` when the coordinator posts the canonical group signature.\n\n---\n\n## Error Reference\n\nAll errors are defined in `programs/distin/src/errors.rs` as variants of `UnbridgeError`. Anchor encodes these starting at offset `6000` in the program's error namespace.\n\n| Variant | Message | Common trigger |\n|---|---|---|\n| `ProtocolPaused` | \"Protocol is paused\" | Any user/operator call while `protocol.paused == true` |\n| `Unauthorized` | \"Caller is not authorized for this action\" | Admin instruction signed by non-admin; `accept_admin` signed by non-nominee |\n| `InvalidThreshold` | \"Threshold must be between 1 and the active operator count / 10000 bps\" | `threshold_bps` outside `[1, 10_000]`; per-request `threshold > operator_count` |\n| `InsufficientBond` | \"Bond amount is below the configured minimum\" | `bond_amount < protocol.min_bond`; `min_bond == 0` in initialize |\n| `OperatorJailed` | \"Operator is jailed or unbonding and cannot sign\" | Jailed operator attempts `submit_partial_signature` |\n| `AlreadyUnbonding` | \"Operator is already unbonding\" | Calling `begin_unbonding` a second time |\n| `NotUnbonding` | \"Operator has not begun unbonding\" | Calling `withdraw_bond` without prior `begin_unbonding` |\n| `UnbondingNotComplete` | \"Unbonding period has not elapsed yet\" | `current_slot < operator.unbonding_at` |\n| `RequestExpired` | \"Signing request has expired\" | `submit_partial_signature` after `expiry_slot` |\n| `RequestNotPending` | \"Signing request is not in a pending state\" | Acting on `Aggregated`, `Cancelled`, or `Expired` request |\n| `ThresholdNotMet` | \"Collected staked weight or partial count is below threshold\" | Premature finalization attempt |\n| `RequestAlreadyFinalized` | \"Signing request has already been finalized\" | Second finalization attempt |\n| `MalformedPartialSignature` | \"Partial signature share is malformed\" | 64-byte `share` fails off-chain format check |\n| `EmptyMessageHash` | \"Message hash must be non-empty\" | `message_hash` is all-zero bytes |\n| `StaleOraclePrice` | \"Oracle price is stale\" | Pyth price account has aged past the staleness threshold |\n| `InvalidOracleAccount` | \"Oracle account does not match the configured price feed\" | `lst_price_feed` passed does not match `protocol.lst_price_feed` |\n| `InvalidVault` | \"Provided vault or pool account is invalid\" | Vault/pool key mismatch |\n| `InvalidValidityWindow` | \"Validity window is outside the allowed bounds\" | `validity_slots` or `max_validity_slots` outside `[1, 432_000]` |\n| `NoActiveOperators` | \"No active operators in the signing set\" | `create_signing_request` when `protocol.operator_count == 0` |\n| `SlashAmountExceedsBond` | \"Slash amount exceeds the operator's bonded collateral\" | `amount > operator.bonded_amount` in `slash_operator` |\n| `InvalidAdminTransfer` | \"Invalid admin transfer target\" | `new_admin == Pubkey::default()` |\n| `MathOverflow` | \"Arithmetic overflow\" | `checked_add`/`checked_mul` returns `None` |\n\n---\n\n## Account Size Reference\n\n| Account | INIT_SPACE | Total (with discriminator) |\n|---|---|---|\n| `Protocol` | 248 bytes | 256 bytes |\n| `Operator` | 143 bytes | 151 bytes |\n| `SigningRequest` | 224 bytes | 232 bytes |\n| `PartialSignature` | 146 bytes | 154 bytes |\n\nRent values at the Solana mainnet minimum (0.00000348 SOL/byte):\n\n| Account | Approx. rent-exempt balance |\n|---|---|\n| `Protocol` | ~0.00179 SOL |\n| `Operator` | ~0.00107 SOL |\n| `SigningRequest` | ~0.00163 SOL |\n| `PartialSignature` | ~0.00108 SOL |\n\nOperators pay `SigningRequest` and `PartialSignature` rent at instruction time. `SigningRequest` rent is refunded to `requester` when the account is closed on finalization, cancellation, or expiry.\n\n---\n\n## Economic Security Invariant\n\nThe threshold check at finalization evaluates two conditions simultaneously:\n\n```\npartials_collected >= request.threshold\nAND\nstake_weight_collected >= request.required_stake_weight\n```\n\nwhere:\n\n```\nrequest.required_stake_weight = protocol.total_bonded × protocol.threshold_bps / 10_000\n```\n\n`required_stake_weight` is **snapshotted at request creation**, not re-evaluated at finalization. This means:\n- Operators that unbond or are slashed after a request is created do not lower the bar for existing requests.\n- Operators that join after a request is created can still contribute partials (their `stake_weight` is added to `stake_weight_collected`), but the snapshot target does not increase.\n\nBoth conditions must be true. A large number of small-weight operators can meet the `partials_collected >= threshold` count without satisfying the stake-weight floor, and vice versa if a single high-weight operator cannot provide enough partials on its own."
  },
  {
    "slug": "errors",
    "title": "Error Reference",
    "body": "# Error Reference\n\nComplete reference for every custom error the Unbridge on-chain program can emit. Each entry maps the Anchor error code to its exact trigger condition in `programs/distin/src/lib.rs`, the on-chain state that caused it, and the concrete steps to resolve it.\n\nProgram ID: `4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6`\nError enum: `UnbridgeError` in `programs/distin/src/errors.rs`\nAnchor base offset for custom errors: **6000**\nTotal variants: **22** (codes `6000`–`6021`)\n\n---\n\n## Quick Reference\n\n| Code | Name | Anchor # | Message |\n|------|------|----------|---------|\n| `ProtocolPaused` | Protocol frozen | 6000 | Protocol is paused |\n| `Unauthorized` | Wrong signer | 6001 | Caller is not authorized for this action |\n| `InvalidThreshold` | Out-of-range bps or count | 6002 | Threshold must be between 1 and the active operator count / 10000 bps |\n| `InsufficientBond` | Bond below minimum | 6003 | Bond amount is below the configured minimum |\n| `OperatorJailed` | Jailed operator attempt | 6004 | Operator is jailed or unbonding and cannot sign |\n| `AlreadyUnbonding` | Double unbond | 6005 | Operator is already unbonding |\n| `NotUnbonding` | Premature withdrawal | 6006 | Operator has not begun unbonding |\n| `UnbondingNotComplete` | Too early to withdraw | 6007 | Unbonding period has not elapsed yet |\n| `RequestExpired` | Slot deadline passed | 6008 | Signing request has expired |\n| `RequestNotPending` | Wrong lifecycle state | 6009 | Signing request is not in a pending state |\n| `ThresholdNotMet` | Insufficient weight/count | 6010 | Collected staked weight or partial count is below threshold |\n| `RequestAlreadyFinalized` | Post-finalization write | 6011 | Signing request has already been finalized |\n| `MalformedPartialSignature` | Bad share bytes | 6012 | Partial signature share is malformed |\n| `EmptyMessageHash` | All-zero hash | 6013 | Message hash must be non-empty |\n| `StaleOraclePrice` | Price feed too old | 6014 | Oracle price is stale |\n| `InvalidOracleAccount` | Wrong price feed key | 6015 | Oracle account does not match the configured price feed |\n| `InvalidVault` | Bad vault/pool account | 6016 | Provided vault or pool account is invalid |\n| `InvalidValidityWindow` | Slot range out of bounds | 6017 | Validity window is outside the allowed bounds |\n| `NoActiveOperators` | Empty signing set | 6018 | No active operators in the signing set |\n| `SlashAmountExceedsBond` | Slash > bond | 6019 | Slash amount exceeds the operator's bonded collateral |\n| `InvalidAdminTransfer` | Zero pubkey target | 6020 | Invalid admin transfer target |\n| `MathOverflow` | u64 checked arithmetic | 6021 | Arithmetic overflow |\n\n---\n\n## Error Taxonomy\n\n```mermaid\ngraph TD\n    E[UnbridgeError] --> P[Protocol-Level]\n    E --> O[Operator Lifecycle]\n    E --> R[Request Lifecycle]\n    E --> X[Cryptographic / Schema]\n    E --> Q[Oracle & Vault]\n    E --> A[Arithmetic]\n\n    P --> P1[ProtocolPaused]\n    P --> P2[Unauthorized]\n    P --> P3[InvalidAdminTransfer]\n\n    O --> O1[InsufficientBond]\n    O --> O2[OperatorJailed]\n    O --> O3[AlreadyUnbonding]\n    O --> O4[NotUnbonding]\n    O --> O5[UnbondingNotComplete]\n    O --> O6[SlashAmountExceedsBond]\n\n    R --> R1[InvalidThreshold]\n    R --> R2[InvalidValidityWindow]\n    R --> R3[NoActiveOperators]\n    R --> R4[EmptyMessageHash]\n    R --> R5[RequestExpired]\n    R --> R6[RequestNotPending]\n    R --> R7[ThresholdNotMet]\n    R --> R8[RequestAlreadyFinalized]\n\n    X --> X1[MalformedPartialSignature]\n\n    Q --> Q1[StaleOraclePrice]\n    Q --> Q2[InvalidOracleAccount]\n    Q --> Q3[InvalidVault]\n\n    A --> A1[MathOverflow]\n```\n\n---\n\n## Protocol-Level Errors\n\n### `ProtocolPaused` — 6000\n\n**Message:** `Protocol is paused`\n\nThe `paused: bool` flag on the singleton `Protocol` account (PDA seed `[b\"protocol\"]`) is `true`. Every user-facing and operator-facing state transition gate-checks this flag before doing any work:\n\n```rust\n// register_operator, begin_unbonding, create_signing_request all start with:\nrequire!(!protocol.paused, UnbridgeError::ProtocolPaused);\n```\n\nThe flag is set by `pause(ctx: Context<AdminConfig>)` and cleared by `unpause(ctx: Context<AdminConfig>)`. Both instructions require `ctx.accounts.admin.key() == protocol.admin`.\n\n**Affected instructions:** `register_operator`, `begin_unbonding`, `create_signing_request`, `submit_partial` (any instruction that calls `require!(!protocol.paused, ...)`).\n\n**Resolution:**\n\n1. Confirm the pause is intentional. Check `Protocol.paused` on-chain:\n\n```bash\nanchor account distin.Protocol 4xy9dYHfAzi7cAcX5JHxNR6EoMJ9PGfeQDMHx6YUQQM6\n```\n\n2. If you are the admin (`Protocol.admin`), call `unpause`:\n\n```typescript\nawait program.methods.unpause().accounts({ protocol, admin }).rpc();\n```\n\n3. If you are not the admin, wait for the protocol to be unpaused or contact the admin (`Protocol.admin` field).\n\n**Edge case:** A transaction that reads `paused = false` at simulation time can still land `ProtocolPaused` if another transaction calling `pause` is confirmed in the same slot. The protocol processes instructions in slot order; retry after the unpause transaction is confirmed.\n\n---\n\n### `Unauthorized` — 6001\n\n**Message:** `Caller is not authorized for this action`\n\nTriggered exclusively in `accept_admin` when the signer does not match `Protocol.pending_admin`:\n\n```rust\nrequire_keys_eq!(\n    protocol.pending_admin,\n    ctx.accounts.new_admin.key(),\n    UnbridgeError::Unauthorized\n);\n```\n\nThe two-step admin handover flow is:\n\n```mermaid\nsequenceDiagram\n    participant Current as Current Admin\n    participant Program as Unbridge Program\n    participant Nominee as Nominee\n\n    Current->>Program: transfer_admin(new_admin: Pubkey)\n    Note over Program: pending_admin = new_admin\n    Nominee->>Program: accept_admin()\n    Program->>Program: require pending_admin == signer\n    Note over Program: admin = pending_admin<br/>pending_admin = Pubkey::default()\n```\n\n**Resolution:** The nominee (`new_admin` as set by `transfer_admin`) must be the transaction signer for `accept_admin`. Verify:\n\n```typescript\n// pending_admin must equal the key that signs this tx\nconst { pendingAdmin } = await program.account.protocol.fetch(protocolPda);\nconsole.log(\"Must sign with:\", pendingAdmin.toBase58());\n```\n\n---\n\n### `InvalidAdminTransfer` — 6020\n\n**Message:** `Invalid admin transfer target`\n\nTriggered in `transfer_admin` if `new_admin` is `Pubkey::default()` (all-zero 32 bytes):\n\n```rust\nrequire_keys_neq!(new_admin, Pubkey::default(), UnbridgeError::InvalidAdminTransfer);\n```\n\n**Resolution:** Pass a real 32-byte public key as `new_admin`. The zero pubkey is rejected to prevent accidentally locking out the admin role.\n\n---\n\n## Operator Lifecycle Errors\n\n### `InsufficientBond` — 6003\n\n**Message:** `Bond amount is below the configured minimum`\n\nTriggered in two contexts:\n\n| Instruction | Condition |\n|-------------|-----------|\n| `initialize` | `min_bond == 0` |\n| `update_config` | `min_bond == Some(0)` |\n| `register_operator` | `bond_amount < protocol.min_bond` |\n\nThe `min_bond` field is stored in the `Protocol` account as a raw LST token amount (not lamports). The Token-2022 mint used is `Protocol.bond_mint`.\n\n```rust\n// initialize / update_config\nrequire!(min_bond > 0, UnbridgeError::InsufficientBond);\n\n// register_operator\nrequire!(bond_amount >= protocol.min_bond, UnbridgeError::InsufficientBond);\n```\n\n**Resolution:**\n\n```typescript\n// Read the current minimum before attempting to register\nconst { minBond } = await program.account.protocol.fetch(protocolPda);\n// bond_amount must be >= minBond in raw token units (respect decimals)\nconst bondAmount = minBond; // or higher\nawait program.methods\n  .registerOperator(groupPubkey, new BN(bondAmount))\n  .accounts({ ... })\n  .rpc();\n```\n\n**Edge case:** `min_bond` can be raised by `update_config` after an operator is already bonded. Existing operators with bonds below the new minimum are not retroactively jailed by `update_config` itself; the slashing path (`slash_operator`) applies the check by comparing `operator.bonded_amount < min_bond` post-slash and jailing if true.\n\n---\n\n### `OperatorJailed` — 6004\n\n**Message:** `Operator is jailed or unbonding and cannot sign`\n\nAn `Operator` account has `jailed: bool = true`. This field is set to `true` by:\n\n- `begin_unbonding` (operator voluntarily exits)\n- `slash_operator` when `operator.bonded_amount < protocol.min_bond` after the slash\n\nA jailed operator's `stake_weight` contribution is excluded from `Protocol.total_bonded`. The operator PDA (seed `[b\"operator\", protocol, authority]`) stores the flag at byte offset 113 in the `Operator` struct (after the fixed-width fields).\n\n**Resolution:** An operator that has begun unbonding cannot re-enter the signing set via the current program. The operator must `withdraw_bond` after the unbonding window and then call `register_operator` again with a fresh bond. There is no `unjail` instruction in the current on-chain program.\n\n---\n\n### `AlreadyUnbonding` — 6005\n\n**Message:** `Operator is already unbonding`\n\n```rust\n// begin_unbonding\nrequire!(operator.unbonding_at == 0, UnbridgeError::AlreadyUnbonding);\n```\n\n`Operator.unbonding_at` is set to `clock.slot + protocol.unbonding_slots` on the first call to `begin_unbonding`. Any subsequent call to `begin_unbonding` for the same operator account hits this guard.\n\n**Resolution:** Check `Operator.unbonding_at`. If non-zero, the operator is already in the unbonding period. Wait until `clock.slot >= unbonding_at` and call `withdraw_bond` to close the account. There is no way to cancel an in-progress unbonding.\n\n---\n\n### `NotUnbonding` — 6006\n\n**Message:** `Operator has not begun unbonding`\n\n```rust\n// withdraw_bond\nrequire!(operator.unbonding_at != 0, UnbridgeError::NotUnbonding);\n```\n\n`withdraw_bond` is called before `begin_unbonding`. The operator account's `unbonding_at` field is `0` (the default for an actively bonded operator).\n\n**Resolution:** Call `begin_unbonding` first. This sets `unbonding_at`, jails the operator, and removes its weight from `Protocol.total_bonded` and `Protocol.operator_count`.\n\n---\n\n### `UnbondingNotComplete` — 6007\n\n**Message:** `Unbonding period has not elapsed yet`\n\n```rust\n// withdraw_bond\nrequire!(\n    clock.slot >= operator.unbonding_at,\n    UnbridgeError::UnbondingNotComplete\n);\n```\n\nThe current slot has not yet reached `Operator.unbonding_at`. The unbonding window is `Protocol.unbonding_slots` slots long (set at `initialize`, tunable via `update_config`). At ~400 ms per slot this translates to roughly:\n\n| `unbonding_slots` | Real-world duration |\n|-------------------|---------------------|\n| 216,000 | ~24 hours |\n| 432,000 | ~48 hours |\n| 1,296,000 | ~6 days |\n\n**Resolution:**\n\n```typescript\nconst { unbondingAt } = await program.account.operator.fetch(operatorPda);\nconst slot = await connection.getSlot();\nconst slotsLeft = unbondingAt.toNumber() - slot;\nconst secondsLeft = slotsLeft * 0.4;\nconsole.log(`Withdraw available in ~${Math.ceil(secondsLeft)}s`);\n```\n\n---\n\n### `SlashAmountExceedsBond` — 6019\n\n**Message:** `Slash amount exceeds the operator's bonded collateral`\n\n```rust\n// slash_operator\nrequire!(\n    amount <= ctx.accounts.operator.bonded_amount,\n    UnbridgeError::SlashAmountExceedsBond\n);\n```\n\nThe `slash_operator` instruction moves `amount` from `bond_vault` to `slash_pool` via a PDA-signed `transfer_checked` CPI (signer seeds `[b\"protocol\", &[protocol.bump]]`). Passing `amount > operator.bonded_amount` would overdraw the vault if allowed through, so it is rejected before the CPI.\n\n**Resolution:** Read `Operator.bonded_amount` and cap the slash amount to that value. Partial slashes are supported; only a full-bond slash (amount == bonded\\_amount) will drain the operator completely.\n\n```typescript\nconst op = await program.account.operator.fetch(operatorPda);\nconst slashAmount = op.bondedAmount; // full slash\nawait program.methods\n  .slashOperator(new BN(slashAmount), reason)\n  .accounts({ ... })\n  .rpc();\n```\n\n---\n\n## Request Lifecycle Errors\n\n### `InvalidThreshold` — 6002\n\n**Message:** `Threshold must be between 1 and the active operator count / 10000 bps`\n\nThe same error name covers two distinct range checks in two distinct contexts:\n\n**Context A — protocol-wide bps threshold (`initialize` / `update_config`)**\n\n```rust\nrequire!(\n    threshold_bps as u64 >= 1 && threshold_bps as u64 <= BPS_DENOMINATOR,\n    UnbridgeError::InvalidThreshold\n);\n// BPS_DENOMINATOR = 10_000\n```\n\n`threshold_bps: u16` in the `Protocol` account must fall in `[1, 10_000]`. A value of `6_000` means 60% of `total_bonded` must be accumulated before a request finalizes.\n\n**Context B — per-request operator count threshold (`create_signing_request`)**\n\n```rust\nrequire!(\n    threshold >= 1 && (threshold as u32) <= protocol.operator_count,\n    UnbridgeError::InvalidThreshold\n);\n```\n\nHere `threshold: u16` is the minimum number of distinct partial signatures (by operator count, not weight) the request requires. It is bounded by the live `Protocol.operator_count` at creation time.\n\n| Field | Account | Type | Range |\n|-------|---------|------|-------|\n| `threshold_bps` | `Protocol` | `u16` | 1–10,000 |\n| `threshold` (per request) | `SigningRequest` | `u16` | 1–`operator_count` |\n\n**Resolution:** For protocol config, pass a value in `[1, 10_000]`. For per-request threshold, query `Protocol.operator_count` first and set `threshold <= operator_count`.\n\n---\n\n### `InvalidValidityWindow` — 6017\n\n**Message:** `Validity window is outside the allowed bounds`\n\nChecked in three places:\n\n```rust\n// initialize / update_config (max_validity_slots on the protocol)\nrequire!(\n    max_validity_slots >= 1 && max_validity_slots <= MAX_VALIDITY_SLOTS_CEILING,\n    UnbridgeError::InvalidValidityWindow\n);\n// MAX_VALIDITY_SLOTS_CEILING = 432_000  (~48h at 400ms/slot)\n\n// create_signing_request (per-request validity_slots)\nrequire!(\n    validity_slots >= 1 && validity_slots <= protocol.max_validity_slots,\n    UnbridgeError::InvalidValidityWindow\n);\n```\n\n`Protocol.max_validity_slots` acts as a ceiling that admins can tighten below `MAX_VALIDITY_SLOTS_CEILING`. A user request's `validity_slots` must be within `[1, protocol.max_validity_slots]`.\n\n**Resolution:** Read `Protocol.max_validity_slots` before building the request instruction:\n\n```typescript\nconst { maxValiditySlots } = await program.account.protocol.fetch(protocolPda);\n// Pass any value in [1, maxValiditySlots.toNumber()]\nconst validitySlots = Math.min(desiredSlots, maxValiditySlots.toNumber());\n```\n\n---\n\n### `NoActiveOperators` — 6018\n\n**Message:** `No active operators in the signing set`\n\n```rust\n// create_signing_request\nrequire!(protocol.operator_count > 0, UnbridgeError::NoActiveOperators);\n```\n\n`Protocol.operator_count` is a `u32` incremented by `register_operator` and decremented by `begin_unbonding` and `slash_operator` (when the operator is subsequently jailed). If the set empties, request creation is blocked until at least one operator bonds.\n\n**Resolution:** One or more operators must call `register_operator` with `bond_amount >= Protocol.min_bond`. Monitor the set before submitting requests:\n\n```typescript\nconst { operatorCount } = await program.account.protocol.fetch(protocolPda);\nif (operatorCount === 0) throw new Error(\"No active signing operators\");\n```\n\n---\n\n### `EmptyMessageHash` — 6013\n\n**Message:** `Message hash must be non-empty`\n\n```rust\n// create_signing_request\nrequire!(message_hash.iter().any(|b| *b != 0), UnbridgeError::EmptyMessageHash);\n```\n\nThe `message_hash: [u8; 32]` parameter (stored as `SigningRequest.message_hash`) must have at least one non-zero byte. All-zero 32 bytes is rejected to prevent accidental or malicious null-hash requests that operators would otherwise be obligated to sign.\n\n**Resolution:** Hash the actual transaction payload intended for the destination chain before calling `create_signing_request`. For EVM targets (`TargetVm::Evm` / `SignatureScheme::Gg20Secp256k1`) this is typically `keccak256(rlp(tx))`; for SVM targets (`SignatureScheme::FrostEd25519`) it is `sha256(message_bytes)`.\n\n---\n\n### `RequestExpired` — 6008\n\n**Message:** `Signing request has expired`\n\nA `SigningRequest` expires at slot `expiry_slot`, which is computed at creation as:\n\n```\nexpiry_slot = clock.slot + validity_slots\n```\n\nAfter `expiry_slot` the request's `status` transitions to `RequestStatus::Expired` and any `submit_partial` or finalization instruction will hit this error. The `expiry_slot` is stored in the `SigningRequest` account (see Account Layout below).\n\n```mermaid\nstateDiagram-v2\n    [*] --> Pending: create_signing_request\n    Pending --> Aggregated: threshold met before expiry_slot\n    Pending --> Expired: clock.slot > expiry_slot\n    Pending --> Cancelled: admin cancel (not in current program)\n    Aggregated --> [*]: relayer broadcasts on destination chain\n    Expired --> [*]: requester recovers rent\n```\n\n**Resolution:** Submit partial signatures within the `validity_slots` window. If the window is too tight and `RequestExpired` fires before threshold is met, the requester must create a new `SigningRequest` with a larger `validity_slots` value (up to `Protocol.max_validity_slots`).\n\n---\n\n### `RequestNotPending` — 6009\n\n**Message:** `Signing request is not in a pending state`\n\n`submit_partial` and the aggregation/finalization instruction both require `SigningRequest.status == RequestStatus::Pending`. Trying to submit a partial to an already-`Aggregated`, `Cancelled`, or `Expired` request triggers this error.\n\n`RequestStatus` is a one-byte discriminated enum stored in `SigningRequest.status`:\n\n| Variant | Wire value | Meaning |\n|---------|-----------|---------|\n| `Pending` | `0` | Accepting partial signatures |\n| `Aggregated` | `1` | Threshold met; aggregate sig published |\n| `Cancelled` | `2` | Administratively cancelled |\n| `Expired` | `3` | `expiry_slot` passed before threshold met |\n\n**Resolution:** Read `SigningRequest.status` before submitting. Only `0` (Pending) allows further writes.\n\n---\n\n### `ThresholdNotMet` — 6010\n\n**Message:** `Collected staked weight or partial count is below threshold`\n\nThe finalization check requires both conditions simultaneously:\n\n- `SigningRequest.partials_collected >= SigningRequest.threshold` (operator count gate)\n- `SigningRequest.stake_weight_collected >= SigningRequest.required_stake_weight` (economic security gate)\n\n`required_stake_weight` is snapshotted at creation:\n\n```rust\nlet required_stake_weight = protocol.total_bonded\n    .checked_mul(protocol.threshold_bps as u64)\n    .ok_or(UnbridgeError::MathOverflow)?\n    / BPS_DENOMINATOR;\n```\n\nSo if `total_bonded = 500_000` and `threshold_bps = 6_000`, then `required_stake_weight = 300_000`. Weight from jailed or unbonding operators does not count toward `stake_weight_collected`.\n\n**Resolution:** Wait for more operators to submit partials. Track progress:\n\n```typescript\nconst req = await program.account.signingRequest.fetch(requestPda);\nconst pctWeight = req.stakeWeightCollected.toNumber() / req.requiredStakeWeight.toNumber();\nconst pctCount  = req.partialsCollected / req.threshold;\nconsole.log(`Weight: ${(pctWeight * 100).toFixed(1)}%  Count: ${(pctCount * 100).toFixed(1)}%`);\n```\n\nBoth must reach 100% for finalization to succeed.\n\n---\n\n### `RequestAlreadyFinalized` — 6011\n\n**Message:** `Signing request has already been finalized`\n\nThe request `status` is `Aggregated` (1). The `aggregate_sig: [u8; 64]` field in `SigningRequest` has already been written. Any subsequent finalization attempt is rejected.\n\n**Resolution:** Read the `aggregate_sig` from the finalized `SigningRequest` account and hand it to the off-chain relayer for broadcast on the destination chain. The signature is already final and chain-native; no further on-chain action is required.\n\n---\n\n## Cryptographic / Schema Errors\n\n### `MalformedPartialSignature` — 6012\n\n**Message:** `Partial signature share is malformed`\n\nThe `share: [u8; 64]` field in a `PartialSignature` submission failed structural validation. The exact validation is delegated to the off-chain `engine/kobe` / `engine/kobe-ecdsa` signing libraries and the program calls into their verification logic. An all-zero share or a share whose length does not match the declared `scheme` triggers this error.\n\n`PartialSignature` account layout for reference (seed `[b\"partial\", request, operator]`):\n\n| Field | Type | Bytes |\n|-------|------|-------|\n| `request` | `Pubkey` | 32 |\n| `operator` | `Pubkey` | 32 |\n| `scheme` | `SignatureScheme` | 1 |\n| `share` | `[u8; 64]` | 64 |\n| `submitted_slot` | `u64` | 8 |\n| `stake_weight` | `u64` | 8 |\n| `bump` | `u8` | 1 |\n| Anchor discriminator | | 8 |\n| **Total** | | **154 bytes** |\n\n**Resolution:** Verify the `share` bytes using the appropriate signing library before submitting the instruction. For FROST (Ed25519): the 64 bytes encode `(R, s)` per RFC 8032. For GG20 (secp256k1): the 64 bytes encode the `(r, s)` components without the recovery byte.\n\n---\n\n### There Is No Scheme-Mismatch Error\n\n`UnbridgeError` has no scheme-mismatch variant. The `scheme: SignatureScheme` on a `PartialSignature` is copied from the parent `SigningRequest` rather than supplied by the submitter, so there is no untrusted-scheme input path to reject. The scheme is fixed once at `create_signing_request`:\n\n| `TargetVm` | `SignatureScheme` | Off-chain signer |\n|------------|-------------------|------------------|\n| `Svm` | `FrostEd25519` | `engine/kobe` |\n| `Cosmos` | `FrostEd25519` | `engine/kobe` |\n| `Evm` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n| `Tron` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n| `Bitcoin` | `Gg20Secp256k1` | `engine/kobe-ecdsa` |\n\n```rust\npub enum SignatureScheme {\n    FrostEd25519,   // SVM / Cosmos SDK chains\n    Gg20Secp256k1,  // EVM / BTC / Tron\n}\n```\n\nChoosing the wrong scheme for a VM is a request-construction mistake, not a partial-submission error: it surfaces as a destination-chain rejection of the final signature, not as an on-chain error.\n\n---\n\n## Oracle and Vault Errors\n\n### `StaleOraclePrice` — 6014\n\n**Message:** `Oracle price is stale`\n\nTriggered inside `compute_stake_weight`, a private helper called by `register_operator` and `slash_operator` to convert a raw LST bond amount into SOL-denominated `stake_weight`. The LST price feed account is `Protocol.lst_price_feed` (a Pyth price account pubkey). If the Pyth price timestamp is too old relative to `Clock::get()?.unix_timestamp`, the helper returns `UnbridgeError::StaleOraclePrice`.\n\n**Resolution:**\n\n1. Ensure the Pyth publisher is actively pricing the bond LST. Check the feed on-chain.\n2. Operators should not attempt to register or be slashed during oracle outages; retry once fresh pricing is available.\n3. If you are running a devnet deployment with a mock price feed, ensure your mock publisher is cranking.\n\n---\n\n### `InvalidOracleAccount` — 6015\n\n**Message:** `Oracle account does not match the configured price feed`\n\n`compute_stake_weight` validates that the account passed as `lst_price_feed` in the instruction's remaining accounts matches `Protocol.lst_price_feed`:\n\n```rust\n// (within compute_stake_weight)\nrequire_keys_eq!(\n    lst_price_feed.key(),\n    protocol.lst_price_feed,\n    UnbridgeError::InvalidOracleAccount\n);\n```\n\n**Resolution:** Pass exactly `Protocol.lst_price_feed` as the oracle account. This pubkey is set in `initialize` and can only be changed via `update_config` (admin only). Do not substitute a different Pyth account.\n\n---\n\n### `InvalidVault` — 6016\n\n**Message:** `Provided vault or pool account is invalid`\n\nAccount constraints in `initialize` and the bond/slash CPI paths validate that `bond_vault` and `slash_pool` are the expected PDAs (seeds `[b\"bond_vault\", protocol]` and `[b\"slash_pool\", protocol]` respectively). Passing any other token account triggers this error.\n\n**Resolution:** Derive the PDA addresses correctly:\n\n```typescript\nconst [bondVault] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"bond_vault\"), protocolPda.toBuffer()],\n  programId\n);\nconst [slashPool] = PublicKey.findProgramAddressSync(\n  [Buffer.from(\"slash_pool\"), protocolPda.toBuffer()],\n  programId\n);\n```\n\nBoth are Token-2022 accounts owned by the protocol PDA and initialized in `initialize`.\n\n---\n\n## Arithmetic Error\n\n### `MathOverflow` — 6021\n\n**Message:** `Arithmetic overflow`\n\nThe program uses Rust's `checked_*` arithmetic throughout and surfaces any `None` result as `MathOverflow`. Every site where this can occur:\n\n| Instruction | Expression |\n|-------------|-----------|\n| `register_operator` | `total_bonded.checked_add(stake_weight)` |\n| `register_operator` | `operator_count.checked_add(1)` |\n| `begin_unbonding` | `clock.slot.checked_add(unbonding_slots)` |\n| `slash_operator` | `slash_count.checked_add(1)` |\n| `create_signing_request` | `total_bonded.checked_mul(threshold_bps as u64)` |\n| `create_signing_request` | `request_nonce.checked_add(1)` (implied by nonce increment) |\n| `submit_partial` | `stake_weight_collected.checked_add(operator.stake_weight)` |\n| `submit_partial` | `partials_collected.checked_add(1)` |\n\nAll fields involved are `u64` (or `u32` for `operator_count`). `MathOverflow` in practice signals either:\n\n- `total_bonded` exceeding `u64::MAX` (essentially impossible under realistic bond sizes)\n- `clock.slot + unbonding_slots` overflowing `u64` (impossible: Solana genesis slot is 0, and `MAX_VALIDITY_SLOTS_CEILING = 432_000` is far from u64 max)\n- Malicious or misconfigured `threshold_bps × total_bonded` overflow (`threshold_bps` is bounded to `10_000` and `total_bonded` is bounded by realistic LST supply)\n\n**Resolution:** `MathOverflow` in production indicates a protocol invariant violation or a deeply unexpected state. Log the full instruction context, read all relevant account fields, and file a bug report. Do not retry blindly.\n\n---\n\n## PDA Seed Reference (for error context)\n\nWhen decoding an error, the account at fault is often identifiable by its PDA seed:\n\n| Account | Seeds | Size (INIT_SPACE + 8) |\n|---------|-------|-----------------------|\n| `Protocol` | `[b\"protocol\"]` | 256 bytes |\n| `bond_vault` | `[b\"bond_vault\", protocol]` | Token-2022 TokenAccount |\n| `slash_pool` | `[b\"slash_pool\", protocol]` | Token-2022 TokenAccount |\n| `Operator` | `[b\"operator\", protocol, authority]` | 151 bytes |\n| `SigningRequest` | `[b\"request\", protocol, request_id_le]` | 232 bytes |\n| `PartialSignature` | `[b\"partial\", request, operator]` | 154 bytes |\n\n`request_id_le` is the 8-byte little-endian encoding of `Protocol.request_nonce` at the time of request creation.\n\n---\n\n## Decoding Errors in TypeScript\n\nAnchor serializes custom errors with code `6000 + variant_index`. To decode programmatically:\n\n```typescript\n\ntry {\n  await program.methods.createSigningRequest(...).rpc();\n} catch (err) {\n  if (err instanceof AnchorError) {\n    console.log(\"Error code:\", err.error.errorCode.number);   // e.g. 6013\n    console.log(\"Error name:\", err.error.errorCode.code);     // e.g. \"EmptyMessageHash\"\n    console.log(\"Error msg: \", err.error.errorMessage);       // human-readable\n    console.log(\"Program:   \", err.program.toBase58());\n  }\n}\n```\n\nRaw transaction logs expose the code as:\n\n```\nProgram log: AnchorError thrown in programs/distin/src/lib.rs:NNN.\nProgram log: Error Code: EmptyMessageHash. Error Number: 6013.\nProgram log: Error Message: Message hash must be non-empty.\n```\n\n---\n\n## Error Frequency in Normal Operation\n\n| Error | Normal? | Operator | User | Admin |\n|-------|---------|----------|------|-------|\n| `ProtocolPaused` | Rare | Wait / retry | Wait / retry | Unpause |\n| `InsufficientBond` | Occasional | Increase bond amount | — | — |\n| `AlreadyUnbonding` | Idempotent guard | Don't call twice | — | — |\n| `UnbondingNotComplete` | Timing | Wait for slot | — | — |\n| `EmptyMessageHash` | Integration bug | — | Fix hash derivation | — |\n| `InvalidValidityWindow` | Config mismatch | — | Read `max_validity_slots` | Tune config |\n| `NoActiveOperators` | Bootstrap state | Register | Retry later | — |\n| `RequestExpired` | Timing / liveness | Sign faster | Increase `validity_slots` | — |\n| `ThresholdNotMet` | Liveness | More partials needed | Wait | — |\n| `StaleOraclePrice` | Oracle outage | Retry | Retry | Monitor feed |\n| `MathOverflow` | Bug / extreme state | Report | Report | Report |"
  },
  {
    "slug": "faq",
    "title": "FAQ",
    "body": "# Frequently Asked Questions\n\n---\n\n## 1. What exactly is Unbridge, and how is it different from a traditional bridge?\n\nTraditional bridges work by locking asset A on chain X and minting a wrapped IOU on chain Y. The user ends up holding a synthetic derivative backed by a custodial vault, a multisig, or an optimistic challenge game. Unbridge does none of that.\n\nUnbridge is a **threshold-signature coordination layer**. When you want to move or sign on chain Y, Unbridge's operator set produces a valid **native signature** that chain Y's own RPC would accept directly — the same Ed25519 or secp256k1 bytes that the destination VM checks natively. There is no wrapped token, no liquidity pool to route through, and no IOU to redeem.\n\n```mermaid\nsequenceDiagram\n    participant User\n    participant Unbridge as Unbridge Program<br/>(Solana)\n    participant Operators as Signing Operators\n    participant Relayer as Off-chain Relayer\n    participant DestChain as Destination Chain\n\n    User->>Unbridge: create_signing_request(message_hash, scheme, target_vm, ...)\n    Note over Unbridge: Snapshots required_stake_weight,<br/>opens SigningRequest PDA\n    Operators->>Unbridge: submit_partial_signature (× N, receipts)\n    Note over Unbridge: Records stake_weight_collected + partials_collected<br/>until ≥ required_stake_weight AND<br/>partials_collected ≥ threshold\n    Note over Operators: off-chain coordinator runs real FROST/GG20\n    Operators->>Unbridge: aggregate_and_emit(aggregate_sig)\n    Unbridge->>Relayer: Emits AggregateSignatureEmitted (64-byte sig)\n    Relayer->>DestChain: Broadcasts native tx signed by the group key\n    Note over DestChain: Verifies native Ed25519 / secp256k1 —<br/>no bridge contract needed\n```\n\nThe trust assumption shifts from \"trust the bridge's custodians\" to \"trust that a staked-weight majority of bonded operators is honest.\" Those operators have slashable LST collateral on the line for every request they process.\n\n---\n\n## 2. Which chains and signature schemes are supported?\n\nThe program defines two enums — `SignatureScheme` and `TargetVm` — that together determine which off-chain signer is invoked. The scheme is fixed once on the `SigningRequest` at creation; a `PartialSignature` carries that scheme by copy rather than accepting a caller-supplied value, so there is no scheme-mismatch path on submission.\n\n| `TargetVm` | `SignatureScheme` | Off-chain library | Native verification |\n|---|---|---|---|\n| `Svm` | `FrostEd25519` | `engine/kobe` | Ed25519 program |\n| `Cosmos` | `FrostEd25519` | `engine/kobe` | Tendermint secp/ed |\n| `Evm` | `Gg20Secp256k1` | `engine/kobe-ecdsa` | `ecrecover` |\n| `Tron` | `Gg20Secp256k1` | `engine/kobe-ecdsa` | Tron's `ecrecover` variant |\n| `Bitcoin` | `Gg20Secp256k1` | *(not yet released)* | Script `OP_CHECKSIG` |\n\n> **FrostEd25519** uses FROST Schnorr over Ed25519 — the threshold aggregation produces a standard Ed25519 signature indistinguishable from a single-key signature on the destination chain. **Gg20Secp256k1** uses GG20-style threshold ECDSA over secp256k1, producing a standard `(r, s, v)` tuple that any EVM contract's `ecrecover` accepts.\n\nThe `target_chain_id` field (a `u64`) carries the chain-specific discriminator: EVM chain ID for EVM targets, a Cosmos chain index for Cosmos, and so on. Unbridge does not validate the numeric value itself — the off-chain relayer is responsible for routing to the correct RPC.\n\n---\n\n## 3. How does the threshold calculation work? There seem to be two thresholds.\n\nThere are indeed two separate, independent threshold conditions, and **both must be satisfied** before a request can be finalized:\n\n**a) Stake-weight threshold (economic security)**\n\nSet globally in `Protocol.threshold_bps` (e.g., `6667` = two-thirds majority). At request creation, `create_signing_request` snapshots:\n\n```\nrequired_stake_weight = (total_bonded × threshold_bps) / 10_000\n```\n\n`BPS_DENOMINATOR = 10_000` is the hard-coded denominator. This value is frozen into `SigningRequest.required_stake_weight` at creation time — a spike in `total_bonded` after the fact cannot raise the bar retroactively. As each operator submits a partial signature, their `Operator.stake_weight` is added to `SigningRequest.stake_weight_collected`. Finalization requires:\n\n```\nstake_weight_collected >= required_stake_weight\n```\n\n**b) Operator-count threshold (sybil floor)**\n\n`create_signing_request` accepts a `threshold: u16` argument — the minimum number of **distinct** operators that must contribute partial shares. This prevents a single operator with an overwhelming bond from satisfying the economic threshold alone. The program enforces `threshold >= 1` and `threshold <= operator_count`. As partials arrive, `partials_collected` is incremented; finalization also requires:\n\n```\npartials_collected >= threshold\n```\n\nIf either condition is unmet when finalization is attempted, the program returns `UnbridgeError::ThresholdNotMet`.\n\n```mermaid\nflowchart TD\n    A[submit_partial_signature] --> B{Operator jailed?}\n    B -- yes --> ERR1[OperatorJailed]\n    B -- no --> D{Request expired?}\n    D -- yes --> ERR3[RequestExpired]\n    D -- no --> E[Record receipt:\\nstake_weight_collected += weight,\\npartials_collected += 1]\n    E --> F{Both thresholds met?}\n    F -- no --> G[Pending — wait for more receipts]\n    F -- yes --> H[coordinator computes aggregate;\\naggregate_and_emit records it, status=Aggregated]\n```\n\n---\n\n## 4. What happens to my signing request if operators don't respond in time?\n\nEvery request has a hard deadline. When you call `create_signing_request`, you supply `validity_slots: u64`. The program computes:\n\n```\nexpiry_slot = Clock::slot + validity_slots\n```\n\nThe ceiling is `MAX_VALIDITY_SLOTS_CEILING = 432_000 slots`. At 400 ms per slot that is approximately 48 hours. The floor is 1 slot. Any value outside `[1, 432_000]` returns `UnbridgeError::InvalidValidityWindow`.\n\nOnce `Clock::slot > expiry_slot`, any further partial submission or finalization attempt returns `UnbridgeError::RequestExpired`. At that point the request's `status` field holds `RequestStatus::Expired`. The `SigningRequest` PDA can then be closed by the relayer or the requester to reclaim rent lamports; the protocol does **not** automatically close expired accounts.\n\nPractical guidance: for EVM targets where the destination chain is itself slow, set `validity_slots` long enough to give operators several Solana epochs. For Svm-to-Svm intents, a window of a few hundred slots (2–3 minutes) is typically sufficient because Solana's 400 ms slots make the coordination loop near-real-time.\n\n---\n\n## 5. How does operator bonding and slashing work?\n\n### Bonding\n\nAn operator calls `register_operator`, providing:\n- `bond_amount: u64` — raw LST token units (must be `>= Protocol.min_bond`)\n- `group_pubkey: [u8; 33]` — compressed group public key / FROST public-share identifier\n\nThe program pulls `bond_amount` from the operator's Token-2022 account into the protocol-owned `bond_vault` PDA via `transfer_checked`. The bond is **not** SOL — it is whatever Token-2022 LST mint is registered in `Protocol.bond_mint`.\n\n`compute_stake_weight` converts the raw LST amount to a SOL-denominated economic weight using the Pyth price feed configured in `Protocol.lst_price_feed`. The result is stored in `Operator.stake_weight` and added to `Protocol.total_bonded`.\n\n### Slashing\n\nAdmin calls `slash_operator(amount: u64, reason: u8)`. The program:\n1. Validates `amount <= operator.bonded_amount` (else `SlashAmountExceedsBond`)\n2. Moves `amount` from `bond_vault` → `slash_pool` via `transfer_checked` (PDA-signed)\n3. Recomputes `Operator.stake_weight` from the residual `bonded_amount`\n4. Jails the operator (`operator.jailed = true`) if `bonded_amount < min_bond` after slashing\n5. Decrements `Protocol.total_bonded` and `Protocol.operator_count` for active operators\n\nThe on-chain effect is immediate; the fraud-proof generation (equivocation / invalid-share / liveness fault detection) is performed off-chain by the `engine/kobe` / `engine/kobe-ecdsa` signing libraries and submitted to the admin before this instruction is called.\n\n| Condition | Effect |\n|---|---|\n| `bonded_amount` drops below `min_bond` after slash | Operator auto-jailed |\n| Operator was active (not already unbonding/jailed) | `total_bonded` and `operator_count` adjusted |\n| Operator was already unbonding | Weight adjustment skipped (already subtracted on `begin_unbonding`) |\n\nAn `OperatorSlashed` event is emitted with the operator pubkey, slashed amount, and reason code.\n\n---\n\n## 6. How long must an operator wait before withdrawing their bond?\n\nWithdrawal is a two-step process gated by `Protocol.unbonding_slots`.\n\n**Step 1 — `begin_unbonding`:**\n\n```\noperator.unbonding_at = Clock::slot + protocol.unbonding_slots\noperator.jailed = true\n```\n\nThe operator is immediately removed from the active signing set (`total_bonded` and `operator_count` decremented). Importantly, jailing means the operator **cannot take on new requests** while the bond is still slashable — this prevents a slash-window exploit where an operator exits right before a fraud proof lands.\n\n**Step 2 — `withdraw_bond`:**\n\nOnly callable once `Clock::slot >= operator.unbonding_at`. If called early, the program returns `UnbridgeError::UnbondingNotComplete`. On success, the full `operator.bonded_amount` is transferred from `bond_vault` back to the operator's token account (PDA-signed), and the `Operator` account is closed, returning its rent to the `authority`.\n\n```\nwithdraw_bond constraints:\n  operator.unbonding_at != 0        → else NotUnbonding\n  Clock::slot >= operator.unbonding_at  → else UnbondingNotComplete\n```\n\nIf an operator calls `begin_unbonding` a second time while already unbonding, the program returns `UnbridgeError::AlreadyUnbonding`.\n\n---\n\n## 7. What is the request fee, and where does it go?\n\nThe `Protocol.request_fee` field holds a lamport amount. When a user calls `create_signing_request` and the fee is nonzero, the program transfers that many lamports from the requester's account to the `Protocol` PDA itself using a `system_program::transfer` CPI:\n\n```rust\n// excerpt from create_signing_request\nif protocol.request_fee > 0 {\n    system_program::transfer(\n        CpiContext::new(...),\n        protocol.request_fee,\n    )?;\n}\n```\n\nThe lamports accumulate in the `Protocol` PDA's native account balance. There is no on-chain distribution mechanism in the current program — fee sweeps to operator reward pools or the treasury are handled off-chain by the admin. The fee is set during `initialize` and can be updated at any time by the admin via `update_config(request_fee: Some(new_fee))`.\n\nA fee of `0` is valid; the CPI is skipped entirely.\n\n---\n\n## 8. What does the aggregate signature actually look like, and how does it reach the destination chain?\n\nThe `SigningRequest.aggregate_sig` field is a `[u8; 64]` slot, zero until finalization — the chain does not accumulate it. Its encoding depends on the scheme:\n\n- **`FrostEd25519`**: standard 64-byte Ed25519 signature `(R || s)` — verifiable by any Ed25519 library without modification.\n- **`Gg20Secp256k1`**: 64-byte compact ECDSA signature `(r || s)` — the `v` recovery bit is communicated out-of-band by the relayer based on the message hash.\n\nOnce both threshold conditions are satisfied, the off-chain coordinator computes the canonical group signature and posts it via `aggregate_and_emit`, which records the 64 bytes into `aggregate_sig`, sets `status = RequestStatus::Aggregated`, and emits the `AggregateSignatureEmitted` event.\n\nThe off-chain relayer monitors Solana for `Aggregated` requests, reads the `aggregate_sig`, and broadcasts a native transaction on the destination chain. **The relayer is not part of the on-chain program** — it is a permissionless off-chain daemon. Any party watching the Unbridge program's logs can relay the signature; there is no designated relayer role in the contract.\n\n```\nSigningRequest PDA (after finalization)\n┌────────────────────────────────────────────────────────┐\n│ status            : Aggregated                         │\n│ aggregate_sig     : [u8; 64]  ← 64 bytes, scheme-enc  │\n│ partials_collected: threshold (or more)                │\n│ stake_weight_collected: ≥ required_stake_weight        │\n└────────────────────────────────────────────────────────┘\n           │\n           ▼ off-chain relayer reads & broadcasts\n┌────────────────────────────────────────────────────────┐\n│ Destination chain receives a *native* signed tx        │\n│ No bridge contract, no wrapped token mint/burn         │\n└────────────────────────────────────────────────────────┘\n```\n\n---\n\n## 9. What are the on-chain accounts and how are their PDAs derived?\n\nAll accounts are Anchor PDAs derived with fixed seeds. The table below lists every seed, the space (rent-exempt size), and the key fields:\n\n| Account | Seeds | Space | Key Fields |\n|---|---|---|---|\n| `Protocol` | `[b\"protocol\"]` | 256 bytes (248 + 8 disc) | `threshold_bps`, `total_bonded`, `request_nonce`, `paused` |\n| `bond_vault` | `[b\"bond_vault\", protocol]` | Token-2022 account | Holds active operator bonds |\n| `slash_pool` | `[b\"slash_pool\", protocol]` | Token-2022 account | Receives slashed collateral |\n| `Operator` | `[b\"operator\", protocol, authority]` | 151 bytes (143 + 8 disc) | `stake_weight`, `jailed`, `unbonding_at` |\n| `SigningRequest` | `[b\"request\", protocol, request_id_le]` | 232 bytes (224 + 8 disc) | `scheme`, `target_vm`, `aggregate_sig`, `status` |\n| `PartialSignature` | `[b\"partial\", request, operator]` | 154 bytes (146 + 8 disc) | `share: [u8; 64]`, `stake_weight` |\n\n`request_id_le` is the little-endian encoding of `Protocol.request_nonce` at the time the request is created. The nonce is monotonically incremented, which guarantees globally unique PDA addresses for all time.\n\nThe `PartialSignature` PDA seed `[b\"partial\", request, operator]` naturally enforces **uniqueness**: an operator cannot submit two shares for the same request because the PDA would already be initialized, causing Anchor's account-init constraint to fail.\n\n---\n\n## 10. What errors can I encounter, and what do they mean?\n\n| Error | Anchor code | When it fires |\n|---|---|---|\n| `ProtocolPaused` | `6000` | Admin has called `pause`; all user/operator state transitions blocked |\n| `Unauthorized` | `6001` | Caller is not the admin, or pending admin mismatch in `accept_admin` |\n| `InvalidThreshold` | `6002` | `threshold_bps` outside `[1, 10_000]`, or request `threshold` outside `[1, operator_count]` |\n| `InsufficientBond` | `6003` | `bond_amount < min_bond` on `register_operator`, or `min_bond = 0` on `update_config` |\n| `OperatorJailed` | `6004` | Jailed or unbonding operator attempts to submit a partial signature |\n| `AlreadyUnbonding` | `6005` | `begin_unbonding` called on an operator where `unbonding_at != 0` |\n| `NotUnbonding` | `6006` | `withdraw_bond` called before `begin_unbonding` |\n| `UnbondingNotComplete` | `6007` | `withdraw_bond` called before `Clock::slot >= unbonding_at` |\n| `RequestExpired` | `6008` | `Clock::slot > expiry_slot` when submitting a partial or finalizing |\n| `RequestNotPending` | `6009` | Request `status != Pending` (already `Aggregated`, `Cancelled`, or `Expired`) |\n| `ThresholdNotMet` | `6010` | Finalization attempted but `stake_weight_collected < required_stake_weight` or `partials_collected < threshold` |\n| `RequestAlreadyFinalized` | `6011` | Attempt to write to a request already in `Aggregated` state |\n| `MalformedPartialSignature` | `6012` | 64-byte share fails the off-chain library's validity check |\n| `EmptyMessageHash` | `6013` | All 32 bytes of `message_hash` are zero |\n| `StaleOraclePrice` | `6014` | Pyth price feed account slot is too far behind `Clock::slot` |\n| `InvalidOracleAccount` | `6015` | Oracle account key does not match `Protocol.lst_price_feed` |\n| `InvalidVault` | `6016` | Provided vault or slash pool does not match the protocol config |\n| `InvalidValidityWindow` | `6017` | `validity_slots` outside `[1, 432_000]` |\n| `NoActiveOperators` | `6018` | `create_signing_request` called when `operator_count == 0` |\n| `SlashAmountExceedsBond` | `6019` | Slash `amount > operator.bonded_amount` |\n| `InvalidAdminTransfer` | `6020` | `transfer_admin` called with `Pubkey::default()` as the target |\n| `MathOverflow` | `6021` | Checked arithmetic overflowed (nonce increment, weight accumulation, etc.) |\n\n---\n\n## 11. How does the emergency pause work, and how is admin authority transferred safely?\n\n### Emergency Pause\n\nThe admin calls `pause`, which sets `Protocol.paused = true`. All instructions that change user or operator state check this flag first and return `UnbridgeError::ProtocolPaused` immediately. Read-only queries (account fetches) are unaffected.\n\nThe admin calls `unpause` to resume normal operation.\n\nNeither instruction requires additional accounts beyond the admin signer and the `Protocol` PDA. They are designed for rapid response — a single transaction halts the entire protocol.\n\n### Two-Step Admin Handover\n\nTo prevent accidental renouncement to an invalid key, admin transfer is a two-transaction process:\n\n```mermaid\nsequenceDiagram\n    participant OldAdmin\n    participant Program as Unbridge Program\n    participant NewAdmin\n\n    OldAdmin->>Program: transfer_admin(new_admin: Pubkey)\n    Note over Program: protocol.pending_admin = new_admin\n    NewAdmin->>Program: accept_admin()\n    Note over Program: Verifies signer == pending_admin<br/>protocol.admin = pending_admin<br/>protocol.pending_admin = Pubkey::default()\n```\n\n`transfer_admin` rejects `Pubkey::default()` (`UnbridgeError::InvalidAdminTransfer`). `accept_admin` verifies that the transaction signer's pubkey matches `Protocol.pending_admin` exactly (`UnbridgeError::Unauthorized` if not). Until the nominee calls `accept_admin`, the old admin retains full authority. The `Protocol.pending_admin` field reverts to `Pubkey::default()` after a successful handover.\n\n---\n\n## 12. \"Bridge-free\" — aren't there still trust assumptions?\n\nYes, and Unbridge is explicit about it. \"Bridge-free\" means no wrapped tokens, no liquidity pools, no custodial vault on either chain. It does **not** mean trustless in the cryptographic sense. Here is the honest breakdown:\n\n| Property | Unbridge's answer |\n|---|---|\n| **Does it issue wrapped tokens?** | No. The destination chain receives a native signature, not a synthetic asset. |\n| **Is there a trusted custodian?** | No single custodian. There is a **staked-weight majority** of bonded operators. |\n| **What is the economic security?** | At any given moment: `Protocol.total_bonded × threshold_bps / 10_000` units of SOL-equivalent collateral must collude to forge a signature. |\n| **Can the admin steal funds?** | The admin controls `pause`, `update_config`, and `slash_operator`. Slashed funds go to `slash_pool` (a protocol-owned PDA), not the admin wallet. The admin cannot directly move user funds. |\n| **What if operators go offline?** | The request expires at `expiry_slot` with status `Expired`. No funds are moved; the user retries with a new request. |\n| **What stops an operator from signing arbitrary messages?** | The group key is a **threshold** key — no single operator holds enough shares to produce a valid aggregate signature alone. The security assumption is honest majority by stake weight. |\n| **Are the cryptographic proofs verified on-chain?** | No. Round validity and the group-combine run off-chain in `engine/kobe` (FROST Ed25519) and `engine/kobe-ecdsa` (GG20 secp256k1); the on-chain partial submission is a participation receipt, not a verified share. The program enforces accounting, economic thresholds, liveness deadlines, and a non-zero check on the recorded signature. This is a deliberate scope boundary — folding shares on-chain is meaningless and the SVM has no curve arithmetic for the group-combine. The off-chain aggregate is itself independently verified: `engine/kobe` checks against `ed25519-dalek`, and `engine/kobe-ecdsa` checks native-valid on Ethereum, Bitcoin, and Tron. |\n\nThe risk model is analogous to a staked validator set in a PoS chain: as long as over `threshold_bps / 10_000` of total staked weight is honest and online, the system produces correct signatures. The slashing mechanism in `slash_operator` is the deterrent; the unbonding window (`unbonding_slots`) is the slashing opportunity window."
  }
]
