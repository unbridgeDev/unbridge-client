[toolchain]
# Host toolchain (checks, tests, coordinator). SBF program builds use
# cargo-build-sbf's own pinned rustc, not this channel.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
